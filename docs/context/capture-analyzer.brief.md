---
subsystem: capture-analyzer
covers:
  - app.js:4900-6600
  - ui/library.js
updated: 2026-07-30
---

# Бриф: разбор захвата (capture analyzer)

Подсистема, которая превращает то, что владелец сказал или напечатал, в **типизированные
объекты**: смену, доход, расход, задачу, событие, цель, привычку, наблюдение, вопрос. Это ядро
продукта: всё остальное (граф, деньги, календарь, знание) питается её результатом. Если она
ошибается, ошибается вся система, и владелец видит это на первом же экране.

## 1. Что входит и что выходит

**Вход:** строка текста. Источник роли не играет — печатный ввод, расшифровка голосовой,
импортированный файл, сообщение в чате приходят в одну и ту же функцию `analyzeArtifactInput`.
Второй аргумент — метаданные файла (`{ name, kind, mime }`), третий — опции (`{ noSplit }`).

**Выход:** объект разбора. Ключевые поля:

| поле | смысл |
|---|---|
| `drafts` | черновики объектов: `{ draftId, type, title, group, destination, reason, quote, fields, confidence }` |
| `speechIntents` | понятые намерения речи с цитатами (см. §4) |
| `detectedClasses` | человеческие ярлыки классификации («смена», «доход», «дело») |
| `entities` | даты, время, суммы, ссылки, почты, wiki-ссылки, заголовки, люди |
| `confidence` | 0.35 при пустом разборе, до 0.96 |
| `uncertainties` | то, что требует решения владельца (например, неоднозначное время) |

**Черновик — не объект.** Он становится предложением (`addProposal`), предложение показывается
владельцу с предпросмотром, и только `applyProposal` создаёт настоящую запись и пишет чек.
Молча в базу не пишется ничего (CLAUDE.md §7).

## 2. Три слоя понимания, в порядке применения

### Слой A — границы смысла (`splitCaptureClauses`)
Надиктовка — это несколько смыслов подряд. Режем по границе предложения и по переводу строки.
**Перевод строки здесь важнее точки:** whisper.cpp отдаёт речь построчно, по паузам говорящего,
и это единственная граница, которую владелец действительно проговаривает. Ограничение объёма
(`CAPTURE_SPLIT_MAX_LENGTH = 600`) нужно ровно одному случаю — вставленному сплошным куском
документу; к речи оно не относится вовсе, у неё свой предел `TRANSCRIPT_SPLIT_MAX_CLAUSES = 40`.
Различаем их не длиной, а наличием переводов строки.

### Слой B — намерения (`extractSpeechIntents`)
Смысл речи живёт **между** предложениями: часы названы в первом, деньги в третьем, ставка не
названа вовсе. Слой читает надиктовку целиком и выдаёт намерения по схеме `SPEECH_INTENT_SCHEMA`
(`shift`, `income`, `expense`, `rate`, `meeting`, `task`, `observation`). Подробности — §4.

### Слой C — словарные правила (тело `analyzeArtifactInput`)
Флаги `isTask`, `isExpense`, `isIncome`, `isGoal`, `isHabit`, `isWorkPlan`, `isSelfObservation`
и т.д., каждый со своим черновиком. Работает по клаузам: клауза, объяснённая намерением, сюда
не попадает (иначе один смысл даёт два объекта).

## 3. Правила, купленные болью (не «упрощать»)

Каждое из них — след настоящей ошибки на экране владельца. Ломать их дёшево, восстанавливать
дорого.

- **Инфинитив в начале фразы = дело.** Опознаётся по гласной перед `-ть` (`оцен-И-ть`), потому
  что существительные на `-ть` идут через согласную («часть», «новость»). Словарь глаголов эту
  форму не покрывал, и «Оценить продажу машины» уходило в «не разобрано».
- **Границы слов в русском обязательны.** JS `\b` по кириллице не работает, поэтому все словари
  сигналов — регулярки с явными `(?<![А-Яа-яЁё])`. Без этого «поСЧИТАТЬ» читалось как «читать»,
  и «Посчитать свободные деньги» становилось ПРИВЫЧКОЙ; «на основании» читалось как «снова».
- **Дело или событие, но не оба.** Названное время делает запись событием дня, отсутствие —
  сроком дела. Раньше при любой дате заводились оба, один смысл давал два объекта с одним
  названием, и они объявлялись конфликтом друг с другом. Разводит их ЯЗЫК ДЕЙСТВИЯ, искать его
  надо во всей фразе: «сегодня в 11 вечера заказать еду» — это дело со временем.
- **Наблюдение о себе — не дело.** «После тренировки закрываю больше задач» нельзя выполнить и
  нечем закрыть, но это самый ценный вид записи. Признак — форма высказывания (сравнение или
  регулярность при отсутствии обязательства), а не словарь глаголов.
- **Название объекта — название, а не пересказ.** Обрезка стоит в одной точке, в `draft()`, на
  90 знаков. Достаточно забыть один раз, чтобы двадцать минут речи стали именем цели во весь
  экран — так и случилось.
- **Сильный тип не заводит задачу-двойника.** Трата, цель, привычка уже несут смысл записи;
  задача с тем же текстом после подтверждения выглядит как «дело, которое уже сделано».
  Сравнение — через `looksLikeSameObject` (энтропия + 3-граммы), не строкой.
- **Знание берётся из слов владельца, а не из тела заметки.** У расшифровки тело начинается со
  служебной шапки («Источник:», «Режим: локальный whisper.cpp»), и каждая её строка становилась
  «выводом». Хуже: шапка одинакова у всех расшифровок, и система находила, что две разные
  записи «об одном».

## 4. Слой намерений (RR-001 Этап B) — подробно

**Зачем.** Владелец надиктовал «поработал с 8 до 11, 3 часа… 316 после налога пришло… потом ещё
1700, получается 4700 всего… еду к Володе… надо в аптеку» и получил задачу с названием во весь
экран, расход 316 ₽ и ещё одну задачу. Заработок стал тратой, смена — делом.

**Контракт.** `SPEECH_INTENT_SCHEMA` описывает типы и поля. `validateSpeechIntents(intents,
sourceText)` — чистая логика без DOM и сети:
- тип обязан быть в схеме;
- **цитата обязана дословно находиться в исходнике** (сравнение по буквам и цифрам без знаков) —
  это единственная защита от выдуманного намерения, и она одинаково строга к регулярке и к
  модели;
- обязано быть хотя бы одно поле из `anyOf`;
- поля приводятся к типу (`time` = `ЧЧ:ММ`, `money` > 0, `hours` ≤ 24, `day` = `ГГГГ-ММ-ДД`).

**Что понимают правила.** Смену (глагол работы + часы диапазоном или прямо), денежный рассказ
(слово-итог смотрит вперёд: «получается 4700» — итог, названные до него суммы становятся полем
`parts`, а не отдельными записями), ставку (частное дохода на часы, помечена `derived: true` —
владелец её не произносил), встречу (глагол движения + имя человека).

**Поле `explains`.** Намерение объясняет не только свою цитату: доход объясняет и фразу с
частью. Клаузы из `explains` не разбираются повторно и не становятся «выводами».

**Слой модели** (`requestSpeechIntentsFromModel`) — за флагом `state.ollama.speechIntents`, по
умолчанию выключен. Та же схема, тот же валидатор; текст владельца идёт в промпт только
размеченным блоком как ДАННЫЕ. Замер 2026-07-28: `qwen2.5:3b` — 44 секунды и частично сломанный
JSON, «доход» угадан по части (316), а не по итогу (4700). **Модель здесь слабее правил**,
поэтому она только добавляет непонятое, а слияние (`mergeModelSpeechIntents`) сравнивает
объяснённые фразы, а не цитаты.

## 5. Границы: чего подсистема НЕ делает

- Не пишет в базу. Только черновики.
- Не разбирает числительные прописью. Замер на настоящем whisper.cpp `ggml-small`: числа
  приходят цифрами в 7 фразах из 8. Прописью покрыт был бы один случай из восьми.
- Не знает про экраны. Рендер — отдельный слой (`ui/*.js`).
- Не ходит в сеть, кроме явного слоя модели за флагом.

## 6. Как проверять изменения

Целевые спеки подсистемы (каждая названа болью, а не функцией):
`raw-speech-becomes-objects` · `one-record-one-meaning` · `spoken-pauses-are-meaning` ·
`voice-to-artifacts` · `event-or-task` · `observation-not-task` · `task-vs-habit` ·
`goal-deadline` · `weekday-deadline` · `person-stance` · `knowledge-not-machinery` ·
`money-fast-capture` · `shift-week-cycle`.

Быстрый замер без браузера-полного-прогона: `window.__lifeosKnowledgeBase.analyzeArtifactInput`
доступен со страницы, разбор чистый и синхронный.

## 6.1. Что разбор отдаёт петле

У каждого черновика из намерения `draftId` начинается со `speech-`, и журнал решений пишет по
нему `origin: "speech-intent"`. Поэтому точность слоя намерений считается отдельно от остальных
правил — и станет видно, где он ошибается, без единого вопроса владельцу
(`docs/context/self-improving-loop.brief.md`).

## 6.2. Разбор больше не единственный источник уверенности

Числа `0.9`, `0.84`, `0.7` в черновиках — ЗАЯВЛЕННАЯ уверенность. Рядом теперь считается
эмпирическая (`calibratedConfidence`), и подставлять её в черновик молча нельзя: И-2 требует,
чтобы смена правила была отдельной операцией. Пока она показывается в Контроле.

## 6.3. Черновик целого текста уступает намерениям

Правило, найденное съёмкой видео (П38), а не спекой: черновик, снятый со ВСЕГО текста, описывает
надиктовку как ОДИН смысл. Если намерения разобрали её на несколько — он заведомо неверен, и не
важно, совпал ли его тип с чьим-то ещё. Без этого три строки про смену давали правильные
смена+доход+ставку И задачу «Так поработал с 8 до 3 часа получается после налога пришло…».

## 6-бис. Деньги из речи пишутся не как с клавиатуры (замер 2026-07-30)

Замер на 24 записях настоящей речи ([docs/MEASURE_2026-07-30_SPEECH_INTENTS.md](../MEASURE_2026-07-30_SPEECH_INTENTS.md))
показал, что суммы терялись НА ЧИСЛЕ, а не на смысле: расшифровка ставит пробел разрядным
разделителем («вышло 6 800»), оставляет «тысяч» словом («28 тысяч») и теряет запятые («8 часов
4900»). Отсюда шесть правил, все в `core/ru-money.mjs`, и все — в одной точке на все пути разбора:

| Правило | Было до правки |
|---|---|
| `normalizeSpokenAmounts` — «28 тысяч» → 28000, «6 800» → 6800, «1,5 тысячи» → 1500 | «Заплатил за квартиру 28 тысяч» давало НОЛЬ объектов |
| `SPOKEN_UNIT_TAIL_RE` — число с единицей измерения или названием месяца деньгами не считается | «20 страниц за вечер» → расход 20 ₽, «до 10 августа» → расход 10 ₽ |
| часы смены берутся из фразы **не про сон** | «Спал 5 часов» → смена на 5 часов |
| одинокая сумма в рассказе о смене — заработок, и только когда трат не названо | «смену. 8 часов 4900» → РАСХОД 4900 ₽ |
| «осталось на карте» / «на счету» — баланс, а не трата | остаток уходил расходом: минус на счёте и никакого баланса |
| деньги смены не разбираются второй раз поклаузно | заработок был и в смене, и отдельным расходом |

`parseShiftEntry` живёт теперь в `core/ru-money.mjs` (вынесен из `app.js` по ратчету
`audit-module-size`). Ищи разбор смены там, а не грепом по монолиту.

## 7. Открытые долги

- Числительные прописью («тысячу двести») — сознательно не сделаны, см. §5. Подтверждено замером
  2026-07-30: whisper отдаёт числа цифрами в 22 записях из 24.
- «отработал с 9 до 19, 10 часов, вышло 6 800» даёт смену И календарный блок с искалеченным
  названием «Отработал с 9 до 10 часов вышло 6» — один смысл двумя объектами.
- Названия, из которых вырезаны цифры, читаются как сломанные: «Отложить .», «Стоит около тысяч.».
- Слой модели проверяется только на этой машине (Ollama локальный) — метка
  **[ЛОКАЛЬНАЯ ПРИЁМКА]**.
- `analyzeArtifactInput` — длинная функция с плоским списком флагов. При расколе `app.js`
  (v1.5 §4, пакет 2.2) она уезжает в модуль `capture-analyzer` целиком; дробить её раньше
  раскола нельзя, номера строк сдвигаются под второй сессией.

<!-- сверено с кодом: 2026-07-29, Дом отвечает на три вопроса -->

## Что забрали из `app.js` (П7, срез 2026-07-29)

Анализатор больше не держит у себя чистые слои: русские сущности, деньги, даты, разбор речи на
клаузы и намерения живут в `core/*.mjs` и импортируются. В `app.js` остаётся только то, что
трогает состояние, хранилище или DOM.

Практическое следствие для агента: искать разбор фразы надо в `core/`, а не грепом по `app.js`.
Правка в `app.js` там, где функция уже вынесена, — правка мёртвого места.

<!-- сверено с кодом 2026-07-30 (полный прогон, §2.6): из этого прогона подсистему касается только
     возврат фокуса без прокрутки вида (`restored.focus({ preventScroll: true })`) в render(). Ни
     контракты, ни данные, ни статусы провайдеров не менялись. -->

<!-- сверено с кодом 2026-07-30 (§2.4, OCR чеков): подсистему касается ровно одно — фото чека
     рождает ПРЕДЛОЖЕНИЕ расхода (`draftId: "receipt-ocr"`), как любой другой разбор, и до
     подтверждения владельца в деньгах не появляется ничего. Разбор самого чека — чистый слой
     `core/receipt-ocr.mjs`; кнопка «Распознать чек» живёт у прикреплённого файла. -->

<!-- сверено с кодом 2026-07-31 (Срез А): подсистему НЕ касается. Правка прогона трогает только
     вычисление доверия предложения на виде (`proposalsWithTrust`), карточку предложения в чате и
     один блок стилей. Дата поднята после сверки, а не вместо неё. -->

<!-- сверено с кодом 2026-07-31 (Срезы Б, В, Г, Д, второй проход): подсистему НЕ касается.
     Прогон трогает построчный разбор на Доме, вердикт по похожей мысли, состав композера,
     автозаморозку канарейки и панель самообзора. Дата поднята после сверки, а не вместо неё. -->

<!-- сверено с кодом 2026-07-31 (финал прогона Срезов Б–Г): добавлены ответы экрана там, где
     обработчик честно решал ничего не делать — пустое «Разобрать», переход на уже открытый
     экран, «Принять всё» без открытых предложений, стрелка у крайнего виджета. Молчащая кнопка
     неотличима от сломанной; правило и тексты — core/surface-notice.mjs. -->

<!-- сверено с кодом 2026-07-31 (сценарии О-1, 4, 7, 8, 9): пять изменений за прогон.
     О-1 — защита от двух вкладок: BroadcastChannel, право записи у СТАРШЕЙ вкладки (в ней может
     лежать несохранённый черновик), младшая уходит в чтение с несъёмной полосой. persistCurrent
     при readOnly не пишет вовсе. Нет BroadcastChannel — работает как раньше, и `supported`
     это честно показывает: врать про защиту, которой нет, нельзя.
     4 — у инсайта раскрывается «Почему сейчас»: только из того, что у него уже есть (число
     опорных записей, тип повода). Нет оснований — нет строки; молчание честнее правдоподобия.
     7 — записи с `systemType` вон с поверхностей владельца (`notes` в виде), полный список
     остался как `allNotes` для графа с Dev-фильтром, экспорта и Контроля. Это и был источник
     «ID, Type, Owner» в связях: слова жили в СОДЕРЖИМОМ наших записей о себе, не в разметке.
     8 — незакрытые петли в вечернем итоге + «Перенести всё на завтра» (одна кнопка, чек в
     Контроле). Берутся только задачи со сроком сегодня или раньше: «когда-нибудь» не петля.
     9 — `computeObjections`: возражение рождается только при цели ВЛАДЕЛЬЦА + сроке + разрыве,
     порог 45%, максимум два. Снимается кнопкой, решает он (И-1). -->

<!-- сверено с кодом 2026-07-31 (требования Т2 и Т4): два изменения.
     Т2 — появился первый в проекте индикатор работы. `markBusy`/`clearBusy`/`withBusy` в app.js:
     занятость живёт ВНЕ хранилища (это про экран, не про данные) и рисуется мимо commit().
     Наружу идёт не факт занятости, а ЧТО ИМЕННО делается — «читаю запись и ищу похожие»,
     «ищу в твоих записях», «разбираю прикреплённое». Скелетон (ui/components/ParseSkeleton.js)
     повторяет ФОРМУ будущего разбора, чтобы экран не дёргался при подстановке; снимается в
     finally, в том числе после падения. Бюджет задержек — docs/MVP_UX_MAP.md §2.
     Т4 — на экране Аудио три блока проверок STT (Whisper/Vosk/whisper.cpp, каждый со своей
     кнопкой «Подготовить» и полем адреса) свёрнуты в ОДНУ строку. Работает расшифровка — про
     неё на экране нет ничего; не работает — одна честная строка и одна кнопка в «Подключения».
     Движки не удалены, их настройка живёт там, где ей место (И-7). -->

<!-- сверено с кодом 2026-07-31 (провал на живой фразе владельца — разбор будущего времени).
     ЗАМЕР: фраза «завтра отработать 12 часов, выбрать в какой промежуток лучше всего» давала
     РОВНО НОЛЬ намерений. Две причины, обе в core/speech-intents.mjs:
     (1) SPEECH_WORK_RE знал только прошедшее время («отработал»), а владелец планирует смену
         инфинитивом («отработать»). Человек планирует чаще, чем отчитывается.
     (2) Типа `plan` не было в SPEECH_INTENT_SCHEMA — валидатор молча выбрасывал намерение.
     ИСПРАВЛЕНО: словарь работы расширен формами будущего и «за рулём/таксовать»; добавлены
     SPEECH_FUTURE_RE и SPEECH_OPEN_QUESTION_RE; будущая смена становится `plan`, а НЕ `shift`
     (у плана нет ни дохода, ни отработанных часов — записать его сменой значит выдать заработок,
     которого не было); нерешённый вопрос внутри фразы («выбрать, в какой промежуток») становится
     ДЕЛОМ НА СЕГОДНЯ, а не на день смены: решать надо накануне вечером.
     Проверено на трёх фразах: план + дело на сегодня для будущего, `shift` для прошедшего. -->

<!-- сверено с кодом 2026-08-01 (разбор с ИИ, микрофон, тип «решение»):
     РАЗБОР С ИИ — вся машинерия (`requestSpeechIntentsFromModel`, валидатор, слияние без дублей,
     построение предложений) существовала и была заперта за выключенным по умолчанию тумблером
     `ollama.speechIntents`, причём работала только для аудио. Добавлен `force`: явное нажатие
     владельца — не то же, что автозапуск на каждом захвате (модель может быть выключена, занята
     или медленная). Действие `capture-with-ai` СНАЧАЛА сохраняет запись правилами и только потом
     зовёт модель — нажать «с ИИ» никогда не хуже обычной кнопки, ничего не теряется.
     Статусы честные: `not-connected`/`off`/сбой пишутся как есть (CLAUDE.md §7).
     МИКРОФОН — работал, но нажатие не меняло на Доме НИЧЕГО: индикатор и «стоп» жили только на
     экране Аудио. Запись шла молча — неотличимо от поломки. Теперь состояние и ошибка доступа
     видны у самого поля.
     ТИП `decision` — новый в SPEECH_INTENT_SCHEMA и в промпте модели. Отличие от `task`: у дела
     есть действие, у решения открытый вопрос. Ложится задачей на день, КОГДА решать (обычно
     сегодня, срок 21:00), а не на день события; поле `question` переживает превращение в задачу,
     чтобы владелец не формулировал запрос заново. -->

<!-- сверено с кодом 2026-08-01 (самообучение продукта через голос владельца):
     Новый чистый модуль `core/product-feedback.mjs` + коллекция `state.productFeedback` + тип
     предложения `product_feedback`. Мысль ПРО САМУ СИСТЕМУ («инсайты непонятные», «микрофон не
     работает», «хочу кнопку с ИИ») раньше падала в общую кучу и умирала там — самая ценная
     обратная связь обрабатывалась хуже всего.
     Распознавание требует ДВУХ признаков разом: упоминание системы её живыми именами (экран,
     разбор, инсайт, микрофон…) И оценка/желание. Одного упоминания мало: «записал в LifeOS» —
     не обратная связь. Разбор по ПРЕДЛОЖЕНИЯМ: в одной надиктовке владелец говорит и про смену
     в такси, и про кривой экран — это две записи. Максимум три из одной надиктовки: больше —
     почти всегда признак, что сработали слова, а не смысл.
     Отдельно от `state.improvements` (то, что система заметила о себе сама): источник разный.
     Действие `export-product-feedback` выгружает Markdown со всеми замечаниями и цитатами —
     список работы для человека, который пишет код.
     ГРАНИЦА: система себя НЕ чинит и не притворяется, что может. Узнаёт, называет, копит. -->

<!-- сверено с кодом 2026-08-01 (зависание, микрофон, польза временем, обратная связь о продукте):
     ЗАВИСАНИЕ — найдена настоящая причина «весь экран виснет». Очередь записи была строгой
     цепочкой `writeQueue.then(task, task)`: одна запись в IndexedDB, не завершившаяся НИКОГДА,
     навсегда останавливала цепочку, а каждый обработчик делает `await store.commit(...)` — значит
     переставала работать ни одна кнопка. Добавлен сторож WRITE_TIMEOUT_MS=15000: цепочка
     отпускается, состояние в памяти цело, saveState="error". Саму транзакцию не отменяем —
     отменить IndexedDB нельзя, и врать, что она не пройдёт, тоже нельзя.
     МИКРОФОН — `audioRecordingStatus` (состояние ЖЕЛЕЗА) сохранялся в снимок и переживал
     перезагрузку: экран рисовал «идёт запись» без живого MediaRecorder. Сбрасывается в hydrate,
     как `streaming:false` у сообщений. Плюс MAX_RECORDING_MS=20мин — забытый микрофон
     останавливается сам И СОХРАНЯЕТ записанное (В-9 из MVP_NEGATIVE_CASES).
     ПОЛЬЗА ВРЕМЕНЕМ — перенесено из ветки агента: ставка `insight-action` на 7 дней при
     закреплении вывода и создании проекта; разрешается по РЕАЛЬНЫМ объектам (задача из
     источников вывода, движение по проекту), не опросом. Три исхода; `unverifiable` при
     отсутствии активности владельца за окно — неделя, которой у него не было, не считается
     провалом вывода. `insightBetsByType` кормит core/self-review.mjs кандидатом `never-useful`
     (порог 20 ПРОВЕРЕННЫХ ставок и ровно hit===0: «никогда» — слово, за которое надо отвечать).
     ОБРАТНАЯ СВЯЗЬ О ПРОДУКТЕ — core/product-feedback.mjs: мысль ПРО САМУ СИСТЕМУ узнаётся по
     предложениям (упоминание продукта + оценка/желание), становится типом `product_feedback` и
     коллекцией state.productFeedback. Система себя НЕ чинит и не притворяется, что может. -->

<!-- сверено с кодом 2026-08-01: `runSpeechIntentModelForSource` звал `store.getState()`, метода
     с таким именем у хранилища НЕТ (есть `getStateSnapshot`). Строка была битой ВСЕГДА, но путь
     оставался мёртвым: тумблер `ollama.speechIntents` выключен по умолчанию. Первое нажатие
     новой кнопки «с ИИ» уронило экран целиком («store.getState is not a function»). Заменено на
     `store.state`. Урок для будущих правок: включая мёртвый путь, сначала прочитай его целиком —
     он не проверялся ничем, потому что не исполнялся. -->
