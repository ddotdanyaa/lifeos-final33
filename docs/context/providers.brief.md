---
subsystem: providers
covers:
  - app.js:9500-10600
  - app.js:10600-11600
  - ui/providers.js
updated: 2026-07-30
---

# Бриф: провайдеры и локальные движки (providers)

Всё, что LifeOS вызывает снаружи себя: расшифровка речи, локальная модель, OCR, парсеры книг,
уведомления, внешний календарь. Общий закон один и он не обсуждается: **провайдер честен**.
Статус бывает `ok` / `not-connected` / `permission-required` / `provider_unavailable` — и никогда
не бывает имитации успеха.

## 1. Расшифровка речи — три пути, один результат

| путь | где живёт | когда |
|---|---|---|
| **whisper.cpp** | локальный сервер `http://127.0.0.1:8090/inference` | основной |
| **Whisper (transformers.js)** | Web Worker, веса с HuggingFace CDN | без демона |
| **Vosk** | `vosk-browser`, модель small-ru | запасной |

Все три заканчиваются одним вызовом `saveSourceTranscript(state, sourceId, text, options)` —
дальше расшифровка живёт одинаково независимо от движка. Один провенанс, один путь.

**Модель выбирается замером, а не названием.** На процессоре владельца (замер 2026-07-28):
`ggml-base` — быстро, но каша без пунктуации, разбору нечего брать; `ggml-large-v3-turbo` —
2 мин 15 с на 11 секунд звука, непригодно; `ggml-small` — **22 секунды**, текст и пунктуация как
у большой. Выбран `small`, `npm run whisper-server` указывает на него.

**`verbose_json`, а не `json`** (П5): тот же текст плюс сегменты с настоящими секундами
(`start`/`end`). Без них обратный путь «вывод → цитата → место в записи» пришлось бы угадывать.

**Мост в 16 кГц моно** (`decodeAudioTo16kMono` + `encodeWav16kMono`) стоит перед отправкой:
телефонная запись возвращалась в 5,5 раза тяжелее нужного.

**Паузы говорящего — границы смысла.** whisper отдаёт речь построчно; `cleanTranscript`
сохраняет переводы строк, `cleanLine` их схлопывает. Перепутать — значит превратить двадцать
минут речи в одну задачу с названием во весь экран (случалось).

## 2. Ollama (локальная модель)

`state.ollama`: `endpoint` (`http://127.0.0.1:11434`), `selectedModel`, `status`, `models`,
`lastError`, `lastLatencyMs`.

Подключение считается настоящим только после ДВУХ явных проверок: `/api/tags` (модели видны) и,
отдельно, тест генерации `/api/generate`. Наличие демона само по себе ничего не доказывает.

Применения: чат (`streamOllamaChatAnswer`, NDJSON-поток), разбор речи в намерения
(`requestSpeechIntentsFromModel`, за флагом `speechIntents`, по умолчанию выключен).

**Что показал замер (2026-07-28, эта машина):** `qwen2.5:3b` — 44 секунды на одну надиктовку и
частично сломанный JSON; `qwen3:4b` — 99 секунд и пустой ответ (бюджет `num_predict` съеден
рассуждениями). Вывод в архитектуру: **правила первичны, модель добавляет непонятое**, её ответ
проходит тот же валидатор с обязательной цитатой из слов владельца.

**Замер на объёме вечера (2026-07-30, 24 записи настоящей речи,
[отчёт](../MEASURE_2026-07-30_SPEECH_INTENTS.md)):**

- `think: false` теперь в запросе. Без него `qwen3:4b` — модель, которая стоит у владельца, —
  возвращала **не JSON во всех 24 случаях** и тратила 11 минут вечера. Молчащий слой неотличим от
  отсутствующего, а это уже не честный статус. Моделям без размышления поле безвредно.
- Слой **остаётся выключенным по умолчанию, и это замерено**: с `think:false` модель добавляет 20
  объектов на 24 записи, из них верных 4, а в четырёх записях **переворачивает направление денег**
  («Заплатил за квартиру 28 тысяч» → доход 28 000 ₽). Правила на том же корпусе дают 34 попадания
  из 40 против 38 у пары «правила+модель» — за 11 минут ожидания и вдвое больший разбор мусора.
- Валидатор усилен: у намерений ОТ МОДЕЛИ суммы, часы и часы времени обязаны звучать в словах
  владельца цифрами. Поймано этим: «доход 8701 ₽» на сказанное 8700, шесть смен с началом «00:00»,
  «23.99 ч» из фразы про замену масла. Цена названа в отчёте: отклоняются и верные догадки
  («тысячу двести» → 1200), потому что отличить их от выдумки нечем.

**Граница доверия (3.1d).** Текст источника — ДАННЫЕ, не команды: подаётся в промпт только
размеченным блоком, и модели прямо сказано, что инструкций внутри него нет.

**`<think>`-блоки** вырезаются (`stripModelThinkingBlocks`): владелец не должен видеть черновик
рассуждений вместо ответа.

## 3. Разрешения (capability grants)

Грант — это `resource + action + scope + locality + approval + budget`. Каждое действие
провайдера проверяет грант ПЕРЕД запуском и пишет `recordProviderRun`. Контейнер разрешений
может отсутствовать в снимке от старой версии — доступ только через `capabilityStore`
(именно необорудованный доступ к `state.control.capabilities` однажды уронил загрузку и привёл
к потере данных, см. бриф `storage-kernel` §3).

## 3.1. Что провайдер оставляет петле

Каждое предложение, рождённое намерением из речи, несёт `origin` — правило или модель. Журнал
решений (`docs/context/self-improving-loop.brief.md`) пишет его на каждое решение владельца,
поэтому точность правил и точность модели считаются ОТДЕЛЬНО. Мерить их вместе значит не узнать
про обе — а замер 2026-07-28 показал, что на этой машине правила сильнее.

## 4. Границы

- Ни одного скрытого сетевого вызова. Веса моделей качаются только после явного клика владельца.
- Ключи, учётки, CAPTCHA вводит только владелец.
- Падение провайдера не роняет платформу: разбор по правилам уже отработал.

## 4.1. Журнал прогонов ограничен

`recordProviderRun` пишет каждый вызов движка, и на тысяче расшифровок этот журнал весит больше
самих записей владельца. П22 обрезает его до 200 последних раз в сутки — остаются самые свежие,
по которым что-то понятно.

## 5. Ловушки среды (проверено болью)

- **Знак процента в пути ломает Playwright.** Проект лежит в `LIFEOS FINAL 33%`, и
  `setInputFiles(<путь>)` на таком пути МОЛЧА не прикрепляет файл. Выглядит как «плавающий»
  дефект приложения. Файлы в спеках передавать **только буфером**.
- **Первый тест в файле падает чаще прочих** — холодный старт браузера и установка service
  worker. Это не дефект продукта.
- Скачивание модели Whisper с HuggingFace в headless упирается в сеть и таймауты: спека
  `whisper-transcribe` красная по среде, а не по коду.

## 6. Как проверять изменения

`whisper-transcribe` · `voice-loop-whispercpp` · `voice-loop-vosk` · `m4a-voice-end-to-end` ·
`ollama-real-daemon` · `ollama-live-chat` · `byok-vault-routing` · `failure-injection` ·
`audio-any-format` · `audio-alac-decode`.

## 6.1. Мост Telegram (П35)

Живёт не здесь, а в `server.mjs`: он транспорт, а не провайдер модели. Приносит байты и отдаёт
их приложению, которое пропускает их через СВОЙ `importFilesFromInput`. Whitelist по user_id
проверяется до любой обработки; без токена мост честно не поднимается. Разбор донора —
`docs/context/donors/voice-from-phone.donor.md`.

## 6.2. Что показал полный набор

Спеки провайдеров — самая красная часть набора, и это честно: `whisper-transcribe` и
`voice-loop-vosk` качают модели с HuggingFace, `voice-loop-whispercpp` требует записи с
микрофона, `waveform-record` — `getUserMedia`, `ollama-real-daemon` — живой демон под нагрузкой.
Они падают по среде, а не по коду, и заменять их на моки нельзя: смысл именно в том, что они
проверяют НАСТОЯЩИЙ движок.

## 7. Открытые долги

- Слой намерений на модели проверяется только на машине владельца — метка
  **[ЛОКАЛЬНАЯ ПРИЁМКА]**.
- Мост Telegram (волна 5, П35) не начат; когда начнётся — второй путь импорта не строить,
  входить в существующий `importFilesFromInput`, транскрипцию оставить свою (провенанс один).

<!-- сверено с кодом: 2026-07-29, Дом отвечает на три вопроса -->

## 8. Где теперь живёт чистый слой

`app.js` больше не единственный файл: разбор речи (`core/speech-intents.mjs`), русские сущности
(`core/ru-entities.mjs`), даты и время (`core/ru-parse.mjs`), деньги (`core/ru-money.mjs`),
выводы (`core/owner-themes.mjs`), подписи (`core/format.mjs`). Провайдеры остаются в `app.js` —
они трогают сеть, разрешения и состояние, то есть по определению не чистые.

Правило при добавлении модуля: дописать его в `service-worker.js` (прекэш) и прогнать
`node tools/build-public.mjs`, иначе офлайн-сборка соберётся без половины кода.

## Чат с живым `qwen3:4b` отдаёт рассуждение вслух (полный прогон 2026-07-30)

Воспроизведено вне спеки: probe → тест генерации → вопрос — и владелец получает «Хорошо, мне нужно
ответить на вопрос пользователя кратко и по делу…». Это не ответ, это мысли модели.

**`think: false` здесь НЕ ЛЕЧИТ, проверено прямым запросом:** на `/api/generate` флаг рассуждение
не выключает (`qwen3:4b` всё равно начинает с «Okay, let's…»). Он работает там, где ответ ограничен
схемой — `format: "json"` в разборе речи, где и замерен. Честный путь для чата — `/api/chat`, где у
размышления свой канал; это отдельный пакет. Попытка поставить флаг в чат была откачена в том же
прогоне: пустышка с комментарием «работает» хуже её отсутствия.

Спека `ollama-real-daemon` падает РАНЬШЕ этого места: она ждёт роста числа сообщений ассистента, а
пустой плейсхолдер стрима — уже сообщение ассистента, поэтому проверка чека провайдера случается до
конца генерации. Чинить обе вещи придётся вместе.

## OCR чеков — локальный движок с локальными данными (§2.4, 2026-07-30)

`tesseract.js` 7 (Apache-2.0) грузится лениво (`loadTesseract`), языковые данные лежат в
`vendor/tesseract-lang/fast` и скачиваются один раз `node tools/setup-tesseract-lang.mjs`. Разбор
самого чека — чистый слой `core/receipt-ocr.mjs`: сумма берётся у слова итога, а не максимум; нет
слова итога — чек не понят, и сумма не выдумывается.

**Три вещи, без которых движок молча не работает** (каждая проверена запуском):
- ESM-сборка отдаёт API через `default`, именованного `createWorker` нет;
- пути `langPath`/`workerPath`/`corePath` обязаны быть АБСОЛЮТНЫМИ: внутри Web Worker
  относительный путь считается от адреса воркера, а не страницы;
- `gzip: false` — данные распакованы; иначе движок ищет `.gz` и уходит в СЕТЬ.

**Граница §7 проверена опытом, а не обещанием:** при спрятанном файле данных статус
`not-connected`, предложения нет, внешних запросов за прогон — ни одного. В precache service worker
движок НЕ положен намеренно (wasm + данные — десятки мегабайт), поэтому офлайн OCR не работает, и это
названный долг, а не сюрприз.

<!-- сверено с кодом 2026-07-31 (Срез А): подсистему НЕ касается. Правка прогона трогает только
     вычисление доверия предложения на виде (`proposalsWithTrust`), карточку предложения в чате и
     один блок стилей. Дата поднята после сверки, а не вместо неё. -->

<!-- сверено с кодом 2026-07-31 (Срезы Б, В, Г, Д, второй проход): подсистему НЕ касается.
     Прогон трогает построчный разбор на Доме, вердикт по похожей мысли, состав композера,
     автозаморозку канарейки и панель самообзора. Дата поднята после сверки, а не вместо неё. -->

<!-- сверено с кодом 2026-07-31 (финал прогона Срезов Б–Г): добавлены ответы экрана там, где
     обработчик честно решал ничего не делать — пустое «Разобрать», переход на уже открытый
     экран, «Принять всё» без открытых предложений, стрелка у крайнего виджета. Молчащая кнопка
     неотличима от сломанной; правило и тексты — core/surface-notice.mjs. -->

<!-- сверено с кодом 2026-07-31 (сценарии О-1, 4, 7, 8, 9): пять изменений за прогон.
     О-1 — защита от двух вкладок: BroadcastChannel, право записи у СТАРШЕЙ вкладки (в ней может
     лежать несохранённый черновик), младшая уходит в чтение с несъёмной полосой. persistCurrent
     при readOnly не пишет вовсе. Нет BroadcastChannel — работает как раньше, и `supported`
     это честно показывает: врать про защиту, которой нет, нельзя.
     4 — у инсайта раскрывается «Почему сейчас»: только из того, что у него уже есть (число
     опорных записей, тип повода). Нет оснований — нет строки; молчание честнее правдоподобия.
     7 — записи с `systemType` вон с поверхностей владельца (`notes` в виде), полный список
     остался как `allNotes` для графа с Dev-фильтром, экспорта и Контроля. Это и был источник
     «ID, Type, Owner» в связях: слова жили в СОДЕРЖИМОМ наших записей о себе, не в разметке.
     8 — незакрытые петли в вечернем итоге + «Перенести всё на завтра» (одна кнопка, чек в
     Контроле). Берутся только задачи со сроком сегодня или раньше: «когда-нибудь» не петля.
     9 — `computeObjections`: возражение рождается только при цели ВЛАДЕЛЬЦА + сроке + разрыве,
     порог 45%, максимум два. Снимается кнопкой, решает он (И-1). -->

<!-- сверено с кодом 2026-07-31 (требования Т2 и Т4): два изменения.
     Т2 — появился первый в проекте индикатор работы. `markBusy`/`clearBusy`/`withBusy` в app.js:
     занятость живёт ВНЕ хранилища (это про экран, не про данные) и рисуется мимо commit().
     Наружу идёт не факт занятости, а ЧТО ИМЕННО делается — «читаю запись и ищу похожие»,
     «ищу в твоих записях», «разбираю прикреплённое». Скелетон (ui/components/ParseSkeleton.js)
     повторяет ФОРМУ будущего разбора, чтобы экран не дёргался при подстановке; снимается в
     finally, в том числе после падения. Бюджет задержек — docs/MVP_UX_MAP.md §2.
     Т4 — на экране Аудио три блока проверок STT (Whisper/Vosk/whisper.cpp, каждый со своей
     кнопкой «Подготовить» и полем адреса) свёрнуты в ОДНУ строку. Работает расшифровка — про
     неё на экране нет ничего; не работает — одна честная строка и одна кнопка в «Подключения».
     Движки не удалены, их настройка живёт там, где ей место (И-7). -->

<!-- сверено с кодом 2026-07-31 (провал на живой фразе владельца — разбор будущего времени).
     ЗАМЕР: фраза «завтра отработать 12 часов, выбрать в какой промежуток лучше всего» давала
     РОВНО НОЛЬ намерений. Две причины, обе в core/speech-intents.mjs:
     (1) SPEECH_WORK_RE знал только прошедшее время («отработал»), а владелец планирует смену
         инфинитивом («отработать»). Человек планирует чаще, чем отчитывается.
     (2) Типа `plan` не было в SPEECH_INTENT_SCHEMA — валидатор молча выбрасывал намерение.
     ИСПРАВЛЕНО: словарь работы расширен формами будущего и «за рулём/таксовать»; добавлены
     SPEECH_FUTURE_RE и SPEECH_OPEN_QUESTION_RE; будущая смена становится `plan`, а НЕ `shift`
     (у плана нет ни дохода, ни отработанных часов — записать его сменой значит выдать заработок,
     которого не было); нерешённый вопрос внутри фразы («выбрать, в какой промежуток») становится
     ДЕЛОМ НА СЕГОДНЯ, а не на день смены: решать надо накануне вечером.
     Проверено на трёх фразах: план + дело на сегодня для будущего, `shift` для прошедшего. -->

<!-- сверено с кодом 2026-08-01 (разбор с ИИ, микрофон, тип «решение»):
     РАЗБОР С ИИ — вся машинерия (`requestSpeechIntentsFromModel`, валидатор, слияние без дублей,
     построение предложений) существовала и была заперта за выключенным по умолчанию тумблером
     `ollama.speechIntents`, причём работала только для аудио. Добавлен `force`: явное нажатие
     владельца — не то же, что автозапуск на каждом захвате (модель может быть выключена, занята
     или медленная). Действие `capture-with-ai` СНАЧАЛА сохраняет запись правилами и только потом
     зовёт модель — нажать «с ИИ» никогда не хуже обычной кнопки, ничего не теряется.
     Статусы честные: `not-connected`/`off`/сбой пишутся как есть (CLAUDE.md §7).
     МИКРОФОН — работал, но нажатие не меняло на Доме НИЧЕГО: индикатор и «стоп» жили только на
     экране Аудио. Запись шла молча — неотличимо от поломки. Теперь состояние и ошибка доступа
     видны у самого поля.
     ТИП `decision` — новый в SPEECH_INTENT_SCHEMA и в промпте модели. Отличие от `task`: у дела
     есть действие, у решения открытый вопрос. Ложится задачей на день, КОГДА решать (обычно
     сегодня, срок 21:00), а не на день события; поле `question` переживает превращение в задачу,
     чтобы владелец не формулировал запрос заново. -->

<!-- сверено с кодом 2026-08-01 (самообучение продукта через голос владельца):
     Новый чистый модуль `core/product-feedback.mjs` + коллекция `state.productFeedback` + тип
     предложения `product_feedback`. Мысль ПРО САМУ СИСТЕМУ («инсайты непонятные», «микрофон не
     работает», «хочу кнопку с ИИ») раньше падала в общую кучу и умирала там — самая ценная
     обратная связь обрабатывалась хуже всего.
     Распознавание требует ДВУХ признаков разом: упоминание системы её живыми именами (экран,
     разбор, инсайт, микрофон…) И оценка/желание. Одного упоминания мало: «записал в LifeOS» —
     не обратная связь. Разбор по ПРЕДЛОЖЕНИЯМ: в одной надиктовке владелец говорит и про смену
     в такси, и про кривой экран — это две записи. Максимум три из одной надиктовки: больше —
     почти всегда признак, что сработали слова, а не смысл.
     Отдельно от `state.improvements` (то, что система заметила о себе сама): источник разный.
     Действие `export-product-feedback` выгружает Markdown со всеми замечаниями и цитатами —
     список работы для человека, который пишет код.
     ГРАНИЦА: система себя НЕ чинит и не притворяется, что может. Узнаёт, называет, копит. -->

<!-- сверено с кодом 2026-08-01 (зависание, микрофон, польза временем, обратная связь о продукте):
     ЗАВИСАНИЕ — найдена настоящая причина «весь экран виснет». Очередь записи была строгой
     цепочкой `writeQueue.then(task, task)`: одна запись в IndexedDB, не завершившаяся НИКОГДА,
     навсегда останавливала цепочку, а каждый обработчик делает `await store.commit(...)` — значит
     переставала работать ни одна кнопка. Добавлен сторож WRITE_TIMEOUT_MS=15000: цепочка
     отпускается, состояние в памяти цело, saveState="error". Саму транзакцию не отменяем —
     отменить IndexedDB нельзя, и врать, что она не пройдёт, тоже нельзя.
     МИКРОФОН — `audioRecordingStatus` (состояние ЖЕЛЕЗА) сохранялся в снимок и переживал
     перезагрузку: экран рисовал «идёт запись» без живого MediaRecorder. Сбрасывается в hydrate,
     как `streaming:false` у сообщений. Плюс MAX_RECORDING_MS=20мин — забытый микрофон
     останавливается сам И СОХРАНЯЕТ записанное (В-9 из MVP_NEGATIVE_CASES).
     ПОЛЬЗА ВРЕМЕНЕМ — перенесено из ветки агента: ставка `insight-action` на 7 дней при
     закреплении вывода и создании проекта; разрешается по РЕАЛЬНЫМ объектам (задача из
     источников вывода, движение по проекту), не опросом. Три исхода; `unverifiable` при
     отсутствии активности владельца за окно — неделя, которой у него не было, не считается
     провалом вывода. `insightBetsByType` кормит core/self-review.mjs кандидатом `never-useful`
     (порог 20 ПРОВЕРЕННЫХ ставок и ровно hit===0: «никогда» — слово, за которое надо отвечать).
     ОБРАТНАЯ СВЯЗЬ О ПРОДУКТЕ — core/product-feedback.mjs: мысль ПРО САМУ СИСТЕМУ узнаётся по
     предложениям (упоминание продукта + оценка/желание), становится типом `product_feedback` и
     коллекцией state.productFeedback. Система себя НЕ чинит и не притворяется, что может. -->

<!-- сверено с кодом 2026-08-01: `runSpeechIntentModelForSource` звал `store.getState()`, метода
     с таким именем у хранилища НЕТ (есть `getStateSnapshot`). Строка была битой ВСЕГДА, но путь
     оставался мёртвым: тумблер `ollama.speechIntents` выключен по умолчанию. Первое нажатие
     новой кнопки «с ИИ» уронило экран целиком («store.getState is not a function»). Заменено на
     `store.state`. Урок для будущих правок: включая мёртвый путь, сначала прочитай его целиком —
     он не проверялся ничем, потому что не исполнялся. -->
