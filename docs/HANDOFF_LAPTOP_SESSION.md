# Передача: с облака на ноутбук (обновлено 2026-07-29)

**Владельцу: с ноутбука достаточно отправить одну строку.**

```
Прочитай docs/HANDOFF_LAPTOP_SESSION.md целиком и работай по нему сверху вниз. Не спрашивай, что дальше — бери первый пункт §2 и доводи до конца, потом следующий. Коммить и пушь после каждого пункта. Вопросы владельцу — одним списком в самом конце.
```

Всё остальное в этом файле — для той сессии, а не для владельца. Проект описан в `CLAUDE.md`,
ловушки — в `MEMORY.md`, сделанное — в `PROGRESS.md`, очередь облака — в `docs/HANDOFF_PROMPT.md`.

---

## 1. Точка, в которой всё оставлено (2026-07-29)

- **Ветка:** `claude/lifeos-artifact-inspector-5lg95m`. Дерево ЧИСТОЕ, HEAD = `origin`, всё
  запушено. Последний коммит — `d2094a8`.
- **Размер:** `app.js` 1,4 МБ, **141 e2e-спека**, **36 аудитов**.
- **Гейты:** `npm run verify` зелёный, `audit-app-boots` зелёный, **29 аудитов из 36**.

**Семь красных аудитов и их НАСТОЯЩАЯ причина** (проверено запуском, а не предположено):

| Аудит | Почему красный | Чинится тут? |
|---|---|---|
| `audit-final-journeys` | нет скриншотов-доказательств | **да** — их производит полный прогон |
| `audit-nonstop-until-done` | нет `output/playwright/journeys/j01-after.png` и др. | **да** — тем же прогоном |
| `audit-performance-final` | `performance screenshot missing` | **да** |
| `audit-recovery-final` | `recovery screenshot missing` | **да** |
| `audit-release-evidence` | `Missing or empty required screenshots` | **да** |
| `audit-owner-rescue-final` | нужны имя ветки владельца, чистое дерево и `gh` CLI | **да — `gh` есть только тут** |
| `audit-context-freshness` | `docs/context/*.brief.md` старше своего кода | да, это правка доков |

**Шесть из семи — один корень: скриншотов нет в git, а производит их полный e2e.** В облаке он
стоит часы и упирается в среду. Тут он реален — это главная причина садиться за ноутбук.

## 2. Ради чего вообще садиться за ноутбук (делать в этом порядке)

Всё ниже **невозможно проверить в облаке**. Если новая сессия начнёт с обычных пакетов, ноутбук
потрачен зря — облако их и так делает.

> **Статус на 2026-07-29.** Закрыто прошлой ноутбучной сессией: **2.1 голос** (круг на настоящей
> речи и whisper.cpp) и **2.6 полный прогон**. С тех пор облако добавило 141-ю спеку и 36 аудитов,
> поэтому **полный прогон нужен снова — он же добивает шесть аудитов из §1.**
> Открыто отсюда: новые **2.7–2.9** ниже (весь список 2.1–2.6 закрыт).
> Инструмент для замера на речи, а не на печатном тексте: `node tools/make-voice-corpus.mjs`.
>
> **Статус после сессии 2026-07-30 (ноутбук).** Закрыто: **2.4 OCR чеков** (фото → «Распознать
> чек» → локальный движок → предложение расхода; сумма берётся у слова «Итого», а не самая
> большая; без языковых данных честный `not-connected` и НИ ОДНОГО внешнего запроса — проверено
> опытом), **2.6 полный прогон** (332/24/7 за 2,2 ч,
> каждое падение названо в `PROGRESS.md`; пять «скриншотных» аудитов стали зелёными по-настоящему),
> **2.5 живое взаимодействие** (проба `tools/probe-live-interaction.mjs` нашла, что четыре раздела
> из девяти открывались серединой — Контроль на 400 px; вылечено в `render()`, попутно закрылись
> H05/H06/H09), **2.3 эмбеддинги** (замер отменил включение: `nomic-embed-text` на русском не отделяет смысл
> от шума вовсе, `bge-m3` отделяет с запасом 0.027 на двух парах — порог по такому разрыву не
> фиксируется; отчёт [MEASURE_2026-07-30_SEMANTIC_NEIGHBORS.md](MEASURE_2026-07-30_SEMANTIC_NEIGHBORS.md)),
> **2.2 Ollama Этап B** — замер проведён на
> 24 записях настоящей речи, отчёт [docs/MEASURE_2026-07-30_SPEECH_INTENTS.md](MEASURE_2026-07-30_SPEECH_INTENTS.md).
> Главное: слой модели остаётся выключенным по умолчанию **по замеру** (`qwen3:4b` переворачивает
> направление денег в 4 записях из 24 и выдумывает смены), а сам замер нашёл более дорогой дефект —
> **деньги владельца терялись на числе** («28 тысяч» → ноль объектов, «31 тысячу» → доход 31 ₽).
> Правила после правки сильнее модели на том же корпусе. Гейт: `spoken-money-is-not-lost.spec.mjs`.
> Инструмент замера: `node tools/measure-speech-intents.mjs [--no-model|--model=…]`.

### 2.1 Голос — вход №1 в сценарии владельца
Сценарий начинается с 20–30 голосовых за день. Расшифровки нет: whisper.cpp / vosk в облаке не
запускаются, микрофона нет. На ноутбуке — есть.
- Прогнать спеки, которые в облаке падали ПО СРЕДЕ: голос, микрофон, ALAC через ffmpeg.
- Довести цепочку: голосовое → расшифровка → разбор на артефакты → предложения → чеки.
- Провайдер обязан быть честным: `not-connected` / `permission-required`, никогда не имитировать.

### 2.2 Локальная модель (Ollama) — Этап B разбора захвата
`RR-001 Этап A` (лексический разбор) сделан; Этап B — LLM-проход по неразобранному — в облаке
не проверить. На ноутбуке: поднять Ollama, прогнать разбор на настоящем вечернем дампе из
20–30 записей, сравнить с лексическим по числу артефактов и по числу ошибок.
**Граница:** LLM предлагает, но ничего не пишет сам — только proposal → подтверждение → чек (§7).

### 2.3 Настоящие эмбеддинги (I5)
`@huggingface/transformers` — модель надо скачать, в облаке не проверяется. Сейчас работает
лексическая половина («Похожее по тексту») и честный статус «поиск по смыслу недоступен».
На ноутбуке — включить и **замерить**, а не поверить: сравнить пары, которые находит лексика,
с теми, что находит смысл, и выбрать порог ПО ЗАМЕРУ (так уже делалось для `SIMILAR_MIN_SCORE`).

### 2.4 Распознавание чеков с фото
`tesseract.js` есть в списке движков, OCR не сделан. Поэтому «Хранителя чеков» из канона в
списке агентов НЕТ — и это правильно: заглушек не держим. На ноутбуке можно сделать по-честному.

### 2.5 Живое взаимодействие
Скриншоты облако снимать умеет (и это уже ловило дефекты, которых не видели зелёные тесты).
Чего облако не умеет: настоящие клики, ввод, скролл, тач, мобильный экран, drag-n-drop.
Прогнать руками сценарий вечернего дампа целиком и записать найденное спеками.

### 2.6 Полный прогон набора ⟵ НАЧИНАТЬ С ЭТОГО

```
npm start                                            # порт 4173, ровно один сервер
npx playwright test --workers=1 --reporter=line      # 141 спека
for f in tools/audit-*.mjs; do node "$f"; done
```

**DoD:** шесть «скриншотных» аудитов из §1 зелёные ПО-НАСТОЯЩЕМУ, а не пропущены. Каждое
оставшееся падение разобрано поимённо и записано в `PROGRESS.md` одной из двух формулировок:
«средовое, причина такая-то» или «настоящий дефект, чиню».

**База для сравнения — прогон базового коммита, а не память.** Заведомо средовые: vosk,
загрузка моделей с HuggingFace, офлайн-PWA, mobile-pwa. **Но микрофон, ru-RU голос, whisper.cpp,
ffmpeg и Ollama `qwen3:4b` тут ЕСТЬ** — списывать на среду то, что на этой машине работает,
нельзя. Не чинить моками: смысл этих спек в том, что они проверяют настоящий движок.

### 2.7 Az.js — русская лемматизация (владелец сказал «всегда да»)

Обоснование посчитано облаком, лежит в `docs/HANDOFF_PROMPT.md` §2C. Коротко: алгоритмический
стеммер написан (`core/ru-stem.mjs`), но замер показал, что он **чинит ложные склейки и ломает
верные** («память»→«памя» против «памяти»→«памят»), потому что не знает частей речи. Словарь —
единственный способ закрыть обе колонки сразу.

Условия жёсткие: лицензию проверить ПО ФАЙЛУ; грузить **лениво** (`import()`); **в precache
service worker НЕ класть** — 9,7 МБ в оболочке убьют первую загрузку; `stemRu` оставить запасным
путём, когда словарь не загрузился (офлайн честнее пустоты). Гейт — те же пары из §2C: обе
колонки обязаны стать зелёными одновременно.

### 2.8 Артефакт по умолчанию на «Связях»

Инспектор открывает заметку разработки, и в её теле видны `Graph`/`Control`. Слова в тексте
законны — неправильно, что этот артефакт выбран по умолчанию на экране владельца. Менять на
последний живой артефакт; если живых нет — честное пустое состояние, а не служебная заметка.
Спека: на чистом хранилище инспектор не показывает записей с `systemType`.

### 2.9 Пересборка «Что изменилось» и «Связей»

6486 px и 3630 px — высота законная, панелей реально много. Но экран обязан отвечать на три
вопроса (где я · что происходит · что дальше), а не перечислять подсистемы. Это переделка, а не
уборка: делать последней и показать владельцу ДО коммита.

---

## 3. Что облако уже закрыло — НЕ переделывать

Полные записи в `PROGRESS.md`, тут список, чтобы не начать заново:

- **П40 «Донор приходит сам»**: ссылка на репозиторий в захвате → решение с причиной → очередь на
  внедрение (`donorQueue`, переживает перезагрузку). Правило владельца зашито: **изучать можно
  любой проект, включая коммерческий**; лицензия решает только «код дословно или своя реализация».
- **П41 «Выводы о времени»**: ранговая корреляция по дневным рядам + поправка Бенджамини-Хохберга.
  Спека проверяет главное: **15 пар чистого шума → ноль выводов**.
- **П42 «В этот день»**: вехи год/полгода/3 месяца/месяц, каждая подписана своим словом.
- **Шапка** больше не дублирует навигацию; в карточке объекта есть выход «Вся картина связей».
- **«Что изменилось»**: 10 пустых блоков → 6, и все шесть несут действие.
- **Три визуальных дефекта**: перелив «Сегодня» (жёсткая сетка 1142 px при колонке 1000),
  «Лента» **9518 → 3442 px** (колонка кнопок душила текст до 5 px), внутренние термины в
  подписях. Гейты `visual-integrity` V6 и V7 проверены обратным ходом.
- **graphology отклонён по СТЕКУ, а не по лицензии**: MIT у всех трёх пакетов, но `esm`-сборка
  тянет Node-модуль `events`, а louvain и metrics — CJS с `require()`. Бандлера у проекта нет.
  **Louvain, PageRank и betweenness уже написаны нативно** — не искать их заново.

## 4. Ловушки, купленные болью (полный список — `MEMORY.md` §3–§4)

Самое дорогое из последнего прогона:

- **Подстрочное сравнение по-русски ВРЁТ** — уже пять случаев. Сопоставление темы идёт по
  основам (`lifeTermStem`), одинаковым на всех экранах. Минисёрч морфологии не знает: prefix +
  fuzzy 0.2 не покрывают падеж, поэтому «машиной» не находило «машину».
- **Узел графа жизни несёт id самой сильной проекции**, а не того объекта, от которого ищешь.
  Дважды это делало функцию мёртвой без единой ошибки. Искать через `lifeGroupKeyFor`.
- **`node --check` не ловит ReferenceError.** Удалённая переменная с оставшейся ссылкой проходит
  и синтаксис, и smoke — и валит экран в рантайме. Ловит только живой прогон.
- **`normalizeState` — белый список.** Новое поле состояния добавлять И в дефолты, И в
  `normalizeState`. Поле НА АРТЕФАКТЕ переживает нормализацию, поле состояния — нет.
- **Читать лог Playwright только со снятыми ANSI** (`sed 's/\x1b\[[0-9;]*[A-Za-z]//g'`), иначе
  падения невидимы для grep — так однажды 19 падений были приняты за ноль.
- **Правило сворачивания панели:** сворачивается только то, у чего есть СОБСТВЕННОЕ рабочее
  место; в заголовке всегда счётчик и честный статус. Первая попытка свернула связи и статус
  провайдера — две спеки упали по делу, и правильно: это усугубляло ту самую жалобу владельца.

---

- **`audit-no-label-theater` ищет служебные термины в ИСХОДНИКАХ интерфейса — включая
  комментарии.** Объясняя правку, не писать эти строки в пояснениях: аудит покраснеет по делу.
- **Гейт обязан уметь падать.** Каждую новую спеку проверять обратным ходом: откатить фикс →
  красная → вернуть → зелёная. Иначе это украшение, а не проверка.
- **Замер вместо мнения.** Все три визуальных дефекта нашлись скриптом, который прошёл девять
  экранов и померил высоту, перелив, число пустых блоков и латиницу. Такой скрипт окупается
  за один прогон — писать его заново не жалко.
- **`auto` в сетке не умеет сжиматься.** Колонка `auto` не станет уже своей min-content ширины,
  и `minmax(0, auto)` не помогает: `auto` как МАКСИМУМ всё равно означает max-content. В тесноте
  спасает только перенос (`flex-wrap`) или явный предел.
- **Медиазапрос меряет ОКНО, а зажата бывает колонка.** Поэтому `@media (max-width: 920px)` не
  ловил перелив при окне 1440: панель жила в колонке ~1000 px. Сетки внутри рамы делать
  container-driven (`repeat(auto-fit, minmax(min(N, 100%), 1fr))`).

## 5. Гейты перед «готово»

```
npm start                                        # один сервер, порт 4173, лишние гасить
npm run verify                                   # node --check + smoke (86 маркеров)
for f in tools/audit-*.mjs; do node "$f"; done   # 6 падений выше — ожидаемы
node tools/build-public.mjs                      # после любых правок ui/**, app.js, styles.css
npx playwright test output/playwright/<spec>.mjs --workers=1 --reporter=line
```

Плюс правило владельца: **каждый пакет — живой прогон в браузере (probe), а не «на глаз»**.
Пробы этого прогона нашли три дефекта, которых не увидела ни одна зелёная спека, и два из них
были старше самих фич.

---

## 6. Открытые вопросы, решение за владельцем

1. **База PR #1 — `owner-usable-nonstop-rescue`, а не главная ветка.** Поэтому в PR попала ещё и
   вся линия `claude/session-qi3y78` (109 коммитов, 281 файл). Менять базу или нет — решение
   владельца.
2. **Задача без распознанной даты всё равно получает сегодняшнюю.** Осознанно не решено:
   вариантов два (оставить сегодня / оставить без даты), и это продуктовый выбор.
3. **Prompt caching:** замерить `cache write` vs `cache read` в Console → Usage.
4. **Hermes Agent** для телеграм-сценария в репозиторий не скачан — в `research/README.md`
   помечен как pending.
