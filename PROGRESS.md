# LifeOS — PROGRESS (леджер срезов плана v1.4)

План и скоуп: [docs/LIFEOS_V1_4_FULL_BUILD_PLAN.md](docs/LIFEOS_V1_4_FULL_BUILD_PLAN.md).
История до v1.4 (v1.0 → v1.1 → v1.2 → v1.3, ~65 коммитов, все пакеты и решения) — в
леджерах соответствующих планов в `docs/` и в DECISIONS.md; этот файл начат заново
2026-07-20 как единственный живой леджер по правилу владельца: «после каждого среза —
commit + push + PROGRESS.md + инструкция как проверить за 60 секунд».

## 🔴 HANDOFF 2026-07-22 (смена модели Opus 4.8 → Sonnet, нить не терять)

Владелец переходит на Sonnet и хочет «поскорее начать ПОЛЬЗОВАТЬСЯ». Континуитет — здесь и в
доках, читать в этом порядке:
1. `docs/LIFEOS_V1_4_FULL_BUILD_PLAN.md` §4 — новая директива владельца 2026-07-22.
2. Приоритет №1: **финансы-автосистема** (срезы F1–F7 в §4A) — банк-коннект честным гейтом +
   импорт выписки CSV/OFX → автосбор транзакций → доход/час → финцели → визуализация → инсайты.
3. Массовая интеграция донор-функций пачками (§4B) из `docs/DONOR_FUNCTION_CATALOG.md` (160+).
4. Продуктовая линза «боль→вау→желание» (§4C) — гейт каждого среза.

Сделано в сессии 2026-07-22 (закоммичено+запушено, ветка claude/session-qi3y78):
- I3 авто-извлечение сущностей (люди/проекты/места/даты) → типизированный узел (proposal+receipt).
- Полный каталог донор-функций (160+) + перестроенная очередь (13→~35 пакетов).
- Плеер: удаление файла из списка (archive-source) + ограниченный скролл + компактные карточки;
  честные ошибки расшифровки (decode-сообщение + статус «файл >8МБ не сохранён»).
- Дизайн-пасс: реальная типографика заголовков (была голым браузерным дефолтом), усиленные
  тени/глубина карточек, единая система кнопок, починены хардкод-цвета ломавшие тёмную тему.
  Аудит `audit-visual-hierarchy.mjs` поймал негативный letter-spacing (портит кириллицу) —
  исправлено, не ослаблено.
- **Срез F1+F2 закрыт**: `providers.bank` честный gate (not-connected) + импорт CSV/OFX выписки
  в Финансах (`ui/finance.js`) → каждая строка = proposal → accept → реальная транзакция + чек.
  Дедуп при повторном импорте. **Важный урок**: `app.js` содержит МЁРТВЫЕ render-функции
  (renderFinancePanel/V2 и, вероятно, другие похожие) — реальный UI строится в `ui/*.js`
  (shell.js роутит на ui/finance.js, ui/home.js и т.д.). Перед добавлением UI-разметки в
  app.js — сначала проверить playwright-скриншотом/diagnostic, что она вообще рендерится,
  не полагаться на чтение кода.
Следующий конкретный шаг: **срез F5** (визуализация chart.js — cash-flow, доход/час, тепловая
карта; порядок из плана §4A: F1→F2→F5→F3→F4→F6→F7) либо продолжение донор-пачек (§4B).

**Донайдено и закрыто в той же сессии**: владелец продолжал видеть старую версию после деплоя
(старая типографика, отсутствующая кнопка удаления в плеере) даже после жёсткого refresh —
причина НЕ в кэше браузера, а в самом service-worker: он уже вызывал `skipWaiting()`/
`clients.claim()`, но ничто не говорило УЖЕ ОТКРЫТОЙ вкладке перезагрузиться, когда новый SW
берёт управление — вкладка держала в памяти старый JS бесконечно. Исправлено: (1) слушатель
`controllerchange` в `app.js` (`armServiceWorkerAutoReload`) перезагружает страницу РОВНО один
раз, когда активируется новый SW; (2) `registration.update()` на каждом boot — проверка апдейта
сразу, не по ленивому расписанию браузера; (3) обнаружен и запущен уже существующий (но не
выполнявшийся последние несколько коммитов) `npm run stamp` — привязывает бейдж версии в шапке
и имя SW-кэша к git-sha коммита, что и является штатным механизмом инвалидации кэша по плану
среза 1. **Урок**: `npm run stamp` — обязательный шаг перед каждым коммитом (см. `## Как
запустить` выше), пропуск ломает единственный способ владельца понять, на какой версии он
находится. Verified: playwright-скрипт — первая загрузка триггерит ровно один reload (новый SW
берёт control), повторный reload не зацикливается; `finance-bank-import.spec.mjs` (4/4) и
`player-dump-usability.spec.mjs` (2/2) зелёные после фикса; все 24 аудита + `npm run verify`
проходят (кроме заведомо недоступного в этой песочнице `audit-owner-rescue-final.mjs` — падает
из-за отсутствия `gh` CLI одинаково и до, и после фикса, не связано с изменением).

**КРИТИЧНЫЙ баг найден владельцем на реальном ноутбуке (не в песочнице!) при обновлении до
этой ветки**: приложение вообще не рендерилось — пустая страница, ни шапки, ни меню. Причина:
`.gitignore` содержал строку `vendor/` (без ведущего слэша) — этот паттерн матчит ЛЮБУЮ папку
`vendor` на любой глубине дерева, а не только корневую `vendor/whisper-cpp/` (для которой она
задумывалась). Из-за этого `ui/vendor/affine-fuzzy-match.js` и `ui/vendor/xyflow-edge-paths.js`
(два маленьких MIT-vendored файла, статически импортируемых по цепочке `shell.js → graph.js →
GraphCanvas.js`) никогда не попадали в git — существовали только на диске у автора исходной
сессии. Любой свежий `git clone`/`git pull` этой ветки получал 404 на этот импорт → вся
цепочка ES-модулей ломалась → `shell.js` не инициализировался → пустая страница. Баг
существовал с момента создания этих файлов (см. DECISIONS.md, xyflow-донор и affine-fuzzy
донор), не был замечен раньше, потому что тестировался только в той же песочнице, где файлы
уже лежали на диске незакоммиченными.
Исправлено: `.gitignore` `vendor/` → `/vendor/` (только корень, `vendor/whisper-cpp/`
по-прежнему игнорируется), оба файла добавлены в git (`ui/vendor/` и `public-demo/ui/vendor/`).
**Verified по-настоящему** (не только в этой песочнице): `git clone --single-branch` в чистую
папку → `node server.mjs` на отдельном порту → playwright-проверка с нуля (пустой IndexedDB) —
приложение рендерится (после ~3-5с первого посева фикстур на пустой БД, это ожидаемо, не баг),
Дом/Деньги/Аудио/Граф — все без единой page-error, bank-import-форма и nav видны. Это первый
раз в сессии, когда фикс проверялся через настоящий fresh-clone, а не через уже тёплую
песочницу — стоит взять за привычку для доверия к "работает у меня" на будущее.

**Найден и закрыт настоящий кодек-баг плеера (эта же сессия, теперь с реальным доступом к
машине владельца)**: голосовая запись `Новая запись 29.m4a` кидала честную ошибку декодирования
в браузере. `ffmpeg -i` на реальном файле показал точную причину: `Stream #0:0: Audio: alac
(alac / 0x63616c61), 48000 Hz, mono` — **ALAC (Apple Lossless)**, кодек, для которого НИ ОДИН
браузер не реализует `decodeAudioData` (это не баг конкретного файла и не редкий контейнер —
целый кодек вне спеки Web Audio API). Файл не был битым (что владелец уже подтвердил через
Яндекс.Музыку) — decode честно падал ровно там, где и должен был.
Решение (без новых npm-зависимостей, §3.1 — реюз того, что уже стоит на машине): `server.mjs`
получил dev-only endpoint `POST /local-audio-transcode`, который принимает байты, ремуксит через
уже установленный локально `ffmpeg` (`C:\ffmpeg\bin`, есть в PATH) в PCM WAV во временный файл
и возвращает bytes; `decodeAudioTo16kMono` (`app.js`) при провале `decodeAudioData` пробует этот
endpoint как fallback ПЕРЕД честным отказом. На статическом `public-demo` (GitHub Pages, без
Node-сервера) запрос на `/local-audio-transcode` просто 404-ится, и код проваливается в тот же
честный текст ошибки, что и раньше — zero regression для той сборки (§7 — честность провайдеров,
никогда не имитировать успех).
**Verified**: синтетическая ALAC-фикстура `output/playwright/fixtures/alac-sample.m4a`
(`ffmpeg -f lavfi sine -c:a alac`, не личная запись владельца — приватность фикстур) +
новый `output/playwright/audio-alac-decode.spec.mjs` вызывает `decodeAudioTo16kMono` напрямую
через `window.__lifeosKnowledgeBase` (добавлен в список тестовых хуков) — зелёный. Полный набор
e2e (`human-public` + `final-human-product`, 11/11; `player-dump-usability` + `finance-bank-import`,
6/6) прогнан против свежего процесса `server.mjs` — все зелёные, регрессий нет. `npm run verify`
ok; все аудиты, кроме уже задокументированного `audit-owner-rescue-final.mjs` (не отдельная
регрессия — падает на несовпадении имени ветки и на отсутствии `gh CLI`, как и раньше, плюс
сейчас ожидаемо грязное дерево до коммита).
**Важное ограничение окружения**: порт 4173 в этой сессии держит node-процесс с повышенными
правами (PID из другого запуска) — убить его из песочницы нельзя (`Access is denied` даже от
того же пользователя, того же логон-сессии — похоже на UAC-повышение). Весь e2e-прогон в этой
записи шёл против отдельного тестового процесса на порту 4199 с тем же кодом. **Владельцу**:
после `git pull` нужно вручную перезапустить `npm start` (обычным Ctrl+C и `npm start` в своём
терминале), иначе порт 4173 продолжит отдавать старый `server.mjs` без нового эндпоинта, хотя
статические файлы (app.js/index.html и т.д.) обновятся сами, как обычно.

## Как запустить

```
npm start          # http://localhost:4173
npm run verify     # node --check + smoke (86 маркеров)
npm run stamp      # прошить git-hash в шапку UI + имя SW-кэша (перед каждым коммитом)
```

**Проверка «на что я смотрю»**: в правом верхнем углу шапки приложения виден хеш сборки
(`data-testid="build-version"`). Если он НЕ равен последнему коммиту (`git log --oneline -1`)
— ты смотришь на закэшированную старую версию: обнови страницу (Ctrl+Shift+R); новая
сборка сама инвалидирует старый service-worker-кэш при активации.

## Карта продукта (что уже существует — ответ без чтения истории)

| Модуль | Статус | Тест-доказательство | Срез |
| --- | --- | --- | --- |
| Ядро артефактов (47 коллекций, receipts) | ✅ работает | kb-smoke, export-roundtrip | 2 |
| Universal capture (текст/файл/аудио/фото) | ✅ работает | final-human-product H01-H04 | 2 |
| Люди-сущности + происхождение артефакта | ✅ работает | shift-week-cycle (срез 2) | 2 |
| Финансы (фраза → расход/доход, графики) | ✅ работает | money-fast-capture | 3 |
| Смена такси (часы, доход/час, цель недели) | ✅ работает | shift-week-cycle (срез 3) | 3 |
| Голос → текст (whisper.cpp) | ✅ работает | voice-loop-whispercpp | 6 |
| Чат на реальных данных (деньги/смены/совет) | ✅ работает | chat-real-data (без Ollama) | 5 |
| Чат + Ollama (свободные вопросы) | ✅ работает | ollama-live-chat | 5 |
| Граф уровня Obsidian (фокус, hover, тема) | ✅ работает | final-human-product H07 | 7 |
| Reader/Player (позиция, закладки) | ✅ работает | reader-position-bookmarks | — |
| Агенты/flows (dry-run, kill-switch) | ✅ работает | agent-guarded-runs, flow-execution | 13 |
| Утренняя сводка | ⬜ нет | — | 4 |
| Память 4 слоя + поиск (minisearch) | ⬜ нет | — | 8 |
| Timeline (реконструкция дня) | ⚪ частично (лента) | — | 9 |
| Entity resolution (Даня=Данил) | ⬜ нет | — | 10 |
| Insight engine + вечерняя рефлексия | ⚪ точечно | — | 11 |
| User model / decision support | ⬜ нет | — | 12 |
| Adaptive dashboard + инструкции | ⚪ частично | — | 14 |

## Леджер срезов

### ✅ Срез 1 — Аудит + план переиспользования (2026-07-20)

- Аудит всех 18 пунктов скоупа против реального кода — в плане §2 (что есть, что гэп,
  какой срез добирает). Главное: ядро артефактов/capture/финансы/голос/граф/агенты УЖЕ
  работают и переиспользуются; с нуля не пишется ничего.
- Внедрён хеш сборки: `tools/stamp-version.mjs` (`npm run stamp`) пишет git-hash в
  `index.html` (виден в шапке) и в имя кэша `service-worker.js` — закрыта задокументированная
  ловушка «владелец смотрит на протухший кэш и не видит изменений».
- WIP чат-мозга (RU-промпт с граф-контекстом, честные цитаты, вопрос≠заметка, идентичность
  модели) заморожен ЗАКОММИЧЕННЫМ — становится ядром среза 5. Гейт-спека
  `output/playwright/chat-brain-graph-context.spec.mjs` написана, прогоняется при закрытии
  среза 5 против живого Ollama.
- **Проверить за 60 секунд**: открой http://localhost:4173 → в шапке справа виден хеш
  сборки → сверь с `git log --oneline -1` → совпадает = смотришь на свежую сборку.

### ✅ Срез 2 — Artifact Core + текстовый capture (2026-07-20)

- Категории: Architecture, Discovery-free. Зависит от: среза 1.
- Происхождение артефакта (`source.origin`: text/voice/file) + неизменяемый снимок
  исходного ввода (`source.originalText` — парсеры дополняют `text`, но что реально вошло,
  сохранено навсегда). Люди-сущности (`entities.people`, эвристика заглавных слов не в
  начале предложения + стоп-лист) — видны в карточке «LifeOS понял» строкой «Люди: …».
- **Проверить за 60 секунд**: введи на Дом «Обсудить с Женей ремонт машины» → Разобрать →
  в карточке разбора появится «Люди: Женей».
- Долг: entity resolution (Женей=Женя=Жека) — срез 10; места-сущности — вместе с ним.

### ✅ Срез 3 — Финансы + такси + проекции (2026-07-20)

- Категории: Feature (ежедневный цикл владельца). Зависит от: среза 2, U2-парсера.
- Смена одной фразой: «Отработал 12 часов, заработал 8700, бензин 1900» → предпросмотр
  разбора (часы/доход/расходы) в карточке «LifeOS понял» → «Записать смену» одним нажатием
  → доход 8700 с часами, расход «Бензин 1900», блок «Смена 12 ч» в календаре. Правила/regex
  (`parseShiftEntry`), LLM не в критическом пути. Часы живут на доходной транзакции
  (`shiftHours`) — не отдельная коллекция (SSoT).
- Недельный виджет в Деньгах: Часы, Доход/час, редактируемая «Цель недели», «До цели: X».
- Verified: `shift-week-cycle.spec.mjs` (2/2 с первого прогона) + 14 регрессий
  (chat-actions, money-fast, ai-memory-gate, quick-actions, H01-H10) + все аудиты + smoke.
- **Проверить за 60 секунд**: на Дом введи «Отработал 12 часов, заработал 8700, бензин
  1900» → увидишь разбор → «Записать смену» → открой Деньги: в «Неделе» часы 12 ч,
  доход/час 725 ₽, поставь цель 30000 → «До цели: 21 300 ₽».
- Долг: прогноз/кэш-флоу и отчёты через чат — срезы 5 и 11.
### ✅ Срез 4 — Дашборд + утренняя сводка (2026-07-20)

- Категории: Feature (видимая, первый экран). Зависит от: срезов 2, 3.
- Утренняя сводка — первый блок на Доме (`renderMorningSummary`, `morningSummary(state)` в
  app.js). Только реальные данные: дата + день недели по-русски, прогресс к недельной цели
  (из среза 3), задачи на сегодня с названиями, итоги вчера (выполнено/заработано/потрачено).
  Пустое состояние честное («цель не задана», «задач нет», «вчера записей не было»).
- Побочный фикс (видимая шероховатость, найдена на скриншоте): фраза смены со словом «обед»
  триггерила лишнюю задачу с кривым названием — подавлено, когда ввод распознан как смена.
- Verified: `morning-summary.spec.mjs` 1/1 первым прогоном; срезы 2/3 + money-fast + H01-H10
  + quick-actions + chat-actions зелёные; аудиты/smoke/build-public. Скриншоты light+dark в
  `docs/qc/screens/slice4/`.
- **Проверить за 60 секунд**: открой Дом → вверху «Доброе утро» с датой и честной сводкой.
  Запиши смену (срез 3), поставь цель недели в Деньгах → вернись на Дом → «До недельной
  цели» показывает реальный остаток.

### ✅ Срез 5 — Чат на реальных данных (разморозка WIP) (2026-07-20)

- Категории: Feature (главный интерфейс). Зависит от: срезов 3, 4; V1 WIP (разморожен).
- Чат отвечает на вопросы про деньги/смены/совет ТОЛЬКО из реальных артефактов, без Ollama
  (`chatMoneyDataAnswer` в app.js — регекс + агрегация транзакций). «Сколько заработал за
  неделю?» → сумма доходов + остаток до цели; «Какая последняя смена?» → дата/часы/доход/
  ставка; «Стоит ли работать?» → совет от цели и темпа с обоснованием. Цифры детерминированы
  (не отдаются модели на выдумывание) даже когда Ollama доступна. Ollama-путь (V1 CHAT_BRAIN:
  RU-промпт с граф/днём-контекстом) остаётся для свободных вопросов.
- Чистка мусора в глазах (по прямой просьбе владельца): убраны технические строки
  `receipt receipt_xxx` из пузырей чата; кнопка «Сделать задачей» больше не висит под
  вопросами (только под утверждениями).
- Побочный фикс: `pluralRu` в app.js (ui-шный `plural` там недоступен, вызывал тихую
  ошибку → чат не отвечал вообще; поймано браузерной диагностикой).
- Verified: `chat-real-data.spec.mjs` (без Ollama, детерминированный) + chat-actions +
  ai-memory-gate + chat-thread-search + market-owner + owner-rescue + H01-H10 зелёные.
  Скриншоты light+dark в `docs/qc/screens/slice5/`.
- **Проверить за 60 секунд**: запиши 1-2 смены (срез 3), поставь цель недели (Деньги),
  открой Чат → спроси «Сколько я заработал за неделю?» и «Стоит ли завтра работать?» —
  увидишь реальные цифры и обоснованный совет.
- Долг: живой Ollama-гейт (`chat-brain-graph-context.spec.mjs`) — при стабильной среде
  (реальная CPU-генерация флейкует под нагрузкой, см. LESSONS.md); отчёты за месяц/периоды —
  срез 11.

**Срез 5.5 — редизайн чата в стиле ChatGPT/Claude (2026-07-20, frontend-design pass):**
диалог стал героем (во всю ширину, сообщения в центрированной колонке ≤900px, композер
прижат к низу), а гигантская карточка настроек Ollama слева (330px в пол-экрана) сжата в
компактную приглушённую полоску сверху. Все testid сохранены (probe/test остаются видимыми
для тестов). Побочный фикс: тулбар перехватывал клики по кнопкам в треде из-за фиксированной
`100vh`-высоты + `overflow:hidden` → тред теперь скроллится по `max-height`, всё в нормальном
потоке. Скриншоты в `docs/qc/screens/slice5b/`. Verified: все чат-тесты + H01-H10 +
market-owner + owner-rescue зелёные. Примечание: скилл `frontend-design:frontend-design`,
который просил владелец, в сессии недоступен — сделано ручной экспертной CSS-работой.

### ✅ Срез 6 — Whisper + голосовой пайплайн (2026-07-20)

- Категории: Feature (голос как первоклассный вход). Зависит от: срезов 2, 3, 5, 5.5; U3.
- whisper.cpp расшифровка уже работала (U3). Добрано: расшифровка (голос ИЛИ ручная)
  проходит ТОТ ЖЕ разбор, что и печатный ввод — `saveSourceTranscript` теперь кладёт
  расшифровку сообщением владельца в чат и создаёт типизированное предложение через
  `createChatMessageProposal`. Надиктованное «отработал 12 часов заработал 8700 бензин 1900»
  → предложение-СМЕНА в чистом чате (срез 5.5) → «Записать смену» одним нажатием → реальные
  доход+часы+расход. Люди-сущности из расшифровки тоже извлекаются (срез 2). `directTypes`
  получил "shift" — надиктованная смена больше не порождает мусорные «разобрать/задачи».
- Verified: `voice-shift-pipeline.spec.mjs` (детерминированный, без демона — ручная
  расшифровка = тот же путь; реальный голос через whisper.cpp — `voice-loop-whispercpp`,
  нужен демон) 1/1 первым прогоном; shift-week + chat-actions + chat-real-data + H01-H10 +
  whisper honest gate + waveform (изолированно) зелёные.
- **Проверить за 60 секунд**: Плеер → добавь аудио → в «Ручная расшифровка» впиши
  «Отработал 12 часов, заработал 8700, бензин 1900» → «Сохранить расшифровку» → открой Чат:
  увидишь надиктованное сообщение с предложением «Смена» → «Записать смену» → деньги записаны.
  (С запущенным `npm run whisper-server` тот же путь идёт от реального голоса.)
- Долг: сущности эмоции/теги из транскрипта — позже; надёжность реального демона под
  нагрузкой — среда-флейк, см. LESSONS.md.

### ✅ Срез 7 — Граф (редизайн до уровня Obsidian) (2026-07-20)

- Категории: Feature/визуал. Зависит от: срезов 1-6 (данные для узлов).
- Граф-канвас (`GraphCanvas` в app.js) переделан до уровня Obsidian и чище: тема-aware фон
  (раньше захардкожен тёмным независимо от темы приложения); размер узла по числу связей
  (хабы крупнее); hover-подсветка узла + курсор-pointer; **фокус-эффект Obsidian** — навёл/
  выбрал узел → его окрестность яркая, рёбра синие, остальное приглушается; мягкое свечение
  на выбранном/наведённом; умные подписи (у хабов/выбранного/наведённого, иначе прячутся,
  чтобы не было каши). Панель графа, тулбар, фильтры, сегмент-контрол переведены на токены
  темы. **Фикс вёрстки:** тулбар был 2-колоночным grid → 12 фильтров зажимались в узкую
  колонку и стекались на 6 рядов, толкая канвас под фолд (y=911); теперь flex-колонка,
  компактные чипы-фильтры, канвас — герой левой колонки.
- Verified: H07 (граф), kb-smoke, product-brain, product-map-graph, first-run-calm,
  owner-rescue зелёные; визуальные аудиты зелёные. Скриншоты light+dark в
  `docs/qc/screens/slice7/`.
- **Проверить за 60 секунд**: накидай несколько мыслей/смен → открой Граф → наведи/кликни
  узел: его связи и соседи подсвечиваются, остальное гаснет (как в Obsidian). Работает в
  обеих темах.
- Долг: переходы человек→встречи→идеи полнее заиграют после entity resolution (срез 10);
  двойной инспектор (чистый + «Контракт объекта») — отдельная уборка позже.

**Срез 7.5 — честная доводка графа после реакции владельца (2026-07-20):** владелец
сравнил со скриншотами Obsidian — реальный граф был «клубком». Три причины (честно): (1)
объём данных — в Obsidian-картинках зрелые хранилища на сотни-тысячи заметок, у владельца
~15-35 артефактов (это данные, не код); (2) на канвас валилась ВНУТРЕННЯЯ машинерия
(provider-run «ollama probe», flow-run «pxa boot», предложения, чат-логи) — теперь
`graphForDisplay` прячет их (`GRAPH_DISPLAY_HIDDEN_TYPES`), граф показывает только жизненные
артефакты (как Obsidian показывает заметки, а не логи); (3) стиль — узлы уменьшены (5+val*0.6,
cap 16), связи тоньше и бледнее (0.9px, ярко только у окрестности), цвета приглушены,
подписи реже/мельче (11px, только хабы deg≥4/выбранный/наведённый); раскладка шире
(репульсия 8600, стартовый радиус 130-430). Verified: H07, product-map-graph, kb-smoke,
product-brain зелёные; счётчик графа не ассертится порогом, фильтр безопасен. Скриншоты
`graph-refined-*.png`. Остаётся честный разрыв с Obsidian по «галактике» — это объём
хранилища, наполнится с использованием.

**Срез 7.6 — фикс P19 J16 (перехват клика в чате), вскрытый полным прогоном (2026-07-20):**
полный P19 упал на `chat-to-proposal.last().click()` в J16. Три итерации диагностики (первые
две — неверные: z-index; свернуть «Ещё»). Настоящая причина: `.chat-thread` был
`display:grid; align-content:end` → при почти полном треде верхнее сообщение выпирало ВЫШЕ
границы контейнера (кнопка на y=74, `scrollTop` застрял на 0) и пряталось за тулбаром, который
к J16 стал 2-строчным (длинный чип «Ollama: модели найдены» после пробинга в J15). Клик
перехватывался намертво. Фикс: тред → `display:flex; column` + `margin-top:auto` на первом
ребёнке (низ-прижатие без выхода вверх), убран `scroll-behavior:smooth`. Плюс улучшение: «Ещё»
(расширенные настройки Ollama) — управляемое состояние `chatToolbarMoreOpen` (не нативный
`<details>`, который сбрасывался ре-рендером), по умолчанию свёрнуто → тулбар компактнее.
Полный разбор — DECISIONS.md, переиспользуемый урок — LESSONS.md. **Проверить за 60 секунд**:
пропроси Ollama в Чате → накидай ~10 мыслей → отправь сообщение в чат → нажми «Сделать
задачей» под последним сообщением — кликается без залипания. Verified: полный P19 (J01-J24)
зелёный, semantic-search + kb-smoke зелёные.

### ✅ Срез 8 — Память + context engine + поиск (2026-07-21)
**8.1 — 4 слоя памяти как вычисляемая проекция:** память = НЕ 4 хранилища (это сломало бы
SSoT), а `memoryLayers(state)` — чистая проекция над теми же заметками по возрасту (`updatedAt`)
и связности (backlinks + wikilinks). Каждая заметка ровно в одном слое: Активная (свежее ≤2 дн /
открыто сейчас) · Рабочая (≤14 дн или degree≥3) · Долговременная (старее, но degree≥1) · Архив
(старое и одинокое). Панель «Память» в Базе — 4 карточки со счётчиком, подсказкой и топ-заметками
(кликабельны). Пороги — явные константы. Verified: `memory-layers.spec.mjs`; скриншоты
`docs/qc/screens/slice8/`.

**8.2 — minisearch в полнотекстовый поиск (наконец в деле):** установленный minisearch (до сих
пор не использован) подключён динамическим `import()` (dist/es самодостаточен, без bare-импортов —
не как chart.js). `searchMemory(state, q)` строит индекс над заметками (кэш по сигнатуре
notes×updatedAt, пересборка только при изменении) и ищет с prefix+fuzzy+ранжированием. Поиск в
панели «Память» (input + результаты), локальный, всегда доступен (в отличие от семантического —
тот gated за эмбеддинги). Не переписывал `searchNotes` — добавил движок рядом. Verified:
`memory-search.spec.mjs` — СЦЕНАРИЙ: пишу заметки → префикс «финанс» находит «Финансовый…» →
опечатка «кофн» (fuzzy) находит «кофе», нерелевантное отсеяно → клик открывает заметку.

**8.3 — поиск по памяти из чата:** намерение «найди/вспомни в памяти X» в чате отвечается тем же
minisearch'ем (`looksLikeMemoryRecall` + `memoryRecallQuery`), детерминированно, раньше денежного/
LLM. Ответ в чате + запись в ОБЩИЙ `memorySearchReport`, поэтому Чат и панель «Память» Базы
показывают одно и то же (переиспользование, не второй механизм). Verified:
`chat-memory-recall.spec.mjs` — СЦЕНАРИЙ: заметки → в чате «найди в памяти кофе» → ассистент
отвечает названием → тот же результат в панели «Память» → клик открывает заметку.

**Проверить за 60 секунд**: База → «Память»: заметки по слоям + строка поиска (набери «кофе» с
опечаткой — найдёт). Чат → «найди в памяти <слово>» — ответит и подсветит те же заметки в Базе.
Гейты: verify ok, 27/28 аудитов (owner-rescue — dirty-tree), chat-actions/chat-real-data/
ai-memory-gate/kb-smoke зелёные. Побочно починен пред-существующий `chat-brain-graph-context`
(не хватало обработчика confirm-диалога probe; таймауты подняты под реальную скорость qwen3 ~140с).

### ✅ Срез 9 — Timeline (2026-07-21)
Единая ось жизни + реконструкция любого дня. `timelineDays(state)` — проекция над теми же
артефактами (смены, деньги, задачи, планы, напоминания, чистые мысли), сгруппированная по дням;
`state.timelineDay` = фильтр одного дня. Панель «Хроника жизни» в Ленте: дни с суммой доход/
расход, события на вертикальной оси (время · иконка · название · сумма), клик открывает артефакт;
поле даты + «Все дни» восстанавливают/сбрасывают день. **Антимусор** (правило владельца): сырые
`.md`-мысли не дублируют структурный артефакт (источник, чей noteId держит смену/расход/задачу,
скрыт — это вход, не событие), и авто-план-блок смены (общий noteId с транзакцией) не двоит смену;
самостоятельные планы/мысли остаются. Не машинерия (провайдер-раны/паки/твин остались в Ленте-
каналах). Repository First: переиспарзили `state.*` и готовые `.day/.createdAt`, ничего нового не
храним, кроме UI-фильтра. **Проверить за 60 секунд**: наговори смену+расходы+задачу → Лента →
«Хроника»: события по дням; поставь дату в «Восстановить день» — покажет один день; клик по
событию открывает заметку. Verified: `timeline-day-reconstruct.spec.mjs` (СЦЕНАРИЙ: смена+расход →
расход на вчера → два дня → фильтр дня → сброс → клик открывает), presentation-runtime + kb-smoke
зелёные; скриншоты `docs/qc/screens/slice9/`.

### ✅ Срез 10 — Entity Resolution (2026-07-21)
«Данил»=«Даня» → одна сущность; при неуверенности — спросить, не сливать молча. `resolvePeople`
собирает извлечённые `entities.people` источников, канонизирует через известные уменьшительные
(`KNOWN_PERSON_ALIASES`: Даня→Данил, Катя→Екатерина…) и подтверждённые владельцем слияния
(`state.entityAliases`), группирует в канонических людей с алиасами и счётчиком. Неоднозначные
пары (общий префикс/малый edit-distance) — не сливаются авто, а показываются как предложение
`personMergeSuggestions` с уверенностью; владелец жмёт «Объединить»/«Нет» (owner-gated, дух
proposal-механизма). Панель «Люди» в Графе. **Проверить за 60 секунд**: наговори «Проект вёл
Данил. Помогал Даня. Пришёл Артур. Ещё Артурчик» → Граф → «Люди»: Даня под Данилом; предложение
слить Артурчик+Артур; жми «Объединить» — станут одним. Repository First: работаем над уже
извлечёнными именами, новый только `entityAliases`/`dismissedPersonMerges`. **Честное
ограничение**: падежные формы («Женей», «Машей») пока не лемматизируются (нужен рус-стеммер —
отдельный пакет по Repository First, следующий шаг); номинативные резолвятся. Verified:
`entity-resolution.spec.mjs` (авто-слияние + owner-gated пара + подтверждение), kb-smoke +
presentation-runtime зелёные, 27/28 аудитов; скриншоты `docs/qc/screens/slice10/`.

### ✅ Срез 11 — Рефлексия + Insight Engine (2026-07-21)
`computeInsights(state)` — ВЫЧИСЛЯЕМЫЕ закономерности из реальных артефактов: повторяющиеся траты
(категория ≥3 раз), тренд расходов неделя-к-неделе, забытые цели (без движения ≥14 дн),
просроченные задачи. Проекция (как memoryLayers/timelineDays) — пересчитывается на рендере, всегда
свежая при первом открытии за день («ночной» пересчёт бесплатно). Панель «Инсайты» на Доме: иконка,
заголовок, детали, уверенность, ссылки на источники. «Закрепить» создаёт ПОСТОЯННЫЙ insight-артефакт
(reuse `addInsight` + receipt, owner-gated — молчаливой memory-write нет). `eveningReflection` —
«Подвести день»: деньги/задачи/записи за сегодня + верхний инсайт. **Проверить за 60 секунд**:
наговори «кофе 300» трижды и «такси 500/480/520» → Дом → «Инсайты»: «Частый расход: Кофе/Такси»;
жми «Закрепить» — появится артефакт; ниже «Подвести день» с итогом. Repository First: переиспользуем
`state.*` артефакты и существующий `addInsight`; новых коллекций нет (инсайты вычисляемые). Verified:
`insight-engine.spec.mjs` (3 расхода → повторяющаяся трата → закрепление создаёт артефакт → рефлексия),
morning-summary + kb-smoke зелёные, 27/28 аудитов; скриншоты `docs/qc/screens/slice11/`.

### ✅ Срез 12 — User Model + Confidence + Explainability + Decision Support (2026-07-21)
`computeUserModel(state)` — вычисляемые характеристики из артефактов: Дисциплина (доля выполненных
задач), Ритм (активных дней из 14), Энергия (часы смен за неделю). Каждая с УВЕРЕННОСТЬЮ (по размеру
выборки) и раскрытием «Почему» (details) — на каких данных. `workDecisionSupport(state)` — решение
«стоит ли завтра работать?» на реальных цифрах смен: рекомендация + уверенность + обоснование
(остаток до цели, темп ₽/смена) + альтернативы. Панель «О тебе» на Доме (характеристики + карточка
решения). Ничего не приговаривает — только наблюдаемые числа с объяснением. **Проверить за 60
секунд**: задай цель недели в Деньгах, запиши 1-2 смены → Дом → «О тебе»: три характеристики с
«Почему»/уверенностью; ниже «Стоит ли завтра работать?» с обоснованием и альтернативами. Repository
First: переиспользуем артефакты + существующий goal-based совет из чата (срез 5), новых коллекций
нет (всё вычисляемое). Verified: `user-model-decision.spec.mjs` (3 характеристики с уверенностью+
почему; решение «Стоит выйти» с обоснованием «осталось…» и альтернативами), morning-summary +
kb-smoke зелёные, 27/28 аудитов; скриншоты `docs/qc/screens/slice12/`.

### ✅ Срез 13 — Agent Center (2026-07-21)
Делегирование ЗАДАЧИ агенту поверх существующих agentRuns/proposals. `delegateTaskToAgent(state,
taskId)` разбивает задачу на шаги-предложения (выделить время + напоминание), создаёт agentRun со
статусом preview, шагами и прогрессом. Панель «Центр агентов» в Агентах: список открытых задач с
«Поручить», карточки прогонов со статусом/шагами/прогрессом/результатом, «Одобрить» применяет
(reuse `approveAgentRun`). Всё как предложения — та же граница безопасности (никаких тихих действий).
**Проверить за 60 секунд**: запиши задачу → Агенты → «Центр агентов» → «Поручить» у задачи → появится
черновой прогон с шагами (0/2) → «Одобрить» → «выполнено» 2/2, план+напоминание применены. Repository
First: тот же agentRuns/proposals/approve, новых механизмов нет. Verified: `agent-center.spec.mjs`
(делегирование → статус/шаги/прогресс → одобрение применяет), kb-smoke зелёные, 27/28 аудитов;
скриншоты `docs/qc/screens/slice13/`.

### ✅ Срез 14 — Adaptive Dashboard + Инструкции (2026-07-21)
**14.1 — адаптивный дашборд:** виджеты Дома (утро/инсайты/о-тебе/подвести-день/мой-день) владелец
скрывает/переставляет; порядок и скрытые в `state.dashboardLayout` (проекция дополняет новые
ключи). Каждый виджет — hover-контролы ↑/↓/Скрыть; скрытые — в полоске «Скрытые виджеты» с
«показать». Капча-первый: ввод выше настраиваемых панелей. Verified: `adaptive-dashboard.spec.mjs`
(скрыть→в скрытые→показать→переставить, переживает ре-рендер).

**14.2 — правила системы от владельца:** `state.ownerInstructions` — правила поведения как
артефакты; активные РЕАЛЬНО влияют — вплетаются в промпт локальной модели
(`buildOllamaChatPrompt`). Пресеты (готовые частые правила) + свободный ввод + вкл/выкл/удалить.
Панель «Правила системы» в Контроле. Verified: `owner-rules.spec.mjs` — добавить → правило доходит
до промпта (проверено напрямую, без Ollama) → пресет → выключить (уходит из промпта) → удалить.

Repository First: адаптивный дашборд — UI-состояние поверх готовых виджетов; правила — вплетаются
в существующий `buildOllamaChatPrompt`, новых механизмов нет. **Проверить за 60 секунд**: Дом →
наведись на виджет → «Скрыть»/↑↓; внизу «Скрытые» → «показать». Контроль → «Правила системы» →
добавь правило или пресет — оно повлияет на ответы в Чате. Гейты: verify ok, 27/28 аудитов,
kb-smoke/morning-summary зелёные; скриншоты `docs/qc/screens/slice14/`.

## 🏁 v1.4 FULL_BUILD — все 14 срезов закрыты (2026-07-21)

## Канон дизайна в коде — 6 пакетов (2026-07-26)

Работа по `design-system/HANDOFF.md` (единственный канон) и `research/INTEGRATION_ROADMAP.md`.
Правило то же: только вычисленные данные, никаких заглушек; нет функции — честно сказано.

### ✅ 1. Объект (канон Artifact Inspector.dc.html)
Новая поверхность `object` — «проваливание в карточку». Проекция `computeObjectInspector` над
существующими коллекциями, новых сущностей нет. Вердикт считается из реальных чисел (нужно / есть /
свободный поток по счетам за 90 дней); у каждого числа виден источник (закон №5); у связи — тип,
сила, объяснение и время появления (закон №8), причём тип выводится из положения дел (просроченная
задача цели = «блокирует»); хронология меряет разговоры против действий по месяцам; противоречия
дают варианты с посчитанной ценой, выбор реально меняет цель или задачу, пишет чек и откатывается.
Провалиться можно из Графа, Базы, Сегодня, Дома, поиска и журнала чеков. 9 пунктов меню не тронуты.
**Проверить за 60 секунд**: Сегодня → у цели «Объект» → вкладки Суть/Источники/Связи/Хронология/
Противоречия → выбрать вариант → «Вернуть как было».
Verified: `artifact-inspector.spec.mjs` 3/3.

### ✅ 2. Сегодня (канон Today.dc.html)
День начинается с одного предложения о том, что сегодня решается. У каждого блока написано, ПОЧЕМУ
он здесь; пересечение по времени — настоящий конфликт с посчитанным первым свободным окном; у задачи
виден эффект на цель («станет 2 из 3 шагов»), а без цели — честное «посчитать не из чего»;
«не сегодня» показано вместе с причиной. Попутно исправлено: `addTask` вешал каждую новую задачу на
первую попавшуюся активную цель — с «эффектом на цель» это стало бы враньём с уверенностью.
Verified: `today-day-plan.spec.mjs` 3/3.

### ✅ 3. Разбор дня (канон Universal Capture.dc.html)
Стадии конвейера с РЕАЛЬНЫМИ счётчиками: прочитано захватов, расшифровано голосовых, извлечено
сущностей, совпадений с графом (закон №4), новых связей, наблюдений. Спрашивает только про жизнь
владельца: служебные шаги самого разбора считаются отдельно и в вопросы не выносятся.
Verified: `day-digest.spec.mjs` 2/2.

### ✅ 4. Вопрос остаётся вопросом (закон №7)
Раньше вопрос в универсальном вводе превращался в задачу «Вытащить задачи из „Почему я до сих пор
не купил машину?“». Теперь система отвечает: retrieve→rank→answer на существующем minisearch,
дословные цитаты со ссылкой в объект-источник, честный отказ при нехватке данных и явная строка
«Ничего не создано». Уверенность ниже 75% на разборе тоже становится вопросом, а не предложением.
Verified: `question-stays-question.spec.mjs` 3/3.

### ✅ 5. Поиск ⌘K по всему
Один ввод находит артефакты, цели, задачи, людей, файлы и голосовые, цитаты, деньги, привычки,
инсайты, связи и команды — сгруппированно, без префиксов. Нечёткий поиск только по названию (по телу
заметки «м-а-ш-и-н» находилось почти везде), точные совпадения выше нечётких, внутренняя проводка
графа в результаты не попадает. Результат — вход в объект.
Verified: `omni-search.spec.mjs` 3/3.

### ✅ 6. Агенты, Контроль, Builder
Агенты: план → подтверждение → исполнение → отчёт → квитанция; триггер, шаги и права показаны
обеими сторонами (что может и чего НЕ может). В списке только агенты с настоящим кодом за ними:
Вечерний разбор, Наблюдатель целей, Тихий секретарь. Контроль: маршруты наружу с точным составом
данных (отключённый прямо говорит «ничего не уходит») и журнал чеков с откатом там, где возврат
реализован. Builder: обещание «своя сущность сразу получает контракт объекта» показано проверкой по
фактам, а не галочкой авансом.
Verified: `named-agents.spec.mjs` 3/3, `control-routes-receipts.spec.mjs` 2/2,
`builder-contract.spec.mjs` 1/1 (+1 skip — в чистом хранилище записей систем ещё нет).

**Гейты пакетов:** `npm run verify` зелёный, аудиты как на базовом коммите (те же 6 падают по
отсутствующим скриншотам и `gh` CLI — не регресс), `final-human-product` 12/13 (H09 падает и на
базовом коммите), `node tools/build-public.mjs` прогнан.
**Не сделано и честно помечено:** расписания для агентов нет — все запускаются вручную; распознавание
чеков с фото не реализовано, поэтому агента «Хранитель чеков» из канона в списке НЕТ.

### ✅ P0-1 для людей — «один человек, а не список написаний» (2026-07-26)
Люди пошли тем же путём усиления, что задачи, цели и привычки. **Три настоящих дефекта, которые
это вскрыло:**
1. **Слияние людей было невидимо.** `merge-person` писал только аудит — в журнале чеков Контроля
   его не было, то есть значимое решение проходило мимо закона №3. Теперь `mergePersonInto`
   оставляет чек `reinforce` («…упоминания сведены в существующего, второй объект не
   создавался») — тем же текстом, что усиление любого другого объекта.
2. **Слияние было необратимо.** Отменить ошибочное «объединить» из интерфейса было нечем.
   Появились `unmergePerson` и кнопка «Разъединить» в карточке человека; разъединение ещё и
   закрепляет форму, иначе словарь уменьшительных свёл бы её обратно на следующем чтении.
3. **Нечёткое сравнение имён склеивало разных людей.** Расстояния Левенштейна ≤ 1 хватало,
   чтобы предложить слить «Марину» с «Кариной», «Олю» с «Колей». Донор Graphiti
   (`dedup_helpers.py`): нечёткому сравнению можно доверять только на своеобразных именах.
   Правило языка точнее порога энтропии: русское имя расходится в ХВОСТЕ, а не с первой буквы,
   поэтому добавлен порог общего начала `PERSON_MIN_SHARED_PREFIX` = 3 и вторая независимая
   проверка 3-граммами. Предложение теперь называет, на чём держится: «совпали первые 5 букв,
   различие в 1 букве, общих трёхбуквенных кусков 71%».

**Сведение по основе имени стало отменяемым.** «Ярослав» и «Ярослава» дают одну основу и молча
становились одним человеком — теперь владелец говорит «это другой человек», форма попадает в
`state.personSplits` и живёт своим узлом; для неё отключены и словарь уменьшительных, и основа.
id узла человека переехал с основы показанного имени на ключ группы — иначе после разделения обе
карточки открывали бы один объект.

**Проверить за 60 секунд**: наговори «Марина против кредита на машину» и «Карина прислала смету» →
Граф → «Люди»: два человека и НИ ОДНОГО предложения слить. Потом «Ярослав прислал смету» и
«Ярослава ждёт ответа» → один человек → открой карточку → «Это другой человек» → снова двое, чек
в Контроле, и после перезагрузки разделение на месте.
Verified: `people-one-object.spec.mjs` 4/4, `no-duplicates.spec.mjs` 3/3,
`artifact-inspector.spec.mjs` 3/3, `omni-search.spec.mjs` 3/3; `npm run verify` зелёный,
аудиты — те же 6 базовых падений, `node tools/build-public.mjs` прогнан.

### ✅ Наблюдение о себе — не дело (2026-07-26)
Второй дефект из того же сквозного прогона вечернего дампа. «После тренировки закрываю больше
задач» становилось ЗАДАЧЕЙ: слово «задач» включало распознавание дела, и в списке появлялась
строка, которую нельзя ни сделать, ни закрыть. «Английский снова откладываю» не понималось
вообще и оседало непонятым захватом — а это самая ценная запись для второго мозга.

Признак наблюдения — форма высказывания, а не словарь глаголов: **сравнение или регулярность**
(«больше», «чаще», «снова», «постоянно», «обычно») **при отсутствии обязательства** («надо»,
«нужно», «купить», «позвонить»…) **и без срока**. Границы слов заданы явно кириллическими
lookaround'ами — JS `\b` по кириллице не работает, и без них «на основании» читалось бы как
«снова». Наблюдение забирает запись у задачи и календаря и становится инсайтом, у которого в
качестве источника — собственные слова владельца, ничего не досчитано.

**Намеренная граница (закон №6):** запись со сроком наблюдением не считается вовсе. «Завтра снова
еду на объект» — это план, и уводить его из планирования по слову «снова» нельзя. Из-за этого
«Сегодня опять ничего не сделал» правилами не ловится — записано в роадмап честно, полное решение
за фоновым LLM-разбором (P2-5).

**Проверить за 60 секунд**: наговори «После тренировки закрываю больше задач» → примени всё →
в Сегодня такой задачи НЕТ, а в поиске ⌘K она находится как наблюдение. Потом «Надо больше спать»
— это остаётся делом. Разбор дня отдельной строкой пишет, сколько записей — наблюдения о себе.
Verified: `observation-not-task.spec.mjs` 4/4; регресс `evening-dump` 1/1, `graph-brain` 6/6,
`day-digest` 2/2, `behavior-patterns` 3/3, `contradictions` 3/3.

### ✅ Машинерия и повторы не становятся знанием (2026-07-26)
Прогон вечернего дампа пробом (22 записи → применить всё → распечатать ВСЁ, что получилось)
показал: коллекция знаний состояла из машинерии. **60 «инсайтов», из них 44 — служебные шаги**
(«Сохранить источник в библиотеку» ×22, «Записать связи и контроль» ×22), остальные — узлы
сущностей вида «👤 Дмитрию» и «📅 август». Настоящих наблюдений было 3. Четыре дефекта:

1. **Служебный шаг разбора становился объектом-знанием.** Он описывает, что делает СИСТЕМА.
   Теперь шаг отмечается выполненным (запись и связи созданы ещё при захвате) и объекта после
   себя не оставляет.
2. **Человек получал второй объект.** Карточка `person:<имя>` уже собрана из упоминаний, а
   материализация сущностей заводила рядом «👤 Дмитрию»: один человек находился в поиске дважды
   (закон №4). Признание людей теперь фиксируется чеком на существующую карточку. Дата объектом
   не является вовсе — «📅 август» это признак записи.
3. **«Английский» читался как страна Англия** — основа `Англи` совпадает с началом слова.
   Прилагательное отсекается по суффиксу (-ский/-ская/-ском…), а не словарём. Порог пришлось
   уточнять: грубое правило «содержит ск» выкидывало и «Москве».
4. **«Ремонт» читался как человек** — заглавное нарицательное в начале фразы. Плюс многословный
   проект «Новая Платформа» рассыпался на двух людей: имя проекта лежало в наборе целиком, а
   заглавные слова проверялись по одному.

**После:** те же 22 записи дают **3 инсайта — ровно три настоящих наблюдения о себе**, одного
человека (Дмитрий) вместо трёх (Дмитрий, Марина, Ремонт → теперь Дмитрий и Марина), и ни одной
служебной строки в знаниях.

**Намеренное изменение контракта I3** (обосновано, а не подгонка теста): `entity-extraction.spec`
проверял материализацию на ПЕРВОМ предложении — им оказывалось предложение о людях. Теперь
проверяет на месте/проекте (у них нет другого дома) и отдельно требует, чтобы человек второго
объекта не получал.

**Проверить за 60 секунд**: наговори «Ремонт кухни аванс подрядчику» → примени всё → в Базе НЕТ
знания «Сохранить источник в библиотеку», в Графе → «Люди» НЕТ человека по имени Ремонт.
Verified: `knowledge-not-machinery.spec.mjs` 5/5, `entity-extraction.spec.mjs` 5/5.

### ✅ Дело — делом, привычка — привычкой (2026-07-26)
Третий заход пробом по тем же 22 записям. Три дефекта разбора:

1. **«поСЧИТАТЬ» читалось как «читать».** `hasAnyText` сравнивает подстроками, и «Посчитать
   свободные деньги за 3 месяца на машину» становилось ПРИВЫЧКОЙ. Тот же класс ошибки, что
   «на основании» вместо «снова». Сигналы привычки переведены на регулярку с явными границами
   слов и разделены по силе: «привычка», «тренировка», «медитация», «зарядка» означают
   регулярное занятие сами по себе; «вода», «сон», «читать» — нет («Счёт за воду пришёл» это
   платёж), они работают только вместе с явной повторяемостью.
2. **Инфинитив в начале записи не распознавался как дело.** «Оценить продажу старой машины»,
   «Собрать документы» уходили в «не разобрано»: словарь глаголов их не покрывал. Добавлено
   правило языка — инфинитив опознаётся по ГЛАСНОЙ перед «-ть» (оцен-и-ть, посчит-а-ть), потому
   что существительные на «-ть» идут через согласную («часть», «новость», «есть»). Возвратное
   «-ся/-сь» снимается. Числительные и «опять» — закрытым коротким списком.
3. **Заголовок привычки терял величину.** Чистка вырезала любое число из 2–9 цифр вместе с
   необязательным знаком валюты, и привычка называлась «Тренировка силовая минут». Теперь число
   перед единицей измерения (минут, км, кг, раз, страниц…) остаётся.

Плюс: наблюдение о себе больше не заводит привычку — «После тренировки закрываю больше задач»
говорит о связи тренировки с продуктивностью, а не о намерении тренироваться.

**Те же 22 записи, итог:** 5 задач (все настоящие), 1 цель, 1 привычка с правильным названием,
5 денежных записей, 3 наблюдения, 2 человека, 4 записи честно помечены неразобранными — и все
четыре действительно неоднозначны («Марина согласна на август если без кредита», «Смета на кухню
пришла», «Может вообще без машины обойтись», «Счёт за воду пришёл»).

**Проверить за 60 секунд**: наговори «Оценить продажу старой машины» → в Сегодня появилось дело.
«Посчитать свободные деньги» → тоже дело, а не привычка. «Тренировка силовая 55 минут» → привычка
с числом в названии.
Verified: `task-vs-habit.spec.mjs` 6/6.

### ✅ Срок цели — из слов владельца (2026-07-26)
«Хочу купить машину до августа» оставалась целью **вообще без срока**: разбор дат работает по
конкретным дням, а месяц называют в записи чаще, чем число. Без срока не считается ни прогноз,
ни вероятность — цель существовала как надпись.

`parseGoalDeadline` читает месячный срок и различает предлоги по смыслу: **«до августа» — успеть
ДО начала месяца, значит 1-е число; «в сентябре» / «к декабрю» — внутри месяца, значит его
последний день.** Середину месяца не придумываем. Названный месяц всегда ближайший будущий:
«до марта», сказанное в июле, означает март следующего года.

Конкретный день сильнее месяца: если число названо, месячное правило не вмешивается.
У выведенного срока видно происхождение (закон №5): в карточке цели рядом с датой стоит «срок
распознан из слов «до августа»: успеть до начала месяца», а не общий провенанс объекта. У срока,
введённого руками, объяснения нет — и это честно видно.

**Проверить за 60 секунд**: наговори «Хочу купить машину до августа» → примени → открой цель:
срок 1 августа, и под ним написано, из каких слов он взят.
Verified: `goal-deadline.spec.mjs` 4/4.

### ✅ Позиция человека — это условие, а не потерянная запись (2026-07-26)
Строка прямо из целевого сценария владельца: **«Марина согласна на август если без кредита»**
уходила в «не разобрано». Имя система видела, месяц видела — а смысла записи не видела вовсе.
При этом именно условие решает, выполнима цель или нет.

Позиция распознаётся по форме: предикат отношения («согласна», «против», «не хочет», «готов»,
«просит», «требует», «настаивает», «возражает», «отказал», «разрешил») плюс имя человека
**строго перед ним**. Условие берётся из «если / при условии / только». Запись становится
утверждением (`claim` — коллекция уже была), у которого хранится кто сказал, какова позиция и
при каком условии она держится.

**Условие стоит НА ЦЕЛИ**, а не отдельной записью в стороне: на карточке цели появляется связь
типа «ограничивает» с дословным условием и подписью, кто его назвал. Иначе условие не участвует
в решении и никто не увидит его в нужный момент. Связь ищется по общей ОСНОВЕ предметных слов
(«машину» в условии ↔ «машина» в цели): русский склоняет.

Позиция видна и в карточке самого человека — это то, что владелец о нём знает.

Попутный дефект, найденный спекой: «Против кредита на машину» давало **человека по имени
Против** — заглавное слово позиции попадало в имена. Имя теперь должно стоять строго перед
позицией, а сами слова позиции добавлены в стоп-лист имён.

**Проверить за 60 секунд**: наговори «Хочу купить машину до августа», потом «Марина согласна на
август если без кредита» → примени → открой цель → вкладка «Связи»: «ограничивает · Марина —
согласна. Условие: без кредита».
Verified: `person-stance.spec.mjs` 5/5.

### ✅ Похожее по тексту — честный поиск без нейросети (2026-07-26)
Роадмап держит P2-1 «семантические соседи» как эмбеддинги — модели в продукте нет, и обещать её
нельзя. Но лексическая половина гибридного ретривера (донор-идея LlamaIndex/Haystack) делается
нативно и работает: **tf-idf по трёхбуквенным кускам с косинусом**. Русская морфология тут не
мешает, а помогает: «машину» и «машины» дают почти одинаковые наборы кусков, где точное сравнение
слов уже проваливается.

Блок называется **«Похожее по тексту»**, и подпись под ним говорит прямо: совпадение слов и их
форм, а не смысла. У каждой строки — процент и за что она сюда попала («общие слова: машины»).
Соседи по графу исключаются: показывать связанный объект ещё раз как «похожее» — повтор, а не
находка. Сравнение идёт по ключу группы графа жизни, а не по id: узел носит id самой сильной
проекции, и открытая цель искалась бы по id, которого в индексе нет.

**Дефект, который вскрыл проб:** тело заметки начинается служебной шапкой («Source artifact»,
«Kind: text», «Repository status»), одинаковой у ВСЕХ записей. До её удаления «Позвонить маме в
выходные» и цель про машину были похожи на **54%** — по одной этой шапке. Латинские ключи
«слово:» в русском теле — всегда машинерия, по ним правило и построено.

**Проверить за 60 секунд**: наговори «Хочу купить машину до августа» и «Оценить продажу старой
машины» → примени → открой цель: внизу «Похожее по тексту» с процентом и общим словом.
Verified: `similar-records.spec.mjs` 4/4.

### ✅ «До среды» — это среда, а не сегодня (2026-07-26)
Проб вечернего дампа показывал у ВСЕХ пяти задач один и тот же день — сегодня. Причина в разборе
дат: дни недели сравнивались по точному слову, поэтому ловились ровно два — «вторник» и
«четверг», у которых винительный падеж совпадает с именительным. «До среды», «в пятницу»,
«к субботе» не распознавались вовсе, и срок молча уезжал на сегодня: день владельца набивался
делами, которых он на сегодня не ставил.

Дни недели переведены на основы с явными кириллическими границами (`сред[аыуе]`, `пятниц[аыуе]`,
`суббот[аыуе]`, `понедельник[аиу]?`…). Границы обязательны: без них «среди недели» и «средство»
читались бы как среда. Добавлены «выходные» — ближайшая суббота, это то, что владелец имеет в
виду, говоря так в будни.

**Проверить за 60 секунд**: наговори «Надо ответить Дмитрию до среды» и «Позвонить маме в
выходные» → примени → у задач стоят среда и суббота, а не сегодняшнее число.
Verified: `weekday-deadline.spec.mjs` 4/4.

### ✅ Дело или событие, но не оба (2026-07-26)
Прямое следствие предыдущего пакета: как только «до среды» и «в выходные» стали настоящими
датами, стало видно, что при ЛЮБОЙ дате заводились два черновика — задача и блок дня с тем же
названием. Один смысл — два объекта (закон №4). Хуже того, оба попадали в один таймлайн дня и
объявлялись **конфликтом друг с другом**: система находила «пересечение» объекта с его же
двойником.

Правило простое и соответствует речи владельца: **названное ВРЕМЯ делает запись событием дня
(«завтра в 14:00 зал»), отсутствие времени — сроком дела («ответить до среды»)**.

Два дефекта заголовков заодно:
- **строчная буква после снятой команды.** «Надо ответить Дмитрию до среды» превращалось в
  «ответить Дмитрию до среды», и список дел выглядел сломанным;
- **величина вырезалась как сумма.** «Пробежать 10 км» превращалось в «Пробежать км» — та же
  чистка чисел, что уже была поправлена в заголовках привычек, но во второй функции.

**Проверить за 60 секунд**: «Надо ответить Дмитрию до среды» → одна задача на среду и НИ ОДНОГО
блока дня. «Завтра в 14:00 зал» → блок дня и НИ ОДНОЙ задачи-двойника.
Verified: `event-or-task.spec.mjs` 4/4.

### ✅ «Не разобрано» значит именно это (2026-07-27)
После предыдущих пакетов в вечернем дампе оставалось три «непонятых» записи — и ни одна такой
не была. **«Может вообще без машины обойтись»** — сомнение, которое система УЖЕ показывает рядом
с целью в противоречиях. **«Смета на кухню пришла»** и **«Счёт за воду пришёл»** — свершившиеся
факты: это уже случилось, и дела из них выдумывать нельзя («пришёл счёт» не значит «оплати
счёт»). Все три помечались как неудача разбора, хотя смысл каждой системе известен.

Добавлены два признака записи рядом с уже существующими «дело / наблюдение / не разобрано»:
`doubt` (детектор сомнений уже был — он кормит противоречия) и `fact` (пришло, получил, закончил,
сдал, прислал, подписал — при отсутствии обязательства). Обязательство сильнее: «Надо оплатить
счёт за воду» остаётся делом. Разбор дня называет обе группы отдельными строками.

**Итог на 22 записях вечернего дампа: неразобранных не осталось ни одной.** Каждая запись имеет
честно названный смысл — дело, событие, цель, привычка, деньги, наблюдение, позиция человека,
сомнение или факт.

**Проверить за 60 секунд**: наговори «Счёт за воду пришёл» → задачи НЕ появилось, а разбор дня
пишет «1 запись — свершившийся факт: дела из них не выдумываю».
Verified: `doubt-and-fact.spec.mjs` 4/4.

### ✅ Расписание агентов: время предлагает, а не делает (2026-07-27)
Единственный крупный пункт «не реализовано» из роадмапа, который закрывается нативно. Расписаний
у агентов не было вовсе, и в карточке честно стояло «Расписания в LifeOS нет — агент не
просыпается сам».

**Главное решение пакета не техническое, а продуктовое: что расписание вообще значит в системе,
где скрытые действия запрещены.** Канон и §7 не позволяют агенту проснуться и что-то сделать
самому — «в 21:00 сам разобрал день» это ровно то, что продукт запрещает. Поэтому расписание
означает одно: **когда время наступило и владелец открыл систему, агент ПРЕДЛАГАЕТ запуск.** До
подтверждения не меняется ничего, и спека проверяет это прямо — после наступления срока прогон
агента пуст.

Карточка так и написана: «Время 21:00 наступило — предлагаю запуск. Сам не запускаюсь: жду
кнопку.» Сравнение идёт по местному времени владельца, а не по UTC: расписание живёт в его дне.
Задание и снятие расписания — решения владельца, поэтому оба с чеком в Контроле (закон №3).

Новое поле `agentSchedules` прошло обе точки ловушки `normalizeState` — спека проверяет это
перезагрузкой страницы.

**Проверить за 60 секунд**: Системы → Агенты → у «Вечернего разбора» поставь время → «Сохранить
время». Заголовок карточки сменится на «По расписанию в 21:00. Сам не запустится: предложит.»,
а в Контроле появится чек.
Verified: `agent-schedule.spec.mjs` 5/5, регресс агентов 10/10.

### ✅ Псевдоним человека перестал теряться (2026-07-27)
`entity-resolution.spec` падала и до этой сессии — но это не среда, а настоящий дефект ровно
в той области, которую я переписывал. Панель «Люди» обещает «„Данил“ и „Даня“ я узнаю как одного
человека», а показывала только «Данил»: канонизация («Даня» → «Данил») происходит ДО группировки,
и в наборе форм оставалась одна каноническая — исходное написание исчезало.

Псевдонимы теперь собираются из всех написаний, включая исходные. Одна строка кода, но она
возвращает панели её обещание.
Verified: `entity-resolution.spec.mjs` 1/1 (падала на базовом коммите), регресс людей и графа
20/20.

### 📊 Полный набор e2e на границе фазы (2026-07-27)
`output/playwright` целиком: **208 passed, 9 skipped, 15 failed** (≈1 час одним воркером).
Пятнадцать падений — ровно те же, что на базовом коммите сессии `0ba483a`, все по среде облака:
голос (vosk, whisper.cpp), микрофон, ALAC через ffmpeg, скриншоты-доказательства, большой сейф,
undo/redo, мобильная навигация, H09, P19, F1.2, chat-brain, notion-grade-pack.
**Регрессов от сессии не осталось**, а `entity-resolution` из этого списка вычеркнута — починена.

### ✅ Дизайн по скриншотам: пять настоящих поломок (2026-07-27)
Оказалось, что визуальную проверку в облаке сделать МОЖНО: снять скриншоты Playwright и
посмотреть на них. Раньше я писал, что это только на ноутбуке — неверно. Прогнал 17 записей,
снял восемь экранов и нашёл то, чего не видит ни один e2e-тест.

1. **Главная кнопка Дома была невидима — белым по белому.** В «тихих часах» переменная
   `--accent` переопределялась **через саму себя**: по спецификации это цикл, значение становится
   пустым, а `.ui-btn-primary` рисуется как `background: var(--accent); color: #fff`. Ломалось
   ровно вечером — тогда, когда владелец разбирает день. Смешанное значение теперь кладётся под
   другим именем, а `--accent` переопределяется на детях контейнера: цикла нет, приглушение
   осталось.
2. **Экран «Агенты» был свалкой из 13 одинаковых карточек «Local organizer»** — служебный шаг
   «Открыть контекст в чате» заводил прогон агента на КАЖДЫЙ захват. Он добавлен в машинерию;
   осознанный запуск из чата не затронут (отличаем по `draftId`).
3. **Инсайты связывали несвязанное по служебным словам.** «Похоже, „Хочу купить машину“ и „Надо
   ответить Дмитрию“ об одном · общие темы: **suggested, actions**» — слова из шапки тела
   заметки, одинаковой у всех записей. Тот же срез, что уже стоял в поиске похожих, применён к
   «различающим терминам». Стало три связи, и все настоящие: «машину», «машины», «потратил».
4. **Строки задач вылезали за рамку узких колонок «Сегодня»** — кнопки «Главная / →Завтра /
   Объект / Изменить» обрезались краем колонки. В узкой колонке строка раскладывается
   по вертикали; на широких экранах раскладка прежняя.
5. **Панель «Сценарий» обрезалась** — сетки были жёстко на 3 и 5 столбцов в узкой колонке.
   Столбцы переносятся по доступной ширине.

Плюс грамматика карточки человека: «Марина встретился один раз» (род имени системе неизвестен —
это её же правило) и «Это один человек, а не 1 разных» при единственном написании.

**Проверить за 60 секунд**: Дом вечером — главная кнопка синяя и читаемая; Системы → Агенты —
три карточки, без «Local organizer»; Дом → Инсайты — связи по словам о жизни, не по «actions».
Verified: `visual-integrity.spec.mjs` 4/4 (невидимая кнопка · самоссылающиеся переменные ·
переполнение узких колонок · служебные слова в связях).

### ✅ Поток без «.md» и граф без двойников по порядку слов (2026-07-27)
Ещё два дефекта, найденные на скриншотах.

**В потоке каждая запись выглядела файлом.** «Марина согласна на август если без кредита**.md**» —
владелец наговорил мысль, а не создал файл; имя источника у текстового захвата синтетическое.
Теперь текстовый захват показывается своими словами, а подпись под ним не повторяет заголовок.
У настоящих файлов имя с расширением остаётся — там оно и есть имя.

**В темах графа одна денежная запись стояла дважды**: «Потратил 40000 продукты» и «Потратил
продукты 40000» — те же слова в другом порядке, поэтому проекции не схлопывались. Ключ группы
объекта жизни считается теперь по НАБОРУ предметных слов, а не по строке: порядок слов смысла
не несёт. Тот же ключ используется поиском похожих, иначе он не нашёл бы свою группу.

**Честное ограничение:** «Заработал 200000 зарплата» и «Доход 200000» — один захват, но разные
слова, и текстом их не свести. Схлопывать по общему источнику нельзя: один захват законно даёт
РАЗНЫЕ объекты жизни («отработал 12 часов, заработал 8700, бензин 1900» → смена + доход +
расход). Оставлено как есть и записано.
Verified: `graph-brain.spec.mjs` 6/6, `similar-records.spec.mjs` 4/4.

### ✅ Экран «Контроль» стал читаемым (2026-07-27)
Самый сломанный экран из восьми. Скриншот показал: страница **15 158 пикселей**, журнал изменений
сжат в 60-пиксельную колонку по восемь строк на запись, метки времени обрезаны краем, слева —
пустота на 60% ширины.

**Причина одна и простая:** заголовок панели тоже был элементом сетки и занимал ШИРОКУЮ колонку,
а журнал уезжал в боковую на 300px. Заголовок растянут на всю ширину — журнал встал в широкую
колонку, страница ужалась до **6 267 пикселей**.

Дальше — то, что экран обещает заголовком «понятный след без стены технических доказательств»:
- **время** «2026-07-27T05:42» → «27 июл 05:48»;
- **метки событий** по-русски: было «goal create», «finance transaction», «chat answer artifact»;
- **тексты событий** по-русски: было «Goal created:», «Finance income: … RUB», «Inbox text
  captured as artifact», «owner chat artifact recorded:», заголовок «Rollback snapshots»;
- **служебные шаги разбора больше не пишутся в журнал вовсе.** Они ничего не меняют, а три
  строки «объект не создавался» на каждый захват хоронили настоящие изменения владельца. Закон
  №3 требует видеть ЗАПИСИ, а не отсутствие записей; сколько служебных шагов прошло — считает
  разбор дня, и сам шаг виден в своём предложении со статусом «применено»;
- «Снимки отката**0**» → значение отделено от подписи.

**Проверить за 60 секунд**: Контроль — журнал в широкой колонке, каждая строка в одну-две строки,
время человеческое, ни одного английского слова, ни одной строки «объект не создавался».

### ✅ «Как рос граф» — движение вместо среза (2026-07-27)
Прямая просьба владельца: заходить каждый день и видеть, что база стала больше. Данные для этого
уже лежали — дата создания есть у каждого объекта, новой коллекции не понадобилось.

Столбик — **размер графа на тот день** (накопительный итог), число над ним — **приток именно за
этот день**. Пустой день остаётся пустым: сглаживать данные владельца нельзя. Считается по узлам
графа ЖИЗНИ, а не по всем записям хранилища — иначе рост показывал бы машинерию платформы.
Отчёт графа получил ту же мысль словами: «За 5 дней граф вырос на 25 объектов, сегодня
прибавилось 22».

**В первый день блок не показывается вовсе** — одной точки для динамики мало, а рисовать график
из одного дня значит обещать движение там, где его не было.

**Дефект, который вскрыл проб:** тестовый сдвиг дат брал первые заметки подряд, а первыми в
хранилище лежат СЛУЖЕБНЫЕ заметки платформы — они в граф жизни не входят, и сдвиг ничего не
менял. Хук теперь пропускает системные записи.

**Проверить за 60 секунд**: Граф → внизу «Как рос граф»: столбики по дням, над сегодняшним «+N»,
под графиком написано, из чего он посчитан.
Verified: `graph-growth.spec.mjs` 4/4 (в первый день блока нет · рост по дням с притоком ·
подпись о происхождении · строка в отчёте графа).

### ✅ Рост по теме: выбрал проект — видишь, как растёт он (2026-07-27)
Продолжение просьбы владельца: «мог выбрать проекты и видеть, как граф увеличивается». У каждой
темы графа появилась кнопка «Рост темы» — график сужается до её объектов, подпись называет тему,
кнопка «Показать весь граф» возвращает обзор. Фокус хранится **по хабу темы**, а не по её
порядковому номеру: номер сдвигается при пересчёте кластеров, хаб — это конкретный объект.

Выбор темы — фильтр ВЗГЛЯДА, а не изменение данных, поэтому чека он не оставляет: закон №3 про
записи, а не про то, куда владелец смотрит.

**Дефект, вскрытый пробом:** тема без истории ЗАПИРАЛА. Блок роста пропадал целиком — вместе
с кнопкой возврата, — и выйти из темы было нечем. Теперь при нехватке истории блок остаётся,
честно пишет «у темы пока один день истории — рост показывать не из чего» и даёт выход.

Плюс склонение: было «за 4 дней прибавилось». Склонение считает проекция, а не шаблон — шаблон
не должен решать языковые вопросы «на глаз».
Verified: `graph-growth.spec.mjs` 7/7 (первый день · рост по дням · происхождение · строка
в отчёте · сужение по теме · тема без истории не запирает · снятие фокуса).

### ✅ «Что именно выросло» — и граф жизни очистился от демо платформы (2026-07-27)
Клик по дню на графике роста раскрывает состав: какие объекты добавились именно в этот день,
каждый ведёт в свою карточку. Смена темы сбрасывает раскрытый день — состав считается ВНУТРИ
темы, иначе показывался бы состав от прошлой.

**Проб этой фичи вскрыл дефект глубже самой фичи.** Раскрытие дня показало «Builder-AI»,
«CRM / клиенты», «Smart-home dashboard», «Ollama local», «Travel» — то есть **демо-содержимое
платформы считалось жизнью владельца**. Заметки с `systemType` (`product_brain` — канон
разработки, `v34_platform` — демо-системы) проходили фильтр вида «заметка» и попадали в граф
жизни наравне с его записями. Тот же набор УЖЕ отсекался в поиске связей-инсайтов — в графе
жизни его не было.

Это меняло все числа сразу: размер графа, темы, похожие записи, важность в памяти, мосты.
На тех же данных граф стал **6 объектов вместо 25** — и это честное число.

Заодно убраны **осиротевшие призраки**: призрак — это вики-ссылка на ещё не созданную заметку,
и он объект жизни только пока на него кто-то ссылается. После исключения демо-заметок их
призраки («Control», «Builder») висели в графе сами по себе и попадали в раскрытие дня.

**Проверить за 60 секунд**: Граф → «Как рос граф» → клик по столбику: под графиком список того,
что добавилось в этот день, и ни одной записи платформы.
Verified: `graph-growth.spec.mjs` 9/9 (добавлены: состав дня ведёт в объект · демо-заметки
платформы не считаются объектами жизни).

### ✅ Разбор дня говорит, ГДЕ вырос граф (2026-07-27)
«Граф вырос на 6 объектов» — число без смысла, пока не сказано, где именно. Стадия связей
в разборе дня теперь заканчивается так: **«Граф жизни за сегодня вырос на 6 объектов, больше
всего тема „Заработал 200000 зарплата“ (+3)»**. Считается по тем же узлам и датам, что и график
на экране Графа: одно число не должно означать разное на разных поверхностях.

Заодно исправлено расхождение, которое эта строка вскрыла: счёт связей в разборе брался из
**технического** графа («Всего связей в графе: 55»), а рост — из графа **жизни** (6 объектов).
Рядом это читалось как противоречие: 55 связей между 6 объектами. Теперь и то и другое — граф
жизни: «Всего связей в графе жизни: 5».

**Проверить за 60 секунд**: Поток → «Разобрать день» → стадия «Строю связи»: там названо, на
сколько вырос граф жизни и какая тема выросла больше всех.
Verified: `graph-growth.spec.mjs` 10/10, `day-digest.spec.mjs` 2/2.

### ✅ Срезы: один артефакт — много представлений (2026-07-27)
Канон обещает «данные ≠ представление», но в продукте это оставалось словами: сохранённые
поиски существовали в хранилище **без единого элемента интерфейса** (`grep savedSearches ui/`
не находил ничего). Теперь в Базе есть **срез** — представление над теми же артефактами:
источник (Задачи · Цели · Привычки · Деньги · Наблюдения · Утверждения · Захваты), одно условие
(поле · операция · значение) и вид (список или таблица). Копии данных не создаётся: строка среза
ведёт в тот же объект, что и любой другой экран.

**Языка запросов нет намеренно.** У донора (Dataview) есть DataviewJS — произвольный JS в
заметке; это дыра и в приватность, и в предсказуемость. Здесь всё выбирается из закрытых
списков: сломать срез опечаткой невозможно, а прочитать его может человек, а не парсер.
Сохранённый срез получает человеческое имя («Деньги — вид равно доход»), чек в Контроле и
возврат по клику. Повторное сохранение того же среза (закон №4) обновляет, а не удваивает.

**Проба вскрыла ложь, которую все 10 тестов пропустили.** С выбранным «вид равно» и ещё пустым
значением браузер показывал выбранным первый пункт списка — «доход», подпись писала
«условию отвечают 2», а в таблице стояли и доходы, и расходы: экран обещал одно, показывал
другое. Причина в том, что пустое значение = «условие не задано», а `<select>` без пустого
пункта всегда показывает первый. Теперь условие считается заданным только когда оно **полное**
(поле + операция + значение), в списке значений первым идёт «любое», подпись говорит
«условие ещё не дописано — пока показаны все», а сохранить такое можно только как «— все».

**Проверить за 60 секунд**: База → панель «Срезы» → «Откуда: Деньги» → «Поле: вид» →
«Значение: доход» → «Таблица» → «Сохранить срез».
Verified: `lens-views.spec.mjs` 11/11 (в т.ч. спека на находку пробы), `npm run verify`.

### ✅ База отвечает своими числами, а не только цитатами (2026-07-27)
На вопрос «что там с машиной» система отвечала «тема встречается 5 раз» и показывала цитаты —
при том что ЗНАЛА больше: у темы есть цель со сроком, открытые задачи, движение денег и
противоречие, уже посчитанные на других экранах. Теперь над цитатами стоят **факты по
объектам**: «Цель «Купить машину»: срок 1 авг, осталось 5 дней», «Открытых задач по теме: 2»,
«Деньги по теме за 30 дней: расход 20 000 ₽». Каждый факт ведёт в свой объект — число можно
проверить, а не принять на веру. Нет данных для грани — грани нет, и конвейер говорит вслух:
«по объектам считать нечего: ни цели, ни задач, ни трат по теме».

**Проба вскрыла два дефекта глубже самой фичи, и оба жили давно.**

1. **Цитатная часть по-русски почти не работала.** При пяти записях ровно про машину ответ
   писал «дословных записей по теме нет». Минисёрч знает prefix и fuzzy 0.2, а «машиной»
   отличается от «машину» на две правки; прямой проход по захватам сравнивал подстрокой —
   тот самый случай из MEMORY.md, пятый по счёту. Теперь и цитаты, и факты сравниваются по
   ОСНОВАМ — тем же, что строят тематические рёбра графа: одна тема на всех экранах.

2. **Реранк по графу не запускался никогда.** Конвейер обещал «порядок — по близости в графе»,
   но якорь искали по id захвата, а узел графа жизни несёт id самой сильной проекции. Обещание
   не выполнялось ни разу. Узел ищется по ключу группы — ровно как это уже делает поиск похожих
   записей. Заодно разведены две разные причины молчания: «связей для перестановки мало» и
   «первая цитата ещё не связана в графе».

**Проверить за 60 секунд**: Дом → спросить «Что там с машиной?» → над цитатами три факта с
числами, каждый кликабельный; под цитатами — почему каждая на своём месте.
Verified: `answer-with-facts.spec.mjs` 9/9, регресс 9/9 (`question-stays-question`,
`memory-search`, `similar-records`), `npm run verify`, аудиты — те же 6 известных.

### ✅ Прогрессивное раскрытие панелей: 3155 → 2587 пикселей на Базе (2026-07-27)
Владелец назвал приоритетом 2: «много окон, вложенные стрелочки, кучно и непонятно, связи не
читаются глазом». Экран Базы шёл на **3155 пикселей** — три с половиной прокрутки. Появился
переиспользуемый сворачиваемый блок (`collapsiblePanel`) с управляемым состоянием, а не нативным
`<details>`: тот схлопывается на каждом ре-рендере — этот фикс в репозитории уже дважды сделан
для тулбара чата и настроек графа. Выбор запоминается и переживает перезагрузку.

**Два правила, второе выведено из собственной ошибки.**
1. В заголовке свёрнутого блока всегда стоит **счётчик** того, что внутри, и честный статус
   («нужны локальные эмбеддинги»). Свёрнуто ≠ спрятано: блок, который молчит о содержимом, —
   это скрытие данных, а §7 запрещает скрытое поведение и в показе тоже.
2. Сворачивается **только то, у чего есть собственное рабочее место** («Чтение», «Контроль») —
   на чужом экране это дубль. Первая попытка свернула ещё «Обратные ссылки» и «Семантический
   поиск», и обе спеки упали **по делу**: связи владелец как раз и просил читать глазом, а
   честный статус провайдера обязан быть виден без клика. Сворачивание усугубляло ровно ту
   жалобу, ради которой затевалось. Правило исправлено, тесты не ослаблены.

Сворачивание — это ВИД, а не данные: ни чека, ни записи в журнале изменений.

**Проверить за 60 секунд**: База → «Чтение» и «Контроль базы» свёрнуты, со счётчиками в
заголовке; клик раскрывает, перезагрузка помнит.
Verified: `panel-folding.spec.mjs` 6/6, регресс 2/2 (`backlinks-panel`, `semantic-search`),
`npm run verify`, аудиты — те же 6 известных.

## Ноутбук: то, что облако физически не могло проверить (2026-07-27)

Сессия по §2 файла передачи `docs/HANDOFF_LAPTOP_SESSION.md`. На ноутбуке есть микрофон,
русский системный голос (ru-RU), собранный whisper.cpp, Ollama с `qwen3:4b` и `gh` — всё, чего
не было в облаке. Правило прежнее: сначала замер, потом код; каждый пакет — живой прогон.

### ✅ 1. Список Базы читается: название и счётчик связей не слипаются
Живой прогон в браузере: строки списка «Заметки» имели высоту 36 px (глобальный `min-height` у
`button`) при содержимом 54–75 px — строки налезали друг на друга, а «N связей» читалось как часть
названия: «Новая запись 290 связей». Ни одна из 116 зелёных спек этого не видела. Раскладка взята
та же, что уже принята для списков памяти ниже по файлу.
**Проверить за 60 секунд**: База → список слева: название с многоточием, счётчик у правого края.
Verified: `visual-integrity.spec.mjs` 5/5 (новая V5 падает без фикса — проверено откатом CSS).

### ✅ 2. Голос: одна диктовка с тремя смыслами даёт три артефакта
Впервые прогнан весь круг по-настоящему: русская речь → локальный whisper.cpp → расшифровка →
разбор → предложения → подтверждение → артефакты и чеки. Фикстура `owner-voice-ru.wav` — не текст,
а настоящая речь; whisper вернул «Работаю сегодня с 16. Потратил 800 рублей на такси. Надо ответить
в Дмитрию до среды».

**Замер до правки:** три смысла склеивались в ОДНУ задачу с названием «Работаю с Потратил на такси
ответить в Дмитрию до среды». Это пункт №1 очереди `MEMORY.md` — резать по клаузам.

Три правки одного сценария: (1) `splitCaptureClauses` — короткая запись (≤600 знаков, ≤8
предложений) разбирается по предложениям, смысловые черновики берутся у клауз, сводка и служебные
шаги — у целого текста; черновик целого остаётся только если ни одна клауза не дала такого же типа,
поэтому разбор ничего не теряет; (2) предлог «с» в разборе времени («работаю с 16», «смена с 16 до
22») — раньше читались только «в» и «к», у смены не было времени вовсе, а голое число успевало
стать расходом 16 ₽; конца смены не выдумываем; (3) смена больше не порождает вдобавок дело
«Работаю с», которое нельзя выполнить.

**Стало:** смена 16:00 + расход 800 ₽ «Такси» + задача «Ответить Дмитрию до среды».
**Проверить за 60 секунд**: Аудио → «Проверить whisper.cpp» → расшифровать → предложения.
Verified: `voice-to-artifacts.spec.mjs` 1/1 (падает без правки), `voice-shift-pipeline` 1/1.

### ✅ 3. Два дефекта, которых облако не могло увидеть
**Пояс времени.** `new Date("2026-07-27T00:00:00")` — это МЕСТНАЯ полночь, а стоящий рядом
`getUTCDay()` читает её в UTC: в UTC+3 понедельник становится воскресеньем, и каждый срок по дню
недели уезжал на сутки вперёд («до среды» → четверг). В UTC (облако) ошибки нет вовсе. Три места
разбора дат теперь разбирают ключ с суффиксом `Z`. В помощнике `weekday-deadline.spec` была ТА ЖЕ
ошибка дважды, и в UTC+3 они гасили друг друга — верный ответ по неверному пути.

**Самоперезагрузка.** Приложение перезагружало себя через пару секунд после первого открытия:
первая установка service worker'а шлёт `controllerchange`, и он трактовался как «приехала новая
версия». Всё напечатанное исчезало вместе со страницей. У владельца воркер стоит давно — изо дня в
день не видно; видно при ПЕРВОМ знакомстве с продуктом и в каждом прогоне e2e на чистом профиле.
Именно поэтому спеки разбора падали не на разборе, а на пропавшем вводе. Теперь перезагружаемся
только при СМЕНЕ контроллера — исходный смысл фикса 2026-07-22 (владелец видит старую версию)
сохранён полностью.

**Замер на 13 спеках разбора:** база `f13d649` — 20 падений, после правок — 1 (и та в одиночном
прогоне зелёная). Verified: `first-open-no-reload.spec.mjs` 1/1 (падает без правки — страница
навигирует дважды), `weekday-deadline` 4/4, `task-vs-habit` 6/6, `observation-not-task` 4/4.
Аудиты: 27 из 28 (последний требует ветку `owner-rescue` и чистое дерево — не регресс; шести
«вечно падающих» на ноутбуке больше нет, им не хватало окружения).

## Сценарий владельца доведён до конца, и по дороге найдена потеря данных (2026-07-28)

Владелец скинул две голосовые с телефона и не получил ничего: файлы прикреплялись молча, кнопка
«Разобрать» на них не реагировала, расшифровка отвечала «файл не сохранён локально» про файл,
лежащий у него на диске, а в карточке объекта не было самого объекта. Разобрано по одному.

### ✅ Голос доходит до конца (шесть дефектов одним срезом)
Прикрепление теперь видно строкой на файл (имя, размер, честный статус из самой записи).
«Разобрать» при пустом поле либо запускает расшифровку, либо прямо называет, чего не хватает, и
ведёт на экран «Аудио» — молчания больше нет. Потолок 8 МБ снят: байты крупных записей лежат
блобом в IndexedDB (тот же стор, префикс `media:`), формулировка про «не сохранён локально»
исправлена — она читалась как «файла нет на твоём ноутбуке». В карточке объекта исходник показан
первым: плеер и расшифровка. Глобальный поиск перестал терять фокус на каждой букве (перерисовка
меняет весь DOM, фокус возвращали ровно одному полю). Сжатие снимка перестало сдаваться по
жёсткому сроку 1,8 с — бюджет считается от объёма, иначе снимок падал в сырой JSON, рос, и
следующее сжатие срывалось раньше: спираль, которую владелец видел как экран отказа.
Verified: `voice-attachment-flow` 4/4, `object-shows-itself` 1/1 — все падают без правок.

### 🔴 Потеря данных на живом хранилище — и защита от повторения
Загрузка снимка сорвалась (падение по `beforeinstallprompt`: `state.control.capabilities`
отсутствовал), приложение поднялось на пустом состоянии, и первое же автосохранение затёрло
реальные чанки. Две голосовые и всё, что из них выросло, восстановить было нечем.
Закрыто с двух сторон: причина (защищённый доступ `capabilityStore`) и защита от ЛЮБОЙ будущей
причины — в META снимка пишется число записей владельца, и сохранение с нулём поверх ненулевого
отвергается. Экран отказа перестал глотать стек.
**Урок дороже фикса:** оракул был зелёным на 100% (249 спек), пока приложение у владельца не
запускалось вовсе — событие `beforeinstallprompt` в headless не бывает.
Verified: `vault-never-erased.spec.mjs` 1/1.

### ✅ Файл с телефона доходит до текста
Путь `.m4a` проверен целиком на настоящей речи и настоящем whisper.cpp
(`m4a-voice-end-to-end.spec.mjs`). Мост ffmpeg приводит звук сразу к 16 кГц моно — телефонная
запись возвращалась в 5,5 раза тяжелее нужного. Дедуп крупных записей заработал впервые: сумма
считается по байтам файла, раньше она была пустой и одна запись копилась семью копиями.
Флаг «сохранено» больше не обгоняет запись байтов.

### ✅ Вторичное меню «Ещё» не закрывается под рукой
Было родным `<details>` — схлопывалось на каждой перерисовке, то есть после любого действия.
Экраны «Аудио», «Чтение», «Чат» живут там, поэтому путь к ним рвался. Раскрытие живёт в
состоянии. Побочно починилась `reader-position-bookmarks` — падала на невидимой кнопке.
Verified: `nav-more-stays-open` 1/1, регресс 13/13 по экранам через это меню.

**Открыто и не спрятано:** загрузка приложения на непустом хранилище занимает 10–16 секунд;
полный набор — 249 прошло, 20 упало (из них undo-redo, vosk, загрузка модели Whisper, mobile-pwa
известны как средовые, остальные требуют разбора по одному).

### ✅ Одна запись — один смысл (2026-07-28)
Владелец расшифровал одну голосовую и получил **12 объектов, 11 из которых служебные**: две
заметки на файл, четыре «вывода» (три — строки шапки «Источник:» и «Режим: локальный
whisper.cpp»), выдуманный вопрос про имя файла, четыре пункта повторения про файл, цитата во
весь текст и два инсайта про работу самой системы. Плюс на Доме — ложное «похоже, эти две
записи об одном» между записями про совершенно разное.

**Стало: 1 заметка + 1 отметка времени.** Смысл живёт в типизированных предложениях (смена
16:00, расход 800 ₽, задача со сроком), а мысль без своего объекта — наблюдение о себе — доходит
предложением-инсайтом. Знание берётся из слов владельца, а не из тела заметки со служебной
шапкой; фраза, ставшая объектом, не дублируется «выводом»; вопрос не выдумывается; надиктовка не
цитируется сама из себя.

Ложные связи убраны с двух сторон: обезличивание шапки теперь покрывает русские ключи
(`Источник:`, `Режим:`), а слово «Расшифровка» из названия каждой расшифровки ушло в стоп-лист
тем. Настоящая общая тема при этом сохраняется — проверено отдельным тестом.

**Проверить за 60 секунд**: закинь две разные голосовые → в Базе по одной заметке на каждую, в
«Выводах» нет строк «Источник»/«Режим», на Доме нет «похоже, об одном».
Verified: `one-record-one-meaning.spec.mjs` 4/4, регресс 18/18 (связи, похожие записи, граф,
служебные шаги, поиск) + 27/27 (знание, разбор дня, наблюдения, вечерний дамп, ответы фактами).

### ✅ Меню перестало врать (2026-07-28)
Обход всех экранов показал, из чего складывается «сайт перегружен мусором»: **61 кнопка
перехода** при контракте в 9 пунктов, рабочие разделы вперемешку с недописанными каркасами
одинаковыми строками, и **два пункта на один и тот же экран** («Цели» и «Привычки» — 1097 знаков
и 6 кнопок у обоих).

Сделано по канону `docs/design/LIFEOS_DESIGN_CANON.md` §9, где список черновиков зафиксирован по
итогам UX-аудита владельца: во вторичном меню сначала рабочее, затем подпись «🚧 В разработке» и
под ней каркасы; сам каркас признаётся в первой строке экрана; «Цели» и «Привычки» — один пункт.
Девять пунктов главного меню не тронуты.

**Проверить за 60 секунд**: «Ещё» → сверху рабочие разделы, ниже подпись «В разработке» и
каркасы; зайти в «Двойник» — первая строка честно говорит, что сценарий ещё не собран.
Verified: `honest-navigation.spec.mjs` 3/3, регресс 12/14 (обе оставшиеся — офлайн-PWA, известное
средовое падение). Спека мобильной навигации обновлена вместе с продуктом, а не ослаблена.

### ✅ П1 · Речь стала объектами, а не кусками фразы (2026-07-28)
Владелец надиктовал «поработал с 8 до 11, 3 часа… 316 после налога пришло… потом ещё 1700,
получается 4700 всего… еду к Володе… надо в аптеку» — и получил **задачу с названием во весь
экран, расход 316 ₽ и ещё одну задачу**. Заработок числился тратой, смена — делом, ставки не
было вовсе. Замерено на живой сборке до правки, не по памяти.

Причина не в словаре: **смысл надиктовки живёт МЕЖДУ предложениями**. Часы названы в первом,
деньги в третьем, а ставка не названа вовсе — она считается из двух. Этап A (паузы говорящего)
резал речь правильно, но каждый кусок дальше шёл через словарные правила в одиночку.

**Стало (RR-001 Этап B, слой намерений):** смена 08:00–11:00 · доход 4700 ₽ · ставка 1567 ₽/час
(помечена как посчитанная, не услышанная) · встреча «Еду к Володе» · дело «В аптеку зайти».
Пять типизированных объектов, ни одного «Вывод: <кусок той же фразы>».

Как устроено:
- **Схема + валидатор** — чистая логика, проверяется без модели. Намерение без ДОСЛОВНОЙ цитаты
  из слов владельца не проходит ни при какой уверенности источника (И-6). Это же правило —
  единственная защита от выдумки модели.
- **Правила поверх всей надиктовки**, а не по кусочку: слово-итог («получается 4700 всего»)
  смотрит вперёд, части сворачиваются в него и живут полем `parts`, а не тремя записями.
- **Слой модели за флагом** (`ollama.speechIntents`, по умолчанию выключен): та же схема, тот же
  валидатор, текст владельца идёт в промпт только размеченным блоком как ДАННЫЕ (граница
  доверия 3.1d). Нет демона — честный статус `provider_unavailable`/`not-connected`, никогда
  имитация.

**Замеры, купленные болью (не переизмерять):**
- Настоящий whisper.cpp `ggml-small` на русской речи отдаёт числа **цифрами** в 7 фразах из 8
  («16», «800», «8700», «1900», «2500»); словами — только «тысячу двести» после глагола.
  Поэтому разбор числительных прописью в правила не заводился: он покрыл бы 1 случай из 8.
- Локальная модель здесь **слабее правил**: `qwen2.5:3b` — 44 секунды на одну надиктовку и
  частично сломанный JSON, «доход» угадан по части (316), а не по итогу (4700); `qwen3:4b` —
  99 секунд и пустой ответ. Отсюда контракт: правила первичны, модель только ДОБАВЛЯЕТ
  непонятое. Слияние сравнивает не цитаты, а объяснённые фразы — иначе включение модели
  воскрешало бы ровно тот дефект, из-за которого пакет и делался.

**Проверить за 60 секунд**: надиктуй «поработал с 8 до 11, 3 часа… 316 после налога пришло…
потом ещё 1700, получается 4700 всего… еду к Володе… надо в аптеку» → в предложениях смена с
часами, ОДИН доход 4700, ставка за час, встреча и дело; расхода 316 ₽ нет, «Выводов» с кусками
той же фразы нет.

Verified: `raw-speech-becomes-objects.spec.mjs` 5/5 (включая живой Ollama). Спека доказана
красной: с выключенным слоем намерений падает 2 из 5 — на смене и на её доходе до предложений.
Аудиты 27/28; единственный красный — `audit-owner-rescue-final` по пункту «рабочее дерево
чисто», он зелёный только после коммита (И-9: без слова владельца не коммичу).

**Открыто и не спрятано:**
- `one-record-one-meaning.spec.mjs` — плавает импорт аудио в наборе (счётчик записей не растёт
  за 30 с). Поштучно тест зелёный за 15 с, в наборе падает то :125, то :140 — то есть это
  бюджет ожидания/холодный старт, а не дефект разбора. Причина не найдена до конца.
- `money-fast-capture.spec.mjs` был красным ДО этого пакета: ходил в пункт меню «Привычки»,
  который прошлый коммит намеренно слил с «Целями». Спека переведена на `goals` вслед за
  продуктом, утверждения не тронуты (И-8).
- Разбор числительных прописью не делался осознанно — см. замер выше.

### ✅ П2 · Честная пустота: подпись только над данными (2026-07-28)
Владелец открыл Базу и увидел **три заголовка с обещаниями и ноль данных**: «Выводы — инсайты
появятся из чтения, аудио и заметок», «Вопросы — вопросы станут задачами без потери источника»,
«Повторение — карточка появится после извлечения смысла». Панель выглядит наполненной, а
сообщить ей нечего — интерфейс занимает экран разговором о себе.

Это бутафория **формой**, а не словом, поэтому старый `audit-no-label-theater` её не видел: он
искал запрещённые термины («Product Brain», «provider gate»), а тут все слова русские и
приветливые.

**Правило:** нет данных — нет раздела. Пусто всё — ОДНА честная строка на панель, а не по
обещанию на каждый несуществующий раздел. Появились данные — появилась подпись. Формы при этом
остаются всегда: форма — это действие, а не подпись, иначе завести первую цель было бы негде.

Помощник `dataSection()` + `sectionStack()` в `ui/components/shared.js`; переведены **12 мест в
8 файлах**: База (Выводы · Вопросы · Повторение · Заметки), Сегодня (Цели · По времени · Без
времени · Привычки · Напоминания · План дня · Записи систем), Календарь (Без времени · Даты
систем), Деньги (Категории · Подписки · Операции · Счета), Цели и привычки (Чек-ин · Цели),
Сценарии (История), Чтение (Выделения).

Аудит расширен на сам приём: заголовок вплотную перед списком с запасным текстом на случай
пустоты → красный. Он же и нашёл последние три места, которые я пропустил при обходе руками
(`MoneyDashboard.js` ×2, `ReaderSurface.js`) — то есть работает не как формальность.

**Проверить за 60 секунд**: чистое хранилище → «База» → в правой колонке одна строка «Смысл ещё
не извлечён», а не три заголовка с обещаниями; добавь вывод — появляется подпись «Выводы» и
только она.

Verified: `honest-emptiness.spec.mjs` 2/2, доказана красной на старом UI (2 из 2 падают).
Аудиты 27/28 (`audit-no-label-theater` зелёный уже с новой проверкой).

**Открыто и не спрятано:** `finance-deep.spec.mjs:76` (прогноз регулярных платежей) красный и
на чистом HEAD — проверено стешем. Тест зависит от даты: 28 июля до конца месяца остаётся три
дня, и прогноз выходит нулевым. Не трогал: это отдельная боль, у неё будет свой пакет.

### ✅ П5 · Обратный путь: из вывода два клика до места в записи (2026-07-28)
Провенанс в данных был всегда: у вывода есть цитата, у цитаты — источник. **Навигации не было.**
Владелец видел утверждение и не мог проверить, откуда оно взялось: карточка показывала полную
расшифровку целиком, а искать в ней ту самую фразу — его работа. Именно проверяемость отличает
второй мозг от генератора уверенных фраз.

**Стало:** открыл объект (клик 1) → на «Сути» видна ТА САМАЯ фраза со своим временем → «Слушать
с 0:02» (клик 2) → запись играет с этого места.

Три вещи, без которых это было бы враньём:
- **Секунда настоящая, а не посчитанная по длине строки.** whisper.cpp отдаёт её сам, если
  просить `verbose_json` вместо `json` (замер 2026-07-28: у каждого сегмента есть `start`/`end`
  с точностью до сотых). Сегменты расшифровки теперь хранят `startSeconds`.
- **Провенанс переживает применение.** Цитата была у предложения и терялась при подтверждении —
  у самого расхода её уже не было. Обратный путь обрывался ровно там, где владелец хочет
  проверить: на объекте, который он видит в списке через неделю. `applyProposal` переносит
  цитату и источник на созданный объект (И-6 требует ссылку НА ЗАПИСИ).
- **Нет времени — нет кнопки.** У ручной расшифровки без таймкодов времени не существует, и
  «слушать с 0:00» выглядит как ответ, ответом не являясь. Вместо кнопки — честная подпись
  «время в записи неизвестно: расшифровка пришла без таймкодов».

**Проверить за 60 секунд**: расшифруй голосовую через whisper.cpp → подтверди любой расход →
открой его карточку → под плеером стоит именно та фраза и кнопка «Слушать с M:SS» → нажми:
запись играет с этой секунды, а не с начала.

Verified: `back-to-the-source.spec.mjs` 3/3 (настоящий проигрываемый WAV 16 кГц, не заглушка —
перемотку нельзя проверить на записи, которой нет). Регресс П1+П2: 55/55. Аудиты 27/28
(красный прежний — «рабочее дерево чисто», зелёный только после коммита).

### ✅ П3 и П4 · Проверено, а не переделано (2026-07-28)
Перед работой сверился с текущим файлом, а не с планом. Оба пакета оказались закрыты прошлой
сессией, и их контракты держатся спеками — переделывать было нечего:

- **П3 (байты вне состояния):** `voice-attachment-flow.spec.mjs` проверяет ровно те четыре
  пункта, что записаны в задании: `mediaStored === true`, `dataUrl.length === 0`,
  `resolveSourceMediaUrlForTest` возвращает `blob:`, чип содержит «сохранено». 5/5 зелёных.
- **П4 (карточка показывает сам объект):** `object-shows-itself.spec.mjs` проверяет плеер и
  расшифровку внутри карточки, а не только заголовки. 1/1 зелёный.

Сэкономлено примерно два пакета работы. Правило подтвердилось: **сверяться с файлом, а не с
планом** — план перестаёт быть верным в момент, когда в репозитории работает вторая сессия.

### 📏 П6 (замер) · Мёртвого кода в 5,5 раза больше, чем считал план (2026-07-28)
`tools/audit-dead-code.mjs` — граф достижимости от точек входа, а не перепись глазами. Мёртвая
функция не выглядит мёртвой: она выглядит как обычный код, и её читают.

**Замер против плана.** v1.5 §4 (пакет 2.1) говорит: «недостижимо 15 функций / 704 строки =
2,7%… мёртвого слоя в app.js нет, файл большой потому что живой». Инструмент считает иначе:

| | план (перепись 27.07) | замер 28.07 |
|---|---|---|
| недостижимых функций | 15 | **145** из 847 |
| строк | 704 | **3 887** |
| доля файла | 2,7% | ~14% |

Крупнейшие совпадают (`renderEditor` 252, `buildLocalChatAnswerLegacy` 85,
`parseTimeFromTextHuman` 69) — то есть прошлая перепись видела верхушку, а не слой. Вывод плана
«файл большой, потому что живой» держится: 14% это не половина. Но экономия от П6 в пять раз
больше заявленной, и это меняет порядок работ внутри волны 2.

**Находка важнее чисел: 20 мёртвых функций (714 строк) ЗАКРЕПЛЕНЫ ГЕЙТАМИ.** `smoke.mjs`
проверяет наличие 86 строк-маркеров в `app.js`, и часть из них указывает на render-функции,
которые браузер не исполняет ни разу: `renderOwnerHome` (159 строк), `renderCommandCenter`,
`renderCommandCenterV5`, `renderInboxReviewWorkspace`, `renderDataControlPanel`,
`renderChatWorkspace`… **Гейт зелёный, а экрана нет** — смок говорит «интерфейс на месте»,
проверяя функцию, которая не вызывается. Это тот же класс дефекта, что уже записан в этом
леджере: агент правил разметку, которой на экране не было.

Аудит поэтому делит находки надвое: **125 функций / 3 173 строки** удаляются свободно, а
**20 функций / 714 строк** — решение владельца, потому что удаление тянет за собой правку
`smoke.mjs`, а П25 запрещает пакету трогать гейты. **Само удаление не сделано**: полный набор
спек шёл в момент замера, а правка `app.js` на живом сервере испортила бы результаты барьера.
Планка записана в `docs/qc/dead-code-baseline.json` — аудит краснеет, если мёртвого кода станет
больше, и снижается только явным `--write-baseline` после удаления.

### ✅ П8 · Брифы контекста: подсистема за 5 КБ вместо 1,6 МБ (2026-07-28)
Планировщику незачем читать репозиторий, чтобы спроектировать пакет — ему нужен **честный
конспект подсистемы**. Написаны шесть брифов в `docs/context/`:
`capture-analyzer` · `storage-kernel` · `proposal-engine` · `render-surfaces` · `providers` ·
`graph-engine`.

**Замер (это и есть DoD):**

| подсистема | бриф | код, который он заменяет | дешевле |
|---|---|---|---|
| capture-analyzer | 7,9 КБ | 1 617 КБ | **203×** |
| graph-engine | 3,6 КБ | 1 615 КБ | 454× |
| proposal-engine | 4,2 КБ | 1 646 КБ | 394× |
| providers | 4,8 КБ | 1 604 КБ | 337× |
| render-surfaces | 5,4 КБ | 397 КБ | 74× |
| storage-kernel | 4,9 КБ | 1 597 КБ | 329× |
| **всего** | **30,7 КБ** | **8,3 МБ** | **276×** |

Брифы **короче**, чем просил план (2–4 тыс. слов на бриф; вышло 500–1140 слов). Добивать до
объёма водой не стал: бриф ценен плотностью, а DoD — «подсистема за 3к слов» — перевыполнен с
запасом. Если планировщику окажется мало, дописывать надо по конкретной нехватке, а не по счёту
слов.

Что в каждом брифе обязательно есть: что входит/выходит, **правила, купленные болью** (со
следом настоящей ошибки на экране, чтобы их не «упростили» обратно), границы подсистемы, список
целевых спек и **открытые долги**.

`tools/audit-context-freshness.mjs`: бриф старше кода, который он описывает → красный. Возраст
берётся из git (`git log -1 --format=%ct`), а не из mtime — mtime врёт после любого клона и
checkout; незакоммиченная правка считается новее любого коммита, иначе бриф был бы «свежим»
ровно в момент, когда код при нём меняют. Проверено в обе стороны: `touch app.js` — красный,
обновил бриф — зелёный.

### ✅ П9 и П12 · Донорский конвейер: запрос по проблеме вместо чтения каталога (2026-07-28)
Каталог доноров (164 строки, 15 проектов) полезен ровно до того момента, когда нужен ответ
СЕЙЧАС: чтобы им воспользоваться, надо прочитать его целиком и заново решить то, что уже решено.

**П9 — донорские брифы по ПРОБЛЕМАМ, а не по проектам.** Проблема — то, чем задача приходит
(«что делать со старым фактом, когда пришёл новый»), а не то, как называется репозиторий.
Написаны три: `memory-decay` (вытеснение памяти) · `importance-ranking` (что показать наверху) ·
`answer-with-sources` (ответ с источниками). В каждом: ситуация владельца, что делают зрелые
доноры и в чём они СХОДЯТСЯ, как это ложится на наш стек, **чего НЕ брать** (с причиной) и чем
проверять при внедрении.

**П12 — `tools/donor-lookup.mjs`.** Запрос словами задачи:

```bash
node tools/donor-lookup.mjs вытеснение памяти
```

→ три абзаца готового разбора вместо 1150 строк. Поиск идёт по проблеме, её синонимам и телу
брифа. Ничего не нашлось — так и сказано: «разбора нет, значит проблему ещё не разбирали», и
дальше цикл RDD. Выдумывать ответ нельзя — правило то же, что и для продукта.

**П10–П11 — `tools/audit-donor-links.mjs`**, сверка в обе стороны:
- маркер `@donor Донор:Функция` в коде обязан существовать в каталоге (иначе ссылка врёт);
- строка каталога «✔ уже есть» утверждает, что функция у нас реализована — без маркера в коде
  это утверждение ничем не держится;
- маркер на строку, ставшую «⛔ вне стека», помечается устаревшим (П11).

Аудит сразу поймал **девять неточных ссылок в моих же брифах** (я писал имена функций по
смыслу, а в каталоге они называются иначе) — сведены с каталогом дословно. Это и есть проверка
того, что инструмент не формальность.

**Честный долг:** маркеров `@donor` в коде пока **0**, а каталог заявляет «✔ уже есть» в
**58 строках**. Расставить маркеры — правка `app.js` и `ui/**`, а в момент работы шёл полный
набор спек на живом сервере. Планка записана в `docs/qc/donor-links-baseline.json`: аудит
краснеет, если маркеров станет меньше или непокрытых утверждений больше.

### ✅ П23–П25 · Фабрика: права, гейт по уровням, защита от подделки (2026-07-28)

**П25 · Анти-подделка — `tools/audit-no-gate-tampering.mjs`.** Самый дешёвый способ получить
зелёный прогон — ослабить проверку; он же самый разрушительный, потому что после него оракул
врёт молча. Инвариант И-8 перестал быть доброй волей: аудит читает дифф и различает три случая —
**новая спека** (это работа пакета), **дополненный гейт** (проверок стало больше), **удалённое
утверждение** (подделка, пакет отклонён независимо от цвета гейтов). У `smoke.mjs` правило
строже: он список маркеров, там утверждение выглядит обычной строкой, поэтому удаление любой
содержательной строки считается ослаблением.

Проверен отрицательным тестом: убрал один маркер из `smoke.mjs` → красный с указанием строки;
восстановил → зелёный. На собственном диффе сессии честно показывает: 7 новых гейтов (работа),
2 дополненных (`money-fast-capture` +3/−1 навигация, `audit-no-label-theater` +24/−0).

**П24 · Трёхуровневый гейт — `tools/gate.mjs`.** `quick` (на пакет) · `wave` (на волну) ·
`full` (на барьер) · `map`. Карта «файл → спеки» **не ведётся руками и потому не устаревает
молча**: она выводится из брифов подсистем (`covers:` + названные спеки), а свежесть самих
брифов держит `audit-context-freshness`.

Что показала карта: **129 спек, из них 57 привязаны к подсистемам, 72 — ни к одной**. На быстром
гейте эти 72 не гоняются, и это названо прямо в выводе `map`, а не спрятано.

**П23 · Права — `docs/context/governance.brief.md`.** Полстраницы: кто что имеет право писать
(владелец · петля · планировщик · исполнитель · параллельная сессия · диспетчер) и чем каждое
право проверяется. Плюс раздел «что делать при конфликте прав» с тремя настоящими случаями этой
сессии, включая нерешённый: **`smoke.mjs` держит 20 мёртвых функций, и их удаление выходит за
право исполнителя — это решение владельца.**

### ✅ П32 · Меню уплотнено: 16 плоских строк стали 4 группами (2026-07-28)
Владелец: «слева всё намешано, сайт перегружен мусором». Прошлый пакет разделил меню по
честности (рабочее / «🚧 В разработке»), но строк осталось столько же — **шестнадцать подряд**.
Это не меню, а список: глазу не за что зацепиться, и его перечитывают целиком каждый раз.

**Стало четыре группы ПО ДЕЛУ**, а не по алфавиту: **Ввод и разбор** (Входящие · Аудио ·
Чтение · Экран) · **Дела** (Цели и привычки · Проекты) · **ИИ** (Чат · Сценарии · Подключения ·
Модели · Двойник) · **Мастерская** (Паки · Конструктор · Таблицы · Дизайн · Умный дом).
Раскрыта максимум одна: открылась другая — прежняя закрылась сама. Провалился в «Аудио» —
открыт «Ввод и разбор», и видно, где ты. **Девять главных пунктов не тронуты.**

Три решения, каждое против конкретной лжи интерфейса:
- **Каркас признаётся в своей строке** («Экран · в разработке»), а не ссылается в отдельную
  кучу. Суть канона §9 не ослабла, а усилилась: раньше признание стояло один раз на границе,
  теперь — на каждом каркасе в каждой группе.
- **Счётчик у группы считает РАБОЧИЕ разделы.** Свёрнутая группа не прячет, что за ней что-то
  есть, и не обещает больше, чем в ней работает. Ноль рабочих — пишем «в разработке», а не «0».
- **Мобильное меню перестало перечислять всё третий раз.** Было: нижняя панель, потом боковая,
  потом плоский полный список в «Ещё». Стало то же дерево, что и на большом экране.

**Проверить за 60 секунд**: «Ещё» → четыре группы вместо шестнадцати строк; открой «ИИ» →
«Ввод и разбор» закрылся сам; у «Мастерской» вместо счётчика написано «в разработке».

Verified: `honest-navigation.spec.mjs` 4/4. Спека изменена **вслед за намеренным изменением
продукта** и стала строже: правило «рабочее выше каркасов» проверяется теперь во ВСЕХ четырёх
группах сразу, а не на одной границе, и каждый каркас обязан признаться в своей строке.
Дубль `data-testid` между боковым и мобильным меню разведён префиксом.

### ✅ Дом перестал врать о том, что понял (2026-07-28)
Прогнал надиктовку владельца через ЖИВОЕ приложение и снял экран — то, чего не показывает ни
один зелёный тест. Разбор был прав, а Дом врал:

| что на экране | что на самом деле |
|---|---|
| «Доход: **не распознан**» | доход 4700 ₽ лежал предложением рядом |
| «Расходов не найдено» | расходов и не было — строка ради строки |
| «Люди: **Еду**, Володе» | «Еду» — глагол, а не человек |
| «**11** предложений ждут решения» | настоящих решений пять |

Четыре причины, каждая своя:

1. **Панель «LifeOS понял» читала только предложение смены.** После RR-001 доход живёт отдельным
   предложением, а не полем внутри смены — и панель писала «не распознан» ровно в тот момент,
   когда рядом лежало «Доход 4700 ₽». Теперь она смотрит на ВЕСЬ разбор: смена с окном времени,
   доход с частями («из 316 + 1700»), ставка с пометкой «посчитано, не услышано», встреча, дело.
   Появилось «Записать всё из этой записи» — иначе владелец жмёт «Записать смену», а доход и
   встреча остаются висеть.
2. **«Еду» стало человеком.** Формой это не отсечь: окончание «-у» в начале фразы принадлежит и
   глаголу («еду», «иду»), и имени в винительном («Марину», «Анну»), а терять имена ради
   глаголов нельзя. Точечный список глаголов, с которых человек начинает фразу вслух.
3. **Служебные шаги стояли в очереди решений.** «Сохранить источник в библиотеку», «Записать
   связи и контроль», «Открыть контекст в чате» решения не требуют — они описывают, что делает
   система. Введён `isOwnerDecisionProposal`: шаги никуда не делись (видны в Контроле со своим
   статусом), но очередь владельца больше не раздувают.
4. **Одна голосовая давала ДВА одинаковых «Смена 08:00–11:00»**: второе приходило из чата, где у
   предложения не было источника, и дедуп не срабатывал. Плюс «Добавить транскрипт» предлагалось
   под уже готовой расшифровкой — дважды.

**11 → 5.** Ровно пять смыслов, которые он надиктовал.

**Проверить за 60 секунд**: расшифруй голосовую про смену → на Доме «5 предложений ждут
решения», в панели справа доход с частями и ставка, в людях только Володя.

**Доработка П32 по факту прогона (не по плану — по падению):**
- Первая раскладка прятала РАБОЧИЕ разделы за раскрытие кластера. Прогон показал цену:
  76 спек ходят в разделы через это меню, а владелец получил бы два клика вместо одного до
  «Чата» и «Аудио». Переделано: рабочее видно сразу, за строку «+N в разработке» уходят только
  каркасы. Уплотнение не должно оплачиваться тем, что до всего стало дальше.
- На мобильном каркасы разворачиваются всегда: лист «Ещё» и так открывают намеренно и
  прокручивают целиком. Свёрнутые там, они переставали быть достижимыми — это поймала
  `mobile-pwa-continuity` («mobile navigation reaches every v34 surface»), и она права.
- `audit-primary-ui-language` покраснел на переменной с именем `ready`: аудит сканирует
  исходник целиком, включая имена. Переименовал переменную, аудит не трогал.

Verified: `honest-navigation` 4/4, `mobile-pwa-continuity` 1/2 (вторая — офлайн-PWA, известное
средовое падение), регресс по навигации и разбору 22/24.

### 📊 БАРЬЕР 1 · Полный набор: 264 прошло / 34 упало / 7 пропущено (2,6 часа)
Прошлый замер был 249/20 на `202956f`. Спек с тех пор прибавилось, прошло больше.

**Число НЕ полностью достоверно, и я это не прячу:** последняя треть прогона шла в тот момент,
когда я правил `ui/shell.js` под П32 — сервер отдаёт файлы живьём. Всё, что ходит в разделы
через вторичное меню (`kb-smoke`, `palette-search-polish`, `notion-grade-pack`,
`presentation-runtime`, `system-factory`, `ten-starter-packs`, `flow-execution`,
`twin-recovery-drill`, `semantic-search`, `honest-navigation`), падало из-за незаконченной
правки, а не из-за продукта. Набор перезапущен начисто после закрытия П32.

Известные средовые падения (были красными и до сессии): `whisper-transcribe` и
`voice-loop-vosk` (скачивание моделей с HuggingFace), `waveform-record` (getUserMedia),
`ollama-real-daemon`, `mobile-pwa-continuity` (офлайн-PWA), `undo-redo` ×3, `m4a-voice-end-to-end`,
`finance-deep` (зависит от даты).

### ✅ П13 · У0 и П14–П16 · О1–О3: система начала помнить твои решения (2026-07-28)
Волна 3, скрытый слой: **ни одного нового экрана**.

**Было:** решения владельца не сохранялись никуда, кроме строки в аудите. Слово `dismissed`
встречалось в репозитории дважды и не читалось никем. Значит система не могла знать, что
предлагает хорошо, а что плохо, — и «улучшаться» ей было не на чем. При этом уверенности она
показывала уверенно: 0.7, 0.72, 0.84. Это не знание, а привычка автора.

**Стало:**
- **О1 · Журнал решений.** Каждое применение и каждый отказ пишутся с признаками: тип, кто
  породил (правило или намерение из речи), время суток, заявленная уверенность, **правил ли
  владелец текст перед принятием** — самый честный сигнал качества. Провенанс обязателен (И-6):
  без предложения запись не заводится. Служебные шаги системы решением не считаются.
- **О2 · Ретро-заполнение.** Журнал начинается пустым, но решения БЫЛИ — они лежат в аудите с
  первого дня. Достаём их при загрузке, чтобы калибровка стартовала не с нуля. Ретро-ставка
  помечена и весит **вдвое меньше** живой: она восстановлена, а не наблюдалась. Чего из аудита
  не узнать (правил ли текст) — остаётся пустым, а не заполняется догадкой.
- **О3 · Калибровка.** Уверенность — эмпирическая частота принятия, а не назначенное число.
  Затухание с полупериодом 60 дней: сегодняшнее решение весит больше прошлогоднего, но
  прошлогоднее не исчезает. **Меньше 8 решений по типу — числа нет вовсе**, пишем «мало
  данных: 3 из 8». Честнее не знать, чем знать неверно. **Детектор дрейфа**: точность последних
  20 решений упала на четверть → история обнуляется, статус «учусь заново» — иначе система
  держится за устаревшую правду о владельце, который изменился.
- **У0 · surfaceUsage** — пять строк: сколько раз открыт каждый раздел и когда в последний раз.
  Это вход в П34: сворачивать разделы по ДАННЫМ, а не по чьему-то вкусу.

Инварианты соблюдены буквально: **И-2** (память копит, политику не меняет — веса никуда не
подставляются автоматически), **И-4** (обучение только на внешнем вердикте владельца, не на
своих предсказаниях), **И-5** (`forgetDecisions` удаляет по-настоящему, а не ставит флаг).

Видно в **Контроле** — новый экран не заводился (И-7): блок «Что система знает о своих
предложениях» с числом решений, долей принятых и честным «мало данных», когда данных мало.

**Проверить за 60 секунд**: прими и отклони пару предложений → «Контроль» → блок показывает
типы, число решений и статус; пока решений мало — вместо процента написано, сколько не хватает.

Verified: `decisions-become-calibration.spec.mjs` 6/6 — запись с признаками, честный отказ при
малых данных, измеренная частота 7/8 вместо назначенной, затухание (прошлогодние согласия не
перевешивают свежие отказы), детектор дрейфа, удаление по И-5.

### ✅ П20 · О7 · «Polo → Camry» — одна изменившаяся цель, а не две (2026-07-28)
В марте владелец сказал «хочу Polo», в июле — «беру Camry». Система показывала **две цели** и
считала разрыв по обеим. Перетереть старую нельзя — потеряется март и ответ на вопрос «когда и
почему я передумал». Обе крайности неправы.

Донорский разбор был готов заранее (`node tools/donor-lookup.mjs вытеснение памяти`) — и все
шесть источников (Mem0, Graphiti, Zep, Neo4j GDS) сходятся в одном: **никто не удаляет**.

**Сделано ровно так:**
- **Обнаружение по смыслу, а не по строке.** Конфликт — это ОБЩАЯ тема («машина») и РАЗНЫЙ
  предмет («Polo» / «Camry»). Полное совпадение — это дубль, им занимается дедуп; отсутствие
  общей темы — это просто две разные цели, и трогать их нельзя. Ложное вытеснение прячет
  настоящую цель владельца, а это хуже, чем показать лишнюю.
- **Вытеснение — предложение, а не действие.** Система замечает и спрашивает; решает владелец
  (И-1: судья вне петли). В предложении обе цели и причина словами.
- **Старая цель живёт.** На ней ссылка на новую, дата и причина — цитата, из которой следует
  смена (И-6). Статус `superseded` стал третьим законным состоянием цели: без этого нормализация
  возвращала её в «активные» на первой же перезагрузке.
- **Не всплывает, но существует.** Из действующих списков ушла (`isLiveGoal`), на вопрос «что я
  думал в марте» отвечает. Это decay из Mem0, только честный: не «тихо погасло», а «убрано с
  причиной, можно посмотреть».
- **Откат одним движением.** Вытеснение — гипотеза системы, и ошибаться она обязана дёшево.

**Проверить за 60 секунд**: заведи цель «Хочу купить машину Polo», потом «Беру машину Camry» →
появляется предложение «Похоже, цель изменилась» с обеими; подтверди → в целях остаётся одна
Camry, Polo жив с пометкой и причиной; отмени → Polo вернулся ровно как был.

Verified: `goal-superseded-not-doubled.spec.mjs` 4/4 (предложение, применение с провенансом,
откат, отсутствие ложных срабатываний на разных целях), регресс по целям и деньгам 20/20.

### ✅ П17 · О8 канарейка и П18 · О4 анти-лесть: система не может обмануть себя (2026-07-28)
Калибровка считает себя по тем же данным, на которых учится, — и поэтому не способна заметить,
что поехала. Две проверки закрывают ровно эту дыру, и обе смотрят НЕ туда, куда смотрит она.

**О8 · Канарейка.** Пятьдесят ранних вердиктов владельца замораживаются и **никогда не входят в
обучение**. Раз в месяц система заново предсказывает по ним свой же ответ. Эталон — не выдуманная
планка, а доля принятых в момент заморозки, то есть точность стратегии «всегда соглашаться»:
упасть ниже неё значит стать хуже тупого правила. Упала → **обучение замораживается целиком**, и
ни одно число из журнала больше не выдаётся за знание, пока владелец не разберётся. Замирать
честнее, чем уверенно ошибаться в сторону, которую никто не заметил.

**О4 · Анти-лесть.** Самый тихий способ испортить систему — учить её на СОГЛАСИИ. Владелец
соглашается охотнее, когда предложение звучит уверенно, а не когда оно верно; система замечает
это и начинает звучать увереннее. Поэтому меряются две вещи раздельно: **согласие** (доля
принятых) и **правда** (доля принятых БЕЗ ПРАВКИ). Принял и переписал название — поняли
наполовину, складывать это с чистым согласием нельзя. Расхождение ≥20 пунктов и есть лесть.
Рядом сравниваются уверенности на «да» и на «нет»: если система уверена ровно тогда, когда с ней
соглашаются, это подгонка под согласие, а не знание.

Оба сигнала видны в **Контроле** — новых экранов по-прежнему ноль (И-7).

**Проверить за 60 секунд**: «Контроль» → если система начнёт нравиться больше, чем помогать,
там появится строка про это, а не тихий рост процентов.

Verified: `loop-cannot-fool-itself.spec.mjs` 5/5 — ранняя заморозка отклоняется с честным «рано:
10 из 50», замороженные вердикты выходят из обучения ЦЕЛИКОМ (60 → 10), падение точности
замораживает обучение и это немедленно видно в калибровке, согласие с правкой не считается
попаданием, чистое согласие лестью не объявляется. Регресс петли и чеков 8/8.

### ✅ П22 · Забывание: журналы перестали расти бесконечно (2026-07-28)
Всё, что петля пишет, растёт без предела, а место в браузере конечно: журнал провайдеров на
тысяче расшифровок весит больше самих записей владельца. Забывание — не оптимизация, а условие
того, чтобы система прожила годы.

Правило, отличающее забывание от **потери**: удаляется только то, что уже никого не информирует —
журналы работы САМОЙ СИСТЕМЫ (`providerRuns`, `agentRuns`, строки аудита сверх 1000). Данные
владельца не трогаются никогда: ни заметки, ни задачи, ни цели, ни источники. Остаются САМЫЕ
СВЕЖИЕ записи — именно по ним он что-то понимает. Уборка идёт раз в сутки при загрузке и молча.

**Найдено по ходу и не сделано дважды:** корзину уже уплотняет `normalizeState` (hard-purge
после 30 дней, спека `trash-undo-grace`). Я успел написать вторую реализацию того же правила и
**убрал её**: два места, где решается, когда данные владельца исчезают навсегда, рано или поздно
разойдутся. Пакет оставляет за собой только журналы.

Verified: `forgetting-does-not-lose.spec.mjs` 3/3 (500 прогонов → 200 с сохранением свежих,
данные владельца целы до записи, корзина работает как прежде) + `trash-undo-grace` 1/1.

### 🎯 ВОЛНА 3 ЗАКРЫТА: система улучшается сама, ни одного нового экрана
П13 (У0) · П14 (О1) · П15 (О2) · П16 (О3) · П17 (О8) · П18 (О4) · П20 (О7) · П22 — сделаны.
Осталось: П19 (профиль владельца → впрыск в разбор) и П21 (короткий горизонт ставок).

Что теперь есть у системы, чего не было утром: она **помнит** каждое решение владельца с
признаками, **достаёт** прошлые решения из аудита, **считает** уверенность по фактам вместо
назначенных 0.7, **замирает**, когда канарейка показывает падение, **отличает** согласие от
правды и **не удаляет** старую цель, а помечает её вытесненной с причиной.

Все восемь пакетов соблюдают инварианты буквально: судья вне петли (И-1), память отдельно от
политики (И-2), никакого обучения на себе (И-4), удаление есть у каждой коллекции (И-5),
провенанс на записи (И-6), ноль новых поверхностей (И-7).

### ✅ П27 · Диспетчер, П28 · Летопись, П30 · Цена, П31 · Здоровье доноров (2026-07-28)
Волна 4 закрыта. Все четыре — инструменты, ни одной строки в продукте.

**П27 · `tools/dispatcher.mjs`.** Запуск пакета был ручным действием владельца — значит работа
шла только когда он за ноутбуком. Диспетчер берёт первый неотмеченный пакет очереди, зовёт
исполнителя, гоняет гейт и мержит **только по зелёному**.

Он намеренно скучный и трусливый, потому что автономность без тормозов — это способ быстро
наделать того, что потом никто не разберёт. Пять ограничителей:
1. **Окно** 01:00–07:00. Вне окна выходит молча — это не ошибка.
2. **Стоп-файл** проверяется перед КАЖДЫМ пакетом, а не один раз на старте: остановить ночную
   работу владелец должен уметь, не дожидаясь утра и не убивая процесс.
3. **Бюджет** ≤50% окна. Кончился — до следующей ночи.
4. **Ветка на пакет**, merge только после зелёного гейта; грязное дерево — отказ запускаться.
5. **Три провала → BLOCKED**, ветка оставлена владельцу, диспетчер идёт дальше, а не бьётся до
   утра.

Очередь читается из **плана владельца**, а не из своего списка: и таблицы `| **2.2** |`, и
заголовки `### Пакет 2.2 —`. Источник истины один, подстраивается инструмент. Сухой прогон
показывает 19 неотмеченных пакетов.

**П28 · `tools/chronicle.mjs`.** PROGRESS.md — рассказ для человека, и он для этого и нужен. Но
по нему нельзя посчитать, **какой гейт ловит чаще всего** — а это карта тонких мест продукта,
не список наших ошибок. Летопись пишет одну строку JSON на событие: какой гейт, сколько попыток,
что влито, что заблокировано.

**П30 · Цена за принятый пакет** — минуты и попытки, `--ledger` даёт готовую строку для
PROGRESS.md. Числом, но **не метрикой оптимизации**: дешёвый пакет, не закрывший боль, — не
достижение. Пока пакеты запускает владелец руками, летопись честно говорит, что писать нечего.

**П31 · `tools/audit-donor-health.mjs`.** Донорский разбор — снимок чужого проекта на конкретный
день, а проекты живут дальше. Через полгода наш «разбор лучших практик» описывает то, чего у
донора уже нет. Аудит **в сеть не ходит** (§7): считает возраст строк «Проверено ГГГГ-ММ-ДД» в
самом каталоге и называет, что перечитать и какие разборы на этом стоят. Красным становится
только когда протухла половина базы — одна старая запись это норма, половина значит база стала
архивом. Сейчас: 14 доноров, самой старой записи 7 дней.

### ✅ П29 · Калибровка приёмки: владелец утверждает политику, а не каждый пакет (2026-07-28)
Если каждый пакет требует его глаза — автономность упирается в его сутки. Если ни один не
требует — он теряет продукт. Выход не в доверии «вообще», а в доверии **по видам работы**,
подтверждённом историей: он утверждает политику («правки брифов сливай сам»), а не экземпляр
(«вот этот бриф сливай»).

`tools/acceptance-policy.mjs` считает по летописи точность приёмки в разрезе видов работы
(документация · гейты и спеки · интерфейс · разбор захвата · петля · структура кода) и
предлагает политику. Три правила, без которых это самообман:

1. **Политику включает владелец.** Инструмент пишет `approvedByOwner: false` и **не имеет права**
   поставить true. Иначе система разрешала бы себе сама — ровно то, чего нельзя.
2. **Планка с запасом:** ≥95% принятых при ≥20 пакетах вида. На пяти наблюдениях 100% не значит
   ничего.
3. **Необратимое — всегда через него, при любой истории.** Удаление данных · публикация наружу ·
   деньги · ключи · смена контракта продукта. Тут статистика не аргумент: цена одной ошибки не
   компенсируется девяноста девятью удачами.

Диспетчер получил шестой ограничитель: зелёный гейт — необходимое, но не достаточное условие
слияния. Нет политики, не утверждена, вид не разрешён, пакет пахнет необратимым — ветка остаётся
владельцу с записью `awaiting-owner` в летописи.

Сейчас история пуста, поэтому авто-слияние невозможно ни для одного вида — и это **правильный
исход, а не недоработка**: доверие берётся из наблюдений, а не из уверенности.

### 🎯 ВОЛНА 4 ЗАКРЫТА
П23 (права) · П24 (трёхуровневый гейт) · П25 (анти-подделка) · П26 (граница доверия) ·
П27 (диспетчер) · П28 (летопись) · П29 (калибровка приёмки) · П30 (цена) · П31 (здоровье
доноров) — все девять сделаны.

### ✅ П36 · HERMES разобран, pending снят (2026-07-28)
Владелец назвал Hermes как донора, но локально его не было — запись висела `pending` и никем не
проверялась. Апстрим найден: **`NousResearch/hermes-agent`**, 221 тыс. звёзд. Скачан, лицензия
прочитана **файлом** `LICENSE` («MIT License, Copyright (c) 2025 Nous Research»), а не по памяти
— правило CLAUDE.md §3.1 существует ровно потому, что память про лицензии врёт.

**Вывод одной фразой: транспорт, не мозг.** Что стоит перенять:
- **Граница канала.** `plugins/platforms/telegram/adapter.py` — 9911 строк, и это ТОЛЬКО канал:
  приём, разбиение длинных ответов, кнопки, поток. Логики продукта внутри нет.
- **Whitelist как вход, а не опция.** `allow_from` по user_id проверяется до любой обработки.
- **Сменный STT с правилом «встроенное всегда выигрывает»**: плагин не может подменить основной
  путь, и это проверяется дважды — при регистрации и при вызове. Дисциплину берём целиком.

**Чего не берём и почему:**
- **Транскрипцию отдавать нельзя.** Hermes умеет расшифровывать сам, но чужая расшифровка придёт
  готовым текстом без сегментов и секунд — и оборвётся путь «вывод → цитата → секунда записи»,
  который П5 только что построил. Провенанс один.
- **Второй путь импорта не строим**: Telegram → `server.mjs` → существующий
  `importFilesFromInput`. Иначе дедуп по байтам, `.m4a` и статусы придётся реализовывать дважды.
- **Их память — блокнот агента, не база продукта.** Как только «что-то запомнил бот» станет
  источником для базы, провенанс перестанет быть проверяемым.
- **Кода не берём**: Python поверх `python-telegram-bot`, у нас vanilla JS (стек-фильтр).

Разбор доступен запросом: `node tools/donor-lookup.mjs голос с телефона`.

### ✅ П19 · Профиль владельца и П21 · Короткий горизонт — волна 3 на 100% (2026-07-28)
**О5 · Профиль.** Журнал копил факты, но разбор о них не знал — и повторял ту же ошибку в сотый
раз. Профиль читает журнал и говорит разбору, чего владелец не берёт. **Строго в одну сторону:**
предложение не отбрасывается (решать не системе), а получает пониженную уверенность и **причину
словами** — «Ты берёшь такое редко (12% из 10), поэтому предлагаю осторожно». Впрыск в ОДНОЙ
точке — `addProposal`, чтобы не было двух правд. Порог тот же, 8 решений: на трёх наблюдениях
профиль был бы не знанием о владельце, а суеверием о нём.

**О6 · Короткий горизонт.** Выборка растёт медленно, если каждая ставка проверяется неделями.
Ставка «эту задачу он закроет сегодня» проверяется к вечеру. Главное правило: **молчание не
считается ошибкой** — ставка без ответа помечается `unverifiable` и **выходит из знаменателя**.
Считать молчание отказом значит учить систему на том, чего не было.

Verified: `profile-and-short-bets.spec.mjs` 4/4.

### ✅ П34 · Меню сворачивается по данным, а не по вкусу (2026-07-28)
Какой кластер раскрыт по умолчанию — вопрос вкуса ровно до тех пор, пока нет данных. Они есть:
У0 считает открытия каждого раздела. Порядок решения: **что владелец открыл руками → где лежит
текущий экран → куда он ходит чаще всего**. Вкуса в этой цепочке нет нигде. Порог 3 открытия:
один случайный заход не должен переставлять меню под рукой; данных не хватило — остаётся прежний
порядок, а не выдуманное предпочтение.

### ✅ П37 · Петля ценности замкнулась (2026-07-28)
Три механизма были по отдельности: чек фиксирует подтверждённое, журнал помнит, калибровка
считает. Не хватало **замыкания** — момента, когда подтверждённое возвращается выводом.

Владелец подтвердил такси в третий раз — и получил не четвёртое такое же предложение, а
наблюдение: «Такси: 3 раза за период, всего 2400 ₽», собранное из трёх конкретных подтверждений
со ссылками на них.

Правило честности здесь строже обычного: **вывод делается ТОЛЬКО из подтверждённого**.
Отклонённое предложение — не факт жизни владельца, и строить на нём инсайт значит выдавать свою
гипотезу за его данные. Порог 3 повтора: два раза это совпадение, а обесценив вывод один раз,
вернуть ему доверие нечем.

Verified: `value-loop-closes.spec.mjs` 3/3 — вывод собирается и доходит с провенансом,
отклонённое в него не входит, на паре совпадений вывод не делается.

### 🎯 ВОЛНА 3 ЗАКРЫТА ПОЛНОСТЬЮ (П13–П22)

### ✅ П35 · Голос с телефона доходит до артефакта (2026-07-28) [ЛОКАЛЬНАЯ ПРИЁМКА]
Смысл приходит в дороге, где есть телефон и нет LifeOS. К вечеру мысль либо потеряна, либо лежит
в чужом мессенджере, откуда её никто не достанет.

Мост в `server.mjs` построен ровно по трём выводам донорского разбора (`node tools/donor-lookup.mjs
голос с телефона`):
1. **Транспорт, не мозг.** Мост приносит байты и всё. Разбор, дедуп, расшифровка и провенанс —
   в приложении, через **существующий** `importFilesFromInput`. Второго пути импорта нет: иначе
   `.m4a`, дедуп по байтам и статусы жили бы в двух реализациях и однажды разошлись бы.
2. **Whitelist — вход, а не опция.** Без `LIFEOS_TELEGRAM_ALLOW` мост молчит для всех. Не
   «отвечает всем по умолчанию»: бот с токеном доступен любому, кто узнает его имя.
3. **Транскрипция остаётся нашей.** Чужой текст пришёл бы без сегментов и секунд — и оборвался
   бы путь «вывод → цитата → секунда записи», который построил П5.

Токен и whitelist — в переменных окружения, не в репозитории (§7). Без токена мост честно не
поднимается: `not-connected` с объяснением, никакой имитации ожидания.

**Спека вскрыла две моих ошибки о продукте, а не в продукте:**
- Я ждал, что текст из Telegram станет ЗАДАЧЕЙ. Он становится **предложением** — это тот же
  контракт «ничего молча», и мост его не обходит. Проверка исправлена на верную.
- Повтор той же голосовой уходит в **разбор дублей** по байтам, а не удваивает базу. Второго
  механизма дедупа для Telegram не заводилось — работает тот же.

Verified: `voice-from-phone-arrives.spec.mjs` 2/2. Живой бот проверяется на машине владельца с
его токеном от @BotFather — метка **[ЛОКАЛЬНАЯ ПРИЁМКА]**.

### ✅ П38 · Видео 90 секунд — и оно сразу поймало дефект (2026-07-28)
`tools/record-90s-demo.mjs` снимает сценарий владельца на ЖИВОМ приложении: три голосовых → три
артефакта → подтверждение → граф → след в Контроле → одно действие. Playwright пишет плёнку сам,
поэтому там нет ни одного нарисованного кадра. **Сценарий не прошёл — видео не считается снятым**,
и это правильный исход: доказывать нечего, пока путь не работает целиком.

**Первая же съёмка вскрыла настоящий дефект, которого не видела ни одна спека.** Надиктовка из
трёх строк про смену дала правильные смена + доход + ставку — **и задачу** с названием «Так
поработал с 8 до 3 часа получается после налога пришло Потом ещё получается всего». Ровно та
склейка, ради устранения которой делался П1.

Почему спеки этого не ловили: во всех их входах была ещё и клауза-задача («надо в аптеку»), она
попадала в `clauseTypes`, и черновик целого текста отбрасывался заодно. Стоило убрать её из
входа — и он выжил.

**Правило исправлено в корне:** черновик, снятый со ВСЕГО текста, описывает надиктовку как ОДИН
смысл. Если намерения разобрали её на несколько — он заведомо неверен, и не важно, совпал ли его
тип с чьим-то ещё.

Это и есть смысл видео как гейта: оно смотрит на продукт глазами владельца, а не глазами автора
теста. **Ни один зелёный набор этого не показывал.**

Видео: `output/demo/lifeos-90s.webm` (3,7 МБ).
Verified после правки ядра: регресс разбора **37/37** (raw-speech, voice-to-artifacts,
spoken-pauses, one-record-one-meaning, event-or-task, task-vs-habit, observation-not-task,
goal-deadline, money-fast-capture, shift-week-cycle, value-loop).

### ✅ П6 · Мёртвый код удалён: 2342 строки (2026-07-28)
`app.js` 28 472 → **26 172 строки**. Удалено 102 недостижимые функции.

**Две ошибки по дороге, обе — про инструмент, а не про код:**

1. **Массовое удаление съело живую функцию.** Мой разбор границ на Python спотыкается о
   вложенные шаблонные строки: внутри `${...}` бывает ещё один бэктик, и «строка» кончается не
   там. Вырезав 123 функции разом, я унёс с собой `renderCalendarPanel`. Регулярки и самодельные
   парсеры на 1,6 МБ живого кода ошибаются **молча** — поэтому удаление пошло **по одной
   функции с `node --check` и смоком после каждой**, и семь спорных случаев инструмент отверг
   сам.
2. **Аудит читал четыре гейта из тридцати.** Список был написан по памяти. По нему удаление
   уронило `audit-architecture-contract`, `audit-workspace-links` и `audit-product-brain`.
   Исправлено в корне: **гейт — это любой `tools/audit-*.mjs`**, и проверяется не только имя
   функции, но и строки внутри её тела (смок требует `data-testid="global-search"`, а не только
   `function renderCalendarPanel`).

**Что осталось и почему:**
- **35 функций / 1182 строки закреплены гейтами.** `smoke.mjs` и аудиты требуют их наличия,
  хотя браузер не исполняет их ни разу. **Гейт зелёный — а экрана нет.** Удаление потребует
  правки гейтов, то есть выхода за право исполнителя: **решение владельца**.
- **8 функций / 407 строк** недостижимы, но их границы разбор определяет неоднозначно — удалять
  автоматом опасно.

Планка снижена явно (`--write-baseline`): было 125 свободных, стало 8. Все 28 аудитов зелёные,
`npm run verify` зелёный.

### 🔧 Навигация в 65 спеках пошла за продуктом (2026-07-28)
П32 свернул разделы-каркасы за строку «+N в разработке», чтобы рабочее было видно сразу. До
каркаса стал нужен один дополнительный клик — и 65 спек, которые ходят туда общим помощником
`openSurface`, перестали его находить. Мой прежний греп их не увидел: они строят `data-testid`
динамически (`surface-${id}`), а не строкой.

Помощник во всех 65 научен раскрывать строку каркасов, если раздел не виден сразу.
**Ни одного утверждения не удалено** — `audit-no-gate-tampering` подтверждает: `tampering: []`,
все изменения аддитивные. Возвращать прежнюю плоскую простыню из шестнадцати строк ради
удобства тестов было бы решением наоборот.

Проверено на четырёх из пяти упавших: `system-factory` (2), `ten-starter-packs`,
`twin-recovery-drill` — зелёные.

**Пятая — `notion-grade-pack` — падала ДО сессии.** Проверено прямо: откатил `app.js`, `ui/`,
`styles.css` и `index.html` на стартовый коммит `9eebd9f` — падает там точно так же
(`money-sparkline` есть в DOM, но нулевого размера). `ui/home.js`, где он рисуется, за сессию
не менялся ни разу. Это отдельная боль, у неё будет свой пакет.

### 🔧 Спека дней недели перестала пересчитывать календарь сама (2026-07-29)
`weekday-deadline` («до среды») упала в полном наборе — и это оказался не дефект разбора, а
**часовой пояс в самой спеке**.

Она считала ожидаемый день от ЛОКАЛЬНОЙ даты браузера, продукт — от своего `todayKey()`. В
трёхчасовом окне после полуночи (UTC+3) они расходятся: продукт видел вторник и отвечал «завтра
среда» — **верно**, — а спека видела среду и требовала следующую.

Тот же класс ловушки уже записан в самом коде разбора: «владелец в UTC+3 говорил „до среды“ и
получал четверг; в облаке (UTC) ошибка не проявлялась вовсе». Теперь спека спрашивает у продукта
его «сегодня» вместо того, чтобы держать вторую копию календаря. Тест обязан проверять продукт,
а не пересчитывать календарь заново.

**Разбор не менялся.** Проверено поштучно: четверг → завтра, понедельник → следующий, пятница →
через два дня; неверной была только опора спеки.

### 🔧 Навигация в спеках доведена до конца (2026-07-29)
Первая правка помощника `openSurface` починила четыре спеки из пяти, но `presentation-runtime`
осталась красной, и разбор показал две настоящие ошибки в самом помощнике:

1. **Цикл по «первому тумблеру в DOM» ходил по кругу.** Раскрыт всегда ОДИН кластер, поэтому
   после раскрытия «Ввода» первым в DOM становится тумблер «Дел», а после раскрытия «Дел» —
   снова «Ввода». Помощник бесконечно переключался между двумя группами и до «Мастерской» не
   доходил. Теперь группы перебираются **поимённо**.
2. **Не ждал перерисовку.** Раскрытие «Ещё» живёт в состоянии, а не в браузерном `<details>`:
   после клика идёт коммит и полная перерисовка. Проверка видимости успевала до нового DOM.

После правки `system-factory` и остальные зелёные, а `presentation-runtime` дошла до настоящей
проверки (`#view-preset-renderer` ждёт `timeline`, получает `card`) — и **эта поломка была до
сессии**: проверено откатом `app.js`, `ui/`, `styles.css`, `index.html` на стартовый коммит
`9eebd9f`, падает там так же.

`audit-no-gate-tampering`: `tampering: []` — за все три правки помощника не удалено ни одного
утверждения.

## 🎯 БАРЬЕР: полный набор после всей работы — 309 прошло / 17 упало / 7 пропущено

Утренний замер был **249 / 20** (на `202956f`). Спек за сессию стало 136, аудитов 33.

**Все 17 падений — средовые или предсуществующие.** Проверено поимённо:

| спека | причина |
|---|---|
| `whisper-transcribe`, `voice-loop-vosk` | скачивание моделей с HuggingFace |
| `voice-loop-whispercpp`, `waveform-record` | запись с микрофона (`getUserMedia`) |
| `ollama-real-daemon`, `chat-brain-graph-context` | живой локальный демон под нагрузкой |
| `mobile-pwa-continuity` | офлайн-PWA, известное средовое |
| `undo-redo` ×3 | известное средовое, записано в передаче |
| `finance-deep` | зависит от даты — **проверено стешем**, красная и на чистом HEAD |
| `notion-grade-pack`, `presentation-runtime` | **проверены откатом UI на `9eebd9f`** — падали до сессии |
| `final-human-product` H05/H06/H09, `final-journeys` P19, `kb-smoke` | длинные сквозные, копят средовые падения выше |

Заменять их на моки нельзя: смысл именно в том, что они проверяют НАСТОЯЩИЙ движок. Провайдеры —
самая красная часть набора, и это честно.

**Инструменты гейта после сессии:** `npm run verify` зелёный, 31 аудит из 33 зелёные.
Два красных названы: `audit-context-freshness` живёт синхронно с кодом (позеленел после сверки
брифов), `audit-owner-rescue-final` требует ветку `owner-usable-nonstop-rescue`, а работа шла на
`claude/lifeos-artifact-inspector-5lg95m` — это не дефект, а условие аудита.

### ✅ П7 срез 1 · Раскол `app.js` начат: чистый слой вынесен (2026-07-29)
Единственный несделанный пакет плана. Резать 26 тысяч строк одним движением нельзя — сегодня же
мой разбор границ **молча съел живую функцию** при удалении мёртвого кода. Поэтому раскол идёт
срезами, и первый выбран так, чтобы ошибиться было почти негде.

**Что вынесено.** `core/text.mjs` — 24 функции и константа, у которых **нет ни одной зависимости
от состояния, DOM и хранилища**: они принимают значение и возвращают значение. `cleanLine`,
`shorten`, `normalizeTitle`, `repairMojibake`, `escapeHtml`, `todayKey`, `now`, `makeId`,
`dateKeyFromOffset`, `clone`, `chunkString`, `pluralRu`, `uniqueCleanItems` и остальные. Именно
поэтому цикла импортов не возникает: модуль не смотрит назад.

**Ошибка, ради которой всё это делалось срезами.** Первый разбор зависимостей искал только
`имя(` и пропустил функцию, переданную **ссылкой** — `.replace(re, decodeMojibakeRun)`. Приложение
упало на первом же прогоне спек: `ReferenceError` внутри `repairMojibake`. За ней потянулись
`cp1251ByteForCodePoint` и `CP1251_EXTRA_BYTES`. Все три довынесены, модуль замкнут.

**Два аудита, чтобы это не повторилось:**
- `audit-module-closure` — у модуля не должно быть НИ ОДНОЙ ссылки обратно в `app.js`. Проверяет
  все идентификаторы, а не только вызовы, и предварительно вырезает комментарии и строки: без
  этого он нашёл слово «app» внутри «app.js» в комментарии и объявил модуль разорванным.
- `audit-module-size` — планка размеров, которая **только опускается**. Правило раскола, а не
  «не расти»: модули обязаны расти, код в них переезжает; красный только если растёт файл,
  который уже больше 80 КБ.

**Оболочка и сборка знают о `core/`:** `service-worker.js` кэширует модуль (иначе офлайн падал бы
на первом импорте), `tools/build-public.mjs` копирует каталог.

**Честный масштаб.** Вынесено 10 КБ из 1521. Это **0,7% работы П7** — но проверенный механизм и
два аудита, после которых следующие срезы механические. Из 37 файлов проекта цель ≤80 КБ не
выполняет ровно один — `app.js`.

Verified: `npm run verify` зелёный, ключевые спеки 20/20, 33 аудита из 35 зелёные (прежние два —
свежесть брифов, живущая синхронно с кодом, и ветка в `audit-owner-rescue-final`).

### ✅ П7 срезы 2 и 3 · Раскол пошёл механически (2026-07-29)
После первого среза появился инструмент — `tools/split-slice.mjs`. Он берёт «семя» из пары имён,
сам раскрывает **замыкание** (добирает все объявления, на которые срез опирается) и режет только
после трёх отказов: имя не найдено, срез трогает состояние/хранилище/DOM, вырезы перекрываются.

**Каждая проверка появилась из настоящей ошибки, а не из осторожности:**
- Счёт скобок ломался о регулярки: `[А-Яа-яЁё]` внутри `/…/` — тоже скобка, и счётчик уезжал на
  сотни строк. Границы констант теперь определяются по форматированию файла (верхний уровень
  всегда с нулевой колонки).
- Закрывающая строка бывает не голой — объявление может кончаться вызовом в конце. Без этого
  разбор считал его незакрытым и захватывал следующую функцию.
- Импорты собираются ПО МОДУЛЯМ: имя приходит из того файла, где объявлено. Первая версия
  собирала всё из одного и ломала второй модуль.

| срез | модуль | что вынесено | КБ |
|---|---|---|---|
| 1 | `core/text.mjs` | чистый слой: текст, даты, идентификаторы (24 объявления) | 10 |
| 2 | `core/ru-parse.mjs` | разбор русского времени и дат (7 объявлений) | 18 |
| 3 | `core/speech-intents.mjs` | намерения речи целиком, RR-001 Этап B (25 объявлений) | 26 |

**`app.js`: 1521 → 1468 КБ.** Честно: это **3,5% работы П7**, и файл по-прежнему единственный в
проекте, кто не выполняет цель ≤80 КБ. Но механизм проверен трижды, оба аудита зелёные, и
следующие срезы делаются одной командой:

```bash
node tools/split-slice.mjs core/<модуль>.mjs "<заголовок>" <имя1> <имя2>
```

Замер кандидатов: **386 чистых замкнутых функций на 281 КБ** — это 19% файла, вынимаемые тем же
инструментом без единого нового решения. Остальное потребует разбирательства: там состояние.

Verified после каждого среза: `node --check`, `audit-module-closure`, `audit-module-size`,
`npm run verify` и целевые спеки — 13/13 на разборе дат, 9/9 на речи. Оболочка кэширует все три
модуля, публичная сборка их копирует.

### ✅ П7 срезы 4–6 · Шесть модулей, `app.js` 1521 → 1438 КБ (2026-07-29)

| срез | модуль | КБ |
|---|---|---|
| 4 | `core/graph-math.mjs` — кластеры и укладка графа | 7 |
| 5 | `core/ru-money.mjs` — суммы и деньги в русской речи | 7 |
| 6 | `core/ru-entities.mjs` — люди, места, проекты, даты | 18 |

Инструмент отказался резать `graphForCanvas` — тот трогает `state`, и вынос породил бы цикл
импортов. Это и есть работа проверки: она сказала «нельзя» до правки, а не после падения спек.

**Две правки гейтов, обе названы, ни одного утверждения не удалено:**

1. **`smoke.mjs` ищет маркеры и в `core/*.mjs`.** `applyForceTick` переехал в модуль, и смок его
   потерял. Проверка осталась той же — «такая функция обязана существовать», — изменилось только
   место, где она живёт. Ослаблением было бы убрать маркер.
2. **`audit-no-gate-tampering` стал точнее.** Он покраснел на правке выше, потому что считал
   ослаблением ЛЮБУЮ содержательную строку `smoke.mjs`. Теперь подделка — это удалённый **элемент
   списка маркеров**, а не любая строка. И комментарии больше не считаются утверждениями: аудит
   поймал сам себя на слове «expect» внутри пояснения — случай, когда инструмент строже правды и
   потому мешает работать.

Verified: `npm run verify` зелёный, 35 аудитов из 37 (прежние два — свежесть брифов и ветка),
регресс по шести срезам 26/26 (`money-fast-capture` флакнула в наборе, поштучно зелёная за 27 с).

**Что осталось от П7.** 386 чистых замкнутых функций на ~276 КБ вынимаются тем же инструментом
одной командой каждая. Остальное — код, который трогает состояние: там нужен не вынос, а
разделение ответственности, и это отдельная работа с продуктовыми решениями, а не механика.

### ✅ Дом отвечает на три вопроса, а не показывает устройство движка (2026-07-29)
Владелец разобрал запись экрана и сформулировал главное точнее, чем любой мой аудит:
**LifeOS показывает внутреннее устройство движка вместо результата его работы.**

Из этого вытекало почти всё остальное: конкурирующие главные действия, «11 связей» вместо
вывода, меню из имён подсистем, ощущение набора независимых экранов вместо продукта.

**Что сделано — сжатие, а не расширение. Ни одного нового экрана.**

- **Одна колонка, одна история.** Ввод → «Что я понял из записи» → одно главное действие →
  «Мой день». Две колонки заставляли глаз выбирать, с чего начать.
- **Ровно одно главное действие.** Есть свежий разбор — экран про него; нет — экран про то, что
  важнее всего сегодня. **Никогда оба**: раньше «Разобрать поток», поле ввода и «Записать смену»
  спорили за внимание, и мозгу не за что было зацепиться.
- **Данные заранее убраны.** Плитки Деньги/Привычки и сетка виджетов — за строкой «Показать
  цифры дня». Они не удалены: у них отобрано право занимать первый экран.
- **Меню заговорило по-человечески.** «База» → **«Записи»**, «Граф» → **«Связи»**, «Контроль» →
  **«Что изменилось»** (и в верхней панели тоже). Это имена подсистем: так думает разработчик, а
  не человек, который надиктовал мысль. Идентификаторы не тронуты — меняются подписи, а не
  маршруты.
- **Карточка говорит от лица владельца.** «LifeOS понял» → **«Что я понял из записи»**.

**Что я попробовал и отменил.** Прятал «Мой день», когда в нём пусто. Спека `today-home-quick-actions`
показала, что сценарий от этого стал хуже: после быстрого действия владелец должен видеть, что
созданное ДОЛЕТЕЛО, а не искать подтверждение. Правило владельца «запрещено ухудшать сценарий
ради красоты» сработало буквально — и я вернул блок. Пустой «Мой день» честно говорит «сегодня
пусто», и это тоже ответ на «что сейчас происходит».

**Две правки гейтов, названные вслух** (`audit-no-gate-tampering` их и поймал):
`human-public` и `kb-smoke` проверяли подпись «LifeOS понял». Проверка осталась та же — что
карточка понимания на экране и в ней есть текст, — изменилось слово. Это тот самый законный
случай «продукт изменился намеренно», ради которого аудит и требует моего слова владельцу.

Verified: `today-home-quick-actions`, `first-run-calm`, `human-public`, `honest-navigation`,
`money-fast-capture` — 3/3 и 7/8 в двух прогонах, `npm run verify` зелёный, 34 аудита из 37.

**Осталось нечестным и названо:** верхняя панель по-прежнему дублирует навигацию («Ввод»,
«Что изменилось»), потому что на эти кнопки опираются четыре спеки. Дубль убирается вместе с
ними — это следующий пакет, а не то, о чём стоит молчать.

### ✅ Инсайт стал выводом, а не числом (2026-07-29)
Владелец: «Блок называется Инсайты, но инсайтов нет. Инсайт — это не связь, инсайт — это вывод».
И привёл, каким он должен быть: «уже три дня подряд ты говоришь о долговременной памяти».

Раньше блок показывал «11 связей» и «частая тема: память — встречается в 4 заметках». Это
состояние базы данных. По нему нельзя принять ни одного решения.

**Что теперь считается (`core/owner-themes.mjs`, чистый слой — ни хранилища, ни DOM):**

- 🧭 **«Ты возвращаешься к этому 3-й день»** — тема звучала в записях ≥3 РАЗНЫХ дней за неделю.
  Тема — основа слова в 5 знаков: на шести «память» и «памяти» расходятся в разные темы, на пяти
  сходятся в одну. Проверено на живых записях, а не выведено из головы.
- 🎯 **«Сегодня ты думаешь об одном»** — одна тема набрала ≥3 записей И не меньше половины дня.

**Три ошибки по дороге, каждая исправлена в корне, а не порогом.**

1. Первый живой вывод звучал так: **«Сегодня ты думаешь об одном: LifeOS Product Brain»** —
   17 из 37. Продукт считал своей мыслью владельца **собственную документацию**, которую сам же
   завёл при первом запуске. Исключены заметки папки Product Brain.
2. Осталось 12 из 20 — заметки, которые платформа заводит о своих каналах и системах, лежат в
   той же папке, что и заметки владельца. Теперь `ensureV34ArtifactNote` и `ensureProductMapNote`
   помечают их `origin: "system"` в момент создания. **Отсутствие пометки значит «писал он»** —
   поэтому старые заметки не пропали и миграция не нужна (ловушка `normalizeState`).
3. Цитата в выводе выглядела сломанной: «…связаны сильнее, чем Память и провенан…». Заголовок
   заметки часто есть начало её тела, и склейка «заголовок + тело» удваивала первую фразу.

**Порог — два условия сразу, и оба нужны.** Только доля («60% дня») не срабатывала никогда:
любой день разбавлен записями о другом. Только абсолют («3 записи») срабатывал ложно на большой
базе. Фокус — это когда об одном, не меньше трёх раз, и это половина дня.

**Вывод обязан объяснять себя.** У каждого есть `detail` («звучала в записях 3 разных дней»,
«12 из 20 сегодняшних») и `refs` на записи. Вывод без ссылок неотличим от догадки, а догадке
владелец верить не должен.

Гейт: `insights-are-conclusions.spec.mjs` — три спеки, и третья проверяет ЛОЖНЫЙ вывод: три
записи о разном выводом об одном не объявляются. Ложный вывод хуже отсутствующего — он учит не
верить выводам вообще.

Попутно: страница перестала перезагружаться каждые 5–7 секунд (барьер `controllerchange` жил в
памяти страницы и обнулялся самой перезагрузкой — теперь в `sessionStorage`, 60 секунд);
`core/format.mjs` — 10 подписей дат/денег/размеров вынесены из монолита; 7 брифов сверены с
кодом.

Verified: `insights-are-conclusions` 3/3, `npm run verify` зелёный, 36 аудитов из 37
(`audit-owner-rescue-final` требует имени ветки владельца и чистого дерева — среда, не код).

### ✅ Приложение обязано запускаться — новый гейт (2026-07-29)
**Я сломал продукт и запушил сломанным.** При выносе выводов в `core/owner-themes.mjs` параметр
`systemFolderId` остался у одной функции, а проверка с ним уехала в другую. Вместо экрана
владелец получил бы «Хранилище остановило сбой, данные целы».

Молчали ВСЕ проверки, и каждая — законно:
- `node --check` — синтаксис верный;
- `audit-module-closure` — ссылки обратно в `app.js` нет: имени нет нигде вообще;
- `smoke.mjs` — ищет строки в исходнике, а строка на месте;
- 37 аудитов — ни один не запускает приложение.

Поймал только полный набор спек, заплатив двумя часами прогона.

**`tools/audit-app-boots.mjs`** — открывает приложение в браузере и требует оболочку на экране
без аварийного баннера. Пятнадцать секунд. **Проверен обратным ходом:** с возвращённой ошибкой
краснеет, без неё зелёный — иначе это был бы гейт-украшение. Без сервера пропускается ВСЛУХ, а не
проходит молча.

Попытка, которую отменил: усилил `audit-module-closure` до проверки «имя связано хоть где-то».
Он покраснел на флагах регулярок (`giu`, `iu`) и деструктуризации — инструмент строже правды
мешает работать сильнее, чем его отсутствие. Откатил, сделал честный запуск вместо статики.

Попутно: подписи заговорили о владельце — «Инсайты · замечено в твоих данных» → «Что я заметил ·
выводы из твоих записей», «уверенность: средняя» → «пока предположение» и только при НЕ высокой
уверенности.

Verified: `insights-are-conclusions` 3/3, `audit-app-boots` зелёный и проверен обратным ходом,
`npm run verify` зелёный, 37 аудитов из 38 (`audit-owner-rescue-final` требует ветки владельца).

### ✅ Верхняя панель перестала дублировать навигацию (2026-07-29)
Первый долг очереди `docs/HANDOFF_PROMPT.md` §2. В шапке жили две кнопки — «Ввод» и «Что
изменилось», — и обе не добавляли ничего: **«Что изменилось» дословно повторяло девятый пункт
главного меню, тем же словом**, а «Ввод» вёл на экран `capture`, до которого ведут и кластер
«Ввод и разбор», и нижняя панель телефона. Две одинаковые кнопки в двух местах одного экрана —
это не быстрый доступ, а вопрос «а эти две разные?».

**Сценарий проверен ПЕРЕД удалением, а не после** (§3 запрещает ухудшать путь ради чистоты):
на большом экране `surface-control` виден в рейле всегда; на телефоне рейл скрыт, но
`mobile-more-control` лежит в «Ещё», а `capture` — прямо в нижней панели. Ни один путь не
исчез, исчезло только его повторение.

**Четыре спеки переписаны на настоящую навигацию, а не удалены** — в этом и была работа:
`undo-redo` ходит через `surface-control`, `kb-smoke` (телефон) через «Ещё» → `mobile-more-control`
— и теперь проверяет строго больше прежнего, потому что подтверждает существование пути С
ТЕЛЕФОНА без шапки; `final-journeys` и `market-owner` — через `openSurface`/`openCaptureSurface`.

**Гейт поймал меня по делу, и это стоит записать.** Заменив пару «клик + `expect(workspace-capture)`»
на один `openSurface(..., "workspace-capture")`, я считал проверку сохранённой: помощник
утверждает видимость внутри. `audit-no-gate-tampering` увидел иначе — из спеки исчезли три
литеральных утверждения. Он прав не формально: проверка, спрятанная в помощник, перестаёт
читаться глазом в самой спеке. Утверждения возвращены явными строками рядом с навигацией.

**Проверить за 60 секунд**: в шапке остались только линзы, поиск, тема и статус сохранения;
«Что изменилось» — один раз, в меню слева; на телефоне он же в «Ещё».
Verified: `kb-smoke` мобильный 1/1 (переписанный — зелёный), `npm run verify` зелёный,
`audit-app-boots` зелёный, `audit-no-gate-tampering` зелёный после возврата утверждений,
`node tools/build-public.mjs` прогнан.
**Честно красное и НЕ мной**: `kb-smoke:58`, три теста `undo-redo`, `final-journeys` J01 и семь
аудитов (`context-freshness`, `final-journeys`, `nonstop-until-done`, `owner-rescue-final`,
`performance-final`, `recovery-final`, `release-evidence`) красные и на базовом коммите —
сверено прогоном с отложенными правками. `market-owner` целиком `test.skip` с 2026-06-28
(написана под доchat-first оболочку), поэтому мои правки в ней компилируются, но не исполняются.
### 🔶 П40 начат — ДОНОР ПРИХОДИТ САМ (ядро готово, обвязка за удалённой сессией)
Владелец описал сценарий дословно: «нашёл репозиторий, где граф работает лучше, добавил файлом;
вечером пришёл — обработано, определено, что это репозиторий для изучения; дальше вопросник: у
кого лучше, у нас или у них; можем сделать себе лучше — постановка в очередь на внедрение».

**`core/donor-intake.mjs` написан и проверен на живых примерах** (чистый слой: ни сети, ни
хранилища, ни DOM):

- `extractRepoRefs` — ссылка в любой форме: с https и без, в скобках, с `/tree/main`, четыре
  хостинга. Служебные страницы (`/topics/`, `/orgs/`) репозиториями не считаются.
- `classifyLicense` — MIT/Apache/BSD/ISC → код можно; AGPL/GPL/LGPL/MPL/SSPL/BUSL → только идея.
  **Отсутствие LICENSE не значит «наверное MIT»** — значит, что права не переданы вовсе. Порядок
  проверок важен: AGPL содержит слова «GENERAL PUBLIC LICENSE», а BSD-2 — подстроку BSD-3.
- `classifyStack` — Python/Java/Go/React/Node-only → в браузер не встаёт; Vanilla JS, Rust→WASM →
  встаёт. **При конфликте решает ЗАПРЕТ:** ошибка в эту сторону стоит вечера, в другую — продукта.
- `compareWithOurs` — что из этого у нас уже сделано. Без сверки любой донор выглядит заманчиво,
  и мы третий раз пишем готовое.
- `donorVerdict` — три исхода и ни одного «посмотрим потом», всегда с причиной. Отклоняем только
  когда брать нечего: нечистый стек не повод молчать — принцип из Python переносится страницей
  математики, ровно так взят Graphiti.

Проверено: `graphology` → «код · у нас нет: Louvain кластеры». `graphiti` (AGPL + Python) →
«принцип · лицензия запрещает код · стек не встаёт в браузер».

**Граница, которую нельзя размывать:** продукт **не пишет свой код**. Конвейер заканчивается
ОЧЕРЕДЬЮ; код по очереди пишется в прогоне, где есть гейты и внешний судья. Система, правящая
собственные проверки, перестаёт быть проверяемой — она сама решает, что стала лучше.

Осталось (§8 плана v1.5, первым пунктом удалённой сессии): подключить к существующему импорту,
сеть за LICENSE и README с подтверждением и receipt, карточка-решение в существующей очереди
предложений, очередь на внедрение, спека `donor-arrives-by-itself.spec.mjs`.

### ✅ Связь стала следствием объекта — и честный откат половины пакета (2026-07-29)
Пункт 2 очереди `docs/HANDOFF_PROMPT.md`. В карточке объекта у списка связей появился выход
«Вся картина связей»: к общему экрану приходят ОТ конкретной связи конкретного объекта, когда
её стало мало, а не идут туда как в место назначения. Под пустым списком выхода нет — кнопка
«вся картина» там обещала бы картину, которой нет; спека держит обе половины правила.

**Вторую половину пакета — вынос «Связей» из главного меню (9 → 8) — я откатил сам.** Перенос
сработал (меню 8 пунктов, экран цел во вторичном меню, все панели на месте), но `H07` после
него краснел устойчиво: экран графа открывался прокрученным на 693 px. Две попытки починить
продукт — третий сброс прокрутки после дорисовки и `overflow-anchor: none` на оболочке —
причину не сняли. Оставлять известную регрессию ради уборки навигации нельзя, тем более что
владелец в этот момент прямо сказал: приоритет не здесь. Контракт меню вернулся к 9,
`audit-home-complexity`, CLAUDE.md §6 и пять спек — к исходным значениям, ни одно утверждение
не ослаблено.

Из отката сохранено то, что верно само по себе: выход из объекта к общей картине,
`overflow-anchor: none` (браузерная привязка прокрутки возвращала страницу на прежнее место
после дорисовки тяжёлого экрана) и третий сброс прокрутки при смене раздела.

**Урок в общий счёт:** `audit-no-gate-tampering` поймал меня в первом пакете по делу — заменив
пару «клик + expect» на помощник, который утверждает внутри, я счёл проверку сохранённой.
Аудит прав не формально: проверка, спрятанная в помощник, перестаёт читаться глазом в самой
спеке. Утверждения возвращены явными строками.

Verified: `relations-are-consequence.spec.mjs` 2/2, `final-human-product` H01+H07 зелёные,
`npm run verify`, `audit-app-boots`, `audit-no-gate-tampering` зелёные, 29 аудитов из 36
(семь красных — ровно те же, что на базовом коммите: требуют скриншотов-доказательств,
release-артефактов, чистого дерева и ветки владельца).

### ✅ П40 — донор приходит сам: ссылка → решение → очередь (2026-07-29)
Обвязка к готовому ядру `core/donor-intake.mjs`. Кинул ссылку в общий поток вместе с мыслями —
получил решение с причиной, принял — встало в очередь на внедрение.

**Правило владельца, из-за которого пришлось переписать вердикт:** изучать можно ЛЮБОЙ проект,
включая коммерческий. Лицензия ограничивает копирование и распространение кода — не чтение, не
разбор, не описание того, как проект решил задачу. Аудит и сравнение «как у них / как у нас»
законны всегда. Поэтому `studyAllowed` теперь безусловно true, а лицензия решает РОВНО ОДИН
вопрос: берём код дословно или пишем свою реализацию по итогам разбора.

Что изменилось по существу: исход «принцип» («только принцип, не код» — читалось как отказ)
стал исходом **«разбор» — «Изучаем и делаем своё»**, и это полноценная работа, а не утешение.
Отклоняем единственно когда брать нечего: всё, что проект умеет, у нас уже сделано. Ни лицензия,
ни чужой стек отказом больше не являются — `graphiti` (AGPL + Python) идёт в очередь разбором,
`graphology` (MIT + ESM) — кодом. Проверено обоими примерами.

`auditBrief(topic, donors)` — сводка по двум-трём проектам сразу: что умеют, чем сильны, КАК
именно решили. Один донор задаёт вопрос «а бывает лучше?», три на него отвечают. Меньше двух —
честно говорит, что это ещё не сравнение.

`OUR_FUNCTIONS_FOR_DONORS` — список «у нас или у них»: без него любой донор выглядит заманчиво,
и мы третий раз пишем готовое.

**Граница не сдвинута:** конвейер заканчивается ОЧЕРЕДЬЮ. Продукт не пишет свой код — код по
очереди пишется в прогоне, где есть гейты и внешний судья. Система, правящая собственные
проверки, перестаёт быть проверяемой.

**Проверить за 60 секунд**: на Доме напиши «нашёл https://github.com/graphology/graphology —
louvain, es modules» → появится предложение «graphology — Берём кодом · У нас нет: Louvain
кластеры» → прими → запись в очереди, переживает перезагрузку.
Verified: `donor-arrives-by-itself.spec.mjs` 4/4 с первого прогона (решение с причиной ·
копилефт не отказ · очередь переживает перезагрузку · обычная мысль донором не становится),
`npm run verify`, `audit-app-boots` зелёные, 29 аудитов из 36 — семь красных те же, что на
базовом коммите. Новое поле `donorQueue` проведено через ОБЕ точки `normalizeState`, спека
проверяет это перезагрузкой.

### ✅ П41 — выводы о времени, и поправка, без которой они были бы враньём (2026-07-29)
Пункт 5 очереди промта, ноль новых зависимостей. Владелец просит не «11 связей» (это состояние
базы), а наблюдение о себе: «в дни, когда работаешь дольше, закрываешь меньше дел».

`core/day-correlations.mjs` — чистый слой. Ряды по дням строятся из того, что УЖЕ лежит в
хранилище: записей за день, потрачено, закрыто дел, часы смен, отметки привычек. **Ряда про сон
нет и не выдумано** — продукт его не собирает.

Спирмен, а не Пирсон: ряды короткие и с выбросами (одна смена на 14 часов, один день с крупной
покупкой), ранговая корреляция на них не разваливается. Связанные значения получают средний
ранг — без этого самый частый день («ноль закрытых дел») сдвигал бы результат просто порядком
в массиве.

**Главное в пакете — поправка Бенджамини-Хохберга.** Пять рядов дают 10 пар, десять — 45. При
пороге p < 0.05 из 45 пар примерно две окажутся «значимыми» на ЧИСТОМ ШУМЕ. Продукт, который
показывает выдуманную закономерность, учит не верить своим выводам вообще — это дороже, чем не
показать ничего. Порог берётся по самому большому прошедшему индексу, а не по первому
непрошедшему: иначе одна «дырка» в середине режет всё за ней и метод вырождается в Бонферрони.
Сверх значимости режем по РАЗМЕРУ ЭФФЕКТА: «чуть-чуть чаще» человек использовать не может.

Вывод объясняет себя (по скольким дням, насколько сильно, в какую сторону) и честно говорит,
что это наблюдение, а не причина: что на что влияет, из корреляции не видно.

**Дефект, вскрытый по дороге:** отметки привычек лежат ВНУТРИ привычки (`habit.checkins`),
отдельной коллекции нет. Чтение несуществующего `state.habitEntries` дало бы молча пустой ряд —
вывод про привычки не появился бы никогда, и ни одной ошибки на экране. Постоянные ряды (все
нули) отбрасываются ДО счёта: они ничего не несут, но ужесточают поправку остальным.

**Проверить за 60 секунд**: накопи две недели записей → Дом → «Что я заметил»: связь между
показателями дня с числом дней и силой, либо честное «проверено пар N, пережило поправку 0».
Verified: `time-conclusions.spec.mjs` 4/4 — настоящая связь становится выводом · **на 15 парах
чистого шума выводов ноль** · коротких рядов не хватает и это сказано вслух · слабая связь
выводом не становится. `npm run verify`, `audit-app-boots` зелёные, 29 аудитов из 36.
Оба новых `core/`-модуля внесены в precache service worker (иначе офлайн падает на импорте).

### ✅ «Что изменилось» перестал состоять из пустых обещаний (2026-07-29)
Обход девяти экранов замером (не мнением) дал число: на экране «Что изменилось» — **10 пустых
блоков**. Четыре из них — механизмы безопасности, которые молчат почти всегда: снимки отката,
удалённые заметки, повреждённые записи, дубликаты на рассмотрении. Каждый занимал заголовок плюс
строку «ничего нет»: четырнадцать строк, из которых владелец не узнавал НИЧЕГО.

Пока молчат все четыре — они дают одну строку «Ничего не ждёт внимания», где каждое состояние
названо своими словами и своим маркером. **Скрытия данных здесь нет** (§7 запрещает скрытое
поведение и в показе тоже): ни одно состояние не пропало, пропало их повторение заголовками.
Появилось что-то хоть в одном — секции возвращаются целиком, потому что тогда там есть что делать.

**Корзина в свёртку НЕ входит,** хотя тоже пуста: у неё свой срок хранения и кнопка отмены
последнего удаления — там есть что делать и в пустом состоянии. По той же причине остались
«Правила системы» (кнопка добавить), «Obsidian vault» (кнопка сканировать) и «Восстановление
бэкапа» (кнопка загрузить): пустой блок с действием — это вход, а не обещание.

**Существующая спека сохранена настоящей, а не ослаблена:** `import-pipeline` после слияния
последнего дубликата ждёт `merge-review-empty` — маркер живёт внутри общей строки, и проверка
осталась верной по смыслу. Прогнана: 1/1.

Итог замера: **10 пустых блоков → 6**, и все шесть несут действие. Ошибок страницы 0.
Verified: `import-pipeline` 1/1, `npm run verify`, `audit-app-boots` зелёные, 29 аудитов из 36.

**Открыто и не спрятано** (записано в промт §2B, не сделано): «Лента» 9518 px — простыня на пять
прокруток; «Сегодня» переливается вбок при непустом хранилище (рама ни при чём — `main`
укладывается в 1411 из 1440, причина не найдена, наугад CSS не правил); внутренние термины
`canvas`/`Product Brain`/`Graph`/`Control` на двух экранах.

### ✅ П42 — «В этот день» и сверка списка доноров с кодом (2026-07-29)
Пункт 7 очереди промта: ноль зависимостей, данные уже лежали — дата создания есть у каждой
записи, новой коллекции не понадобилось.

**Почему не только «год назад».** Хранилищу владельца несколько месяцев: правило «ровно 365
дней» не сработало бы ни разу, и функция была бы мёртвой при зелёном тесте. Вехи идут лесенкой —
год · полгода · три месяца · месяц, — и каждая подписана СВОИМ словом. Дальняя веха сильнее
ближней: движение видно на большем плече. «Месяц назад» нигде не выдаётся за «год назад» —
на это стоит отдельная спека, потому что соврать здесь ради тёплого чувства проще всего.

Допуск ±3 дня: ровно в дату запись есть далеко не всегда, а «примерно тогда» честно, если
сказать вслух, на сколько промахнулись — подпись так и говорит («на 1 день раньше»).

**Дефект, который поймала своя же проверка:** сдвиг на месяц назад от 31 марта без поправки даёт
3 марта, и подпись «месяц назад» начинает врать. Берём последний день короткого месяца.

**Донор graphology проверен по-настоящему и отклонён по стеку, а не по лицензии.** Поставил все
три пакета, прочитал LICENSE файлами: MIT у всех. Но `graphology.esm.js` импортирует Node-модуль
`events`, а `graphology-communities-louvain` и `graphology-metrics` — вообще CJS с `require()`:
все три требуют бандлера, которого у проекта нет (CLAUDE.md §2). Пакеты удалены, чтобы
`package.json` не врал о стеке. Это ровно тот исход, который называет наш собственный
`classifyStack`, — «изучаем и делаем своё», и это не отказ, а работа.

**И тут же вскрылась ошибка вчерашнего пакета:** `OUR_FUNCTIONS_FOR_DONORS` помечал Louvain,
PageRank и betweenness как «у нас нет». Сверка с кодом: Louvain живёт в
`core/graph-math.mjs` (`louvainPartition`), betweenness — в `computeBetweenness`, PageRank — в
расчёте влияния. Список посоветовал бы внедрять готовое — ровно та ошибка, ради предотвращения
которой он и заведён. Помечено `done: true` по факту кода, а не по памяти.

**Проверить за 60 секунд**: Дом → «Что я заметил»: строка «Год назад ты писал: …» с датой и
тем, насколько промахнулись. На молодом хранилище строки нет — и это правильно.
Verified: `on-this-day.spec.mjs` 5/5 (год подписан годом · дальняя веха сильнее · месяц не
выдаётся за год · молодое хранилище молчит · короткий месяц не уводит веху), `npm run verify`,
`audit-app-boots` зелёные, 29 аудитов из 36. Модуль внесён в precache service worker.

### 🔶 Лемматизация: замер отменил план, и это результат (2026-07-29)
Пункт 6 очереди предполагал `Az.js` (9,7 МБ словаря). Перед тем как тащить такой вес в
локально-первое офлайн-приложение, проверен алгоритмический путь: стеммер Портера для русского
(Snowball) написан нативно — `core/ru-stem.mjs`, четыре шага, ноль веса.

**Замер на живых парах отменил этот путь, а не подтвердил.** Стеммер разводит то, что сейчас
ложно слипается на обрезке в пять знаков («память»/«памятник», «стройка»/«стройный»,
«машина»/«машинально»), но ЛОМАЕТ верные склейки: «память»→«памя» против «памяти»→«памят»,
«кредит»→«кред» против «кредита»→«кредит». Причина не в реализации, а в самом алгоритме —
он не знает частей речи и срезает «ть»/«ит» у существительных.

Подключение обменяло бы один класс ошибок на другой, поэтому `stemRu` **написан, покрыт спекой
и намеренно НЕ подключён** к `themeWords`. Темы графа остались на прежнем правиле.

**Польза замера:** у Az.js впервые появилось обоснование девяти мегабайт — словарная
лемматизация единственная закрывает обе колонки сразу. Условия записаны в промт §2C: грузить
лениво, в precache не класть, `stemRu` держать запасным путём на случай, когда словарь не
загрузился (офлайн честнее пустоты).

Это тот случай, когда правильный результат пакета — «не внедрять», и он стоит дороже зелёной
галочки: без замера мы бы поставили 9,7 МБ вслепую либо подключили стеммер и получили новые
ложные темы вместо старых.

### ✅ «Сегодня» перестал ехать вбок — и гейт, который поймает это впредь (2026-07-29)
Дефект из обхода девяти экранов (промт §2B), который в прошлый раз я НЕ дожал и честно записал
как «причина не найдена». Дожат.

**Причина.** У панели `today-layout` была жёсткая четвёрка колонок
`minmax(260px, .8fr) minmax(320px, 1.1fr) minmax(260px, .8fr) 260px`. Её минимумы вместе с тремя
зазорами по 14 px дают **ровно 1142 px** — то самое число, которое я мерил на всех секциях
экрана. Главная колонка при окне 1440 — около 1000 px. Сетка физически не могла сжаться,
распирала страницу вбок, и за ней растягивались ВСЕ соседние секции «Сегодня»: поэтому перелив
выглядел как общий, а не как дефект одной панели.

**Почему это не ловил медиазапрос и не мог поймать.** Он смотрит на ширину ОКНА (`max-width:
920px`), а зажата колонка ВНУТРИ рамы: при окне 1440 у панели всего ~1000. Условие на окно в
принципе не описывает ширину контейнера — отсюда дефект на обычном десктопе, где «широкий
монитор» звучит как «проблем быть не должно».

**Решение:** `repeat(auto-fit, minmax(min(220px, 100%), 1fr))` — колонки переносятся по
ДОСТУПНОМУ месту. На широком экране остаются четыре, в тесноте переносятся. `min(220px, 100%)`
не даёт колонке потребовать больше, чем есть у контейнера. Тот же приём, которым в этом
репозитории уже чинили панель «Сценарий», — правило, а не разовая заплатка.

**Гейт V6 проверен ОБРАТНЫМ ходом**, иначе он был бы украшением: со старой сеткой спека
краснеет, с фиксом зелёная. Меряет перелив документа на восьми рабочих экранах при окне 1440 —
именно там, где владелец работает каждый день.

**Проверить за 60 секунд**: открой «Сегодня» на широком экране — горизонтальной прокрутки нет,
колонки панели переносятся вместо того, чтобы вылезать за край.
Verified: `visual-integrity.spec.mjs` 6/6 (V6 новый, проверен откатом CSS), `npm run verify`,
29 аудитов из 36. Обход девяти экранов повторно: перелива нет НИ НА ОДНОМ, ошибок страницы 0.
